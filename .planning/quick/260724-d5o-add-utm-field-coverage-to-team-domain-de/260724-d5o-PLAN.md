---
phase: 260724-d5o-add-utm-field-coverage
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/test/team-domain-denial.integration.test.ts
autonomous: true
requirements: [TEAM-06]
must_haves:
  truths:
    - "A Member with no membership on a domain is denied a PATCH of the UTM trio (utmSource/utmMedium/utmCampaign) on that domain's Link, identically to other Link fields (404, no existence oracle)."
    - "An account-admin CAN PATCH the same UTM trio on a foreign-domain Link (200), proving the D-09-02 bypass covers UTM fields too."
  artifacts:
    - "apps/api/test/team-domain-denial.integration.test.ts (extended with UTM coverage)"
  key_links:
    - "PATCH /api/links/:id domain-access gate (resolveOwnedLink -> requireDomainAccess) fires before the request body is applied, so UTM fields inherit the exact same authorization outcome as title/target/slug."
---

<objective>
Close the v1.0 milestone tech-debt item "UTM denial coverage (Phase 9/8 seam)": the existing
TEAM-06 domain-scoped denial suite proves a Member cannot reach an unassigned domain's
Links/QR/Analytics resources, but it never explicitly exercises the Phase 8 UTM fields
(utmSource/utmMedium/utmCampaign) that are written through the same `PATCH /api/links/:id`
gate. This is a pure test-coverage gap — the UTM fields are plain allowlisted columns on the
existing PATCH endpoint (routes/links.ts:110-112), with no separate endpoint and no new
authorization surface — so no production code changes.

Add explicit coverage that a Member's PATCH of the UTM trio on a foreign-domain Link is
denied identically to other fields (Task 1), and — matching the suite's own denial + admin-
bypass symmetry — that an account-admin CAN PATCH the same UTM trio on a foreign Link (Task 2).

Purpose: Make the "UTM fields inherit the domain-access gate" invariant explicit and
regression-proof rather than merely implied by the existing `{ title }` PATCH case.
Output: An extended `apps/api/test/team-domain-denial.integration.test.ts` with green assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# The file being extended — read its existing structure/helpers before editing:
@apps/api/test/team-domain-denial.integration.test.ts

# Confirms the PATCH allowlist includes the UTM trio (utmSource/utmMedium/utmCampaign),
# so a UTM-bearing PATCH body is realistic and the gate rejects before applying it:
@apps/api/src/routes/links.ts

Reference facts already established during planning (do not re-derive):
- The UTM fields are EXACTLY three: `utmSource`, `utmMedium`, `utmCampaign`. There is no
  `utmTerm` / `utmContent` column — `utmTerm` exists only as a mass-assignment negative
  example in links.integration.test.ts and MUST NOT be used here.
- The existing exhaustive `table: DenialCase[]` (first `it()` block) already has a
  `PATCH /api/links/:id` case with `payload: { title: "Attacker rename" }` and
  `expectedStatus: 404`. The new UTM case uses the SAME expectedStatus (404) because the
  domain-access gate (IDOR, no existence oracle) fires regardless of body fields.
- The admin-bypass `it()` block (last test) seeds `readLinkId`/`patchLinkId`/`deleteLinkId`
  via `seedLink(ownerId, domainId, "...")` and asserts admin PATCH of `title` returns 200 and
  echoes the value. The response DTO includes `utmSource`/`utmMedium`/`utmCampaign`
  (lib/links.ts `toLinkDTO`, packages/shared LinkDTO), so an admin UTM PATCH response can be
  asserted the same way.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a UTM-trio PATCH denial case to the Member exhaustive denial table</name>
  <files>apps/api/test/team-domain-denial.integration.test.ts</files>
  <action>
    In the first `it()` block ("Member: every Link/QR/Analytics-by-id endpoint rejects..."),
    add ONE new entry to the `table: DenialCase[]` array, placed immediately after the existing
    `PATCH /api/links/:id` (title) case so the two PATCH-denial cases sit together. The new case:
    - method: "PATCH"
    - url: template literal targeting the seeded foreign `linkId` (reuse the same `${linkId}` the
      adjacent title case uses — it is already in scope from `seedForeignFixtures`).
    - payload: an object setting all three real UTM fields to attacker-supplied non-empty strings,
      e.g. utmSource, utmMedium, utmCampaign each given a distinct attacker value. Use ONLY these
      three keys — do NOT include utmTerm/utmContent (no such columns exist).
    - expectedStatus: 404 (identical to the adjacent title case — the domain-access gate rejects
      the foreign link before any body field is applied; the UTM fields are allowlisted on the
      PATCH schema, so this is a genuine UTM write attempt that the gate must still deny).
    - note: a short descriptive string identifying it as the UTM-trio PATCH case and referencing
      that it is denied identically to other Link fields (D-08-01/D-08-05 seam through TEAM-06).
    The existing `for (const testCase of table)` loop already asserts each case, so no loop change
    is needed. Keep the file's existing formatting/quote style and the `DenialCase` shape exactly.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api test team-domain-denial</automated>
  </verify>
  <done>
    The denial suite runs green with one additional assertion: a Member's
    `PATCH /api/links/:id` carrying utmSource/utmMedium/utmCampaign on a foreign-domain Link
    returns 404, exactly like the pre-existing title PATCH case. No production files changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add the admin-bypass positive half for the UTM trio</name>
  <files>apps/api/test/team-domain-denial.integration.test.ts</files>
  <action>
    In the final `it()` block ("Account-admin reaches the SAME foreign resources the Member is
    denied..."), mirror the suite's denial + admin-bypass symmetry for UTM fields:
    - Seed one dedicated foreign Link alongside the existing read/patch/delete links, e.g.
      `const utmLinkId = await seedLink(ownerId, domainId, "admin-allowed-utm-link");` (a distinct
      link so it does not disturb the existing title/delete assertions).
    - Inject `PATCH /api/links/${utmLinkId}` with the admin cookie and a payload setting the three
      UTM fields (utmSource/utmMedium/utmCampaign) to admin-supplied values.
    - Assert `statusCode` is 200 and that the response JSON echoes at least one UTM field back
      (e.g. `expect(patchUtm.json().utmSource).toBe(<value>)`), following the exact pattern the
      existing admin title PATCH uses (`expect(patchLink.json().title).toBe(...)`).
    Use only the three real UTM keys. Do not modify the Member denial assertions in Task 1.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api test team-domain-denial</automated>
  </verify>
  <done>
    The admin-bypass test additionally proves an account-admin PATCH of the UTM trio on a
    foreign-domain Link returns 200 and reflects the written UTM value — establishing the
    denied-for-Member / allowed-for-admin symmetry for UTM fields. Suite is fully green.
  </done>
</task>

</tasks>

<verification>
Run the full denial suite and confirm every case (including the two new UTM assertions) passes:

`pnpm --filter @kurzly/api test team-domain-denial`

This is an integration suite backed by a real testcontainers Postgres (started once via
`test/globalSetup.ts`, truncate-between-tests via `test/setupFileEach.ts`), so first-run
container startup can push total runtime past 60s — that latency is inherent to the suite and
expected. A green run is the acceptance gate; RED-first is not required since no production code
changes (the gate already returns 404 for the Member and 200 for the admin regardless of which
allowlisted body fields are present — the new assertions codify that existing behavior for UTM).
</verification>

<success_criteria>
- `apps/api/test/team-domain-denial.integration.test.ts` contains a Member UTM-trio PATCH denial
  case (404) in the exhaustive table and an admin UTM-trio PATCH bypass case (200) in the admin block.
- `pnpm --filter @kurzly/api test team-domain-denial` passes with no failures.
- No files outside the single test file are modified.
- Only utmSource/utmMedium/utmCampaign are referenced (no utmTerm/utmContent).
</success_criteria>

<output>
Create `.planning/quick/260724-d5o-add-utm-field-coverage-to-team-domain-de/260724-d5o-SUMMARY.md` when done
</output>
