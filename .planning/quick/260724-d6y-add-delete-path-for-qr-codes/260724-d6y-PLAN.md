---
phase: 260724-d6y-add-delete-path-for-qr-codes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/qrCodes.ts
  - apps/api/test/qrCodes.integration.test.ts
  - apps/web/src/api.ts
  - apps/web/src/components/QrStudioPanel.vue
  - apps/web/src/components/QrStudioPanel.test.ts
  - apps/web/src/views/QrCodesView.vue
  - apps/web/src/views/QrCodesView.test.ts
autonomous: true
requirements: [WR-07]

must_haves:
  truths:
    - "An authenticated user can delete a QR code they own from the QR Studio and the card disappears from the list."
    - "A DELETE against a QR code the caller cannot access (or that does not exist) returns 404 and leaves every row intact."
    - "Deleting a dynamic QR code also removes its remap-history rows (FK cascade), leaving no orphaned audit rows."
    - "The delete confirm dialog and success toast reuse the existing Link-delete copy with 'Link' replaced by 'QR-Code'."
  artifacts:
    - "apps/api/src/routes/qrCodes.ts — DELETE /api/qr-codes/:id route"
    - "apps/web/src/api.ts — deleteQrCode(id) client function"
    - "apps/web/src/components/QrStudioPanel.vue — delete button + confirm dialog + deleted emit"
  key_links:
    - "QrStudioPanel @deleted -> QrCodesView removes the card from qrCodes.value and reselects"
    - "DELETE route -> resolveOwnedQrCode IDOR guard (same domain-scoped check every other QR route uses)"
    - "deleteQrCode client -> DELETE /api/qr-codes/:id -> prisma.qrCode.delete (QrRemapHistory cascades)"
---

<objective>
Add a delete path for QR codes: a backend `DELETE /api/qr-codes/:id` endpoint (mirroring the existing `DELETE /api/links/:id` pattern) and a frontend delete action in the QR Studio panel (mirroring `LinkDetailView`'s delete button + confirm dialog + toast). Closes v1.0 tech-debt item WR-07 (QR codes have no delete path).

Purpose: Users can currently create/style/remap QR codes but never remove one — the only missing verb in the QR lifecycle.
Output: One new backend route + integration tests, one new API client function, a delete action (button + confirm dialog) in `QrStudioPanel.vue` wired up to list removal in `QrCodesView.vue`, plus component/view tests.

Scope guard: DELETE only. Do NOT touch QR uniqueness / the one-static-QR-per-link unique index (tech-debt WR-09 is a separate parallel task).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./.claude/CLAUDE.md

# Backend delete pattern to mirror exactly (route + IDOR guard + 204/404/401 shape):
@apps/api/src/routes/links.ts        # DELETE /api/links/:id at lines 304-315
@apps/api/src/routes/qrCodes.ts      # existing QR routes + resolveOwnedQrCode (198-208), GET :id (305-314)
@apps/api/test/links.integration.test.ts   # DELETE describe block at lines 891-975 — copy this test shape

# Frontend delete pattern to mirror exactly (confirm dialog copy + toast + flow):
@apps/web/src/views/LinkDetailView.vue     # confirmDelete (356-370) + delete dialog template (573-585)
@apps/web/src/api.ts                        # deleteLink (346-352), QR client functions (399-449)
@apps/web/src/components/QrStudioPanel.vue  # studio panel: emits (51-55), studioCode (118), header (338-342)
@apps/web/src/views/QrCodesView.vue         # handleStyled (99-105), studio panel mount (370)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend DELETE /api/qr-codes/:id route + integration tests</name>
  <files>apps/api/src/routes/qrCodes.ts, apps/api/test/qrCodes.integration.test.ts</files>
  <behavior>
    Write these failing integration tests FIRST in a new `describe("DELETE /api/qr-codes/:id (route layer, IDOR guard — WR-07)")` block in apps/api/test/qrCodes.integration.test.ts, mirroring the structure of the DELETE block in apps/api/test/links.integration.test.ts (lines 891-975). Reuse the existing helpers already in this file: signInAs, resolveSessionUserId, seedOwnedDomainForRoute, seedLinkForRoute, createQrCode, and the ROUTE_OWNER_EMAIL / ROUTE_OUTSIDER_EMAIL fixtures.
    - Test 1: owner deletes an accessible QR code -> status 204, and prisma.qrCode.findUnique for that id returns null.
    - Test 2: DELETE a non-existent id -> 404, and prisma.qrCode.count() is unchanged before/after.
    - Test 3: an outsider (no membership on the QR's domain) DELETEs the owner's QR -> 404, and the row still exists afterward (IDOR guard leaks nothing, same as GET/PATCH).
    - Test 4: DELETE with no session cookie -> 401.
    - Test 5 (cascade proof): create a dynamic QR, remap it once so at least one QrRemapHistory row exists, then DELETE the QR -> 204 and prisma.qrRemapHistory.findMany({ where: { qrCodeId } }) returns []. (QrCode->QrRemapHistory is onDelete: Cascade in schema.prisma line 376, so no manual cleanup is needed — this test proves that.)
  </behavior>
  <action>
    Add `app.delete("/api/qr-codes/:id", ...)` in apps/api/src/routes/qrCodes.ts immediately after the GET "/api/qr-codes/:id" handler (ends line 314), inside registerQrCodesRoute. Mirror DELETE /api/links/:id (links.ts 304-315) exactly: resolve the caller via `resolveUserId(auth, request)` and 401 if absent; look the QR up via the existing `resolveOwnedQrCode(prisma, userId, id)` helper (do NOT invent a new authorization check — this is the same domain-scoped IDOR guard every other QR route uses) and reply 404 if it returns null; then `await prisma.qrCode.delete({ where: { id } })` and `return reply.code(204).send()`. Do not add any manual QrRemapHistory deletion — the FK cascade handles it. Add a one-line comment above the route noting WR-07 and that it reuses the same IDOR guard as GET/PATCH, matching the surrounding route comment style. Do not modify lib/qrCodes.ts (this route talks to prisma directly for delete, exactly like links.ts does).
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api test qrCodes.integration</automated>
  </verify>
  <done>All five new DELETE tests pass; the full apps/api suite stays green; the route returns 204/404/401 identically to DELETE /api/links/:id and reuses resolveOwnedQrCode unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend deleteQrCode client + QrStudioPanel delete action wired to QrCodesView</name>
  <files>apps/web/src/api.ts, apps/web/src/components/QrStudioPanel.vue, apps/web/src/components/QrStudioPanel.test.ts, apps/web/src/views/QrCodesView.vue, apps/web/src/views/QrCodesView.test.ts</files>
  <behavior>
    Write these failing tests FIRST.
    In apps/web/src/components/QrStudioPanel.test.ts (add `deleteQrCode` to the existing vi.hoisted + vi.mock("../api") block, mirroring how updateQrCode is mocked):
    - Clicking the delete button opens the confirm dialog (dialog is not present before the click).
    - Confirming: deleteQrCode is called with props.qr.id; component emits `deleted` with that id; component emits `toast` with the success message; dialog closes.
    - Cancelling the dialog calls deleteQrCode zero times and closes the dialog.
    - deleteQrCode rejecting (ApiError) emits `toast` with the failure message and does NOT emit `deleted` (the card must stay).
    In apps/web/src/views/QrCodesView.test.ts (add `deleteQrCode` to its api mock): after the child QrStudioPanel emits `deleted` for the selected QR id, that card is removed from the rendered list and selection moves to the first remaining card (or null when the list becomes empty).
  </behavior>
  <action>
    1) apps/web/src/api.ts: add `deleteQrCode(id: string): Promise<void>` mirroring `deleteLink` (346-352) exactly — `fetch(`/api/qr-codes/${id}`, { method: "DELETE" })`, throw `new ApiError(response.status, response.statusText)` when `!response.ok`. Place it next to the other QR client functions (after remapQrCode / getQrRemapHistory) with a matching one-line JSDoc.
    2) apps/web/src/components/QrStudioPanel.vue: add a delete action to the studio panel. Add a `deleted: [id: string]` entry to the existing `defineEmits` block (alongside styled/toast). Import `deleteQrCode` from ../api. Add a `showDeleteDialog` ref plus `requestDelete()`, `cancelDelete()`, and `confirmDelete()` handlers mirroring LinkDetailView.vue's confirmDelete (356-370): on success call `deleteQrCode(props.qr.id)`, close the dialog, `emit("deleted", props.qr.id)`, then `emit("toast", ...)` with the success message; on failure `emit("toast", ...)` with the failure message (the parent owns the toast display, exactly like the existing save-failure path emits `toast`). Add a delete button in the studio header area (near the studio-header at 338-342) and a confirm-dialog block in the template mirroring LinkDetailView's dialog markup (573-585). REUSE THE LOCKED COPY — mirror the Link-delete confirm dialog structure with the word "Link" replaced by "QR-Code": dialog title is the German "QR-Code löschen?" (from "Link löschen?" at LinkDetailView 575); the body mirrors "… wird gelöscht. Bestehende Aufrufe dieses Links leiten danach nicht mehr weiter." with "dieses Links" -> "dieses QR-Codes" and the leading identifier being the panel's existing `studioCode` (118) in place of hostname/slug; cancel button label "Abbrechen"; confirm button label "Löschen". Success toast message: "QR-Code gelöscht" (from LinkDetailView's "Link gelöscht" at 361). Failure toast message: "QR-Code konnte nicht gelöscht werden." (from "Link konnte nicht gelöscht werden." at 368). Do not invent new wording — derive every string from the Link-delete originals by that single Link->QR-Code substitution.
    3) apps/web/src/views/QrCodesView.vue: bind `@deleted` on the `<QrStudioPanel>` (mount at 370, next to `@styled="handleStyled"`) to a new `handleDeleted(id: string)` that removes the entry from `qrCodes.value` and reselects — set `selectedQrId.value` to the first remaining card's id or `null` when empty, mirroring resolveInitialSelection's first-or-null shape (147-155). Do not add a second toast here — the panel already emits the success toast through the existing `@toast="showToast"` binding.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/web test QrStudioPanel && pnpm --filter @kurzly/web test QrCodesView</automated>
  </verify>
  <done>New component + view tests pass; clicking delete in the QR Studio opens a "QR-Code löschen?" confirm dialog; confirming removes the card, reselects, and shows the "QR-Code gelöscht" toast; a failed delete shows the failure toast and keeps the card; the whole apps/web suite stays green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser SPA -> `/api/qr-codes/:id` (DELETE) | Untrusted, authenticated request; caller-supplied `:id` could target any QR in the instance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-WR07-01 | Elevation of Privilege (IDOR) | DELETE /api/qr-codes/:id | high | mitigate | Reuse `resolveOwnedQrCode` (domain-scoped) verbatim before any delete; return 404 (not 403) for not-found and forbidden alike so the two are indistinguishable — identical to DELETE /api/links/:id and the existing QR GET/PATCH routes. No new auth check introduced. |
| T-WR07-02 | Denial of Service | DELETE /api/qr-codes/:id | low | accept | Same authenticated, rate-limited surface as the existing Link/QR mutation routes; a single-row delete adds no new abuse vector beyond what PATCH already exposes. |
| T-WR07-03 | Tampering (orphaned data) | QrRemapHistory rows | low | mitigate | Rely on the existing `onDelete: Cascade` FK (schema.prisma 376) rather than an app-layer delete loop; Task 1 Test 5 proves history rows are removed with the QR. No schema/migration change in this plan. |
</threat_model>

<verification>
- `pnpm --filter @kurzly/api test qrCodes.integration` — all DELETE cases green (204/404/401 + cascade).
- `pnpm --filter @kurzly/web test QrStudioPanel` and `pnpm --filter @kurzly/web test QrCodesView` — delete flow + list removal green.
- Full workspace check: `pnpm -r test` stays green (no regressions in adjacent QR/Link suites).
- Grep sanity: the new route uses `resolveOwnedQrCode` (no bespoke authorization); no `prisma.qrRemapHistory.delete` call was added (cascade only).
</verification>

<success_criteria>
- `DELETE /api/qr-codes/:id` exists, is IDOR-guarded via the existing helper, returns 204 on success and 404/401 exactly like DELETE /api/links/:id.
- Deleting a dynamic QR removes its remap history via cascade (proven by test).
- QR Studio shows a delete button; confirming opens the "QR-Code löschen?" dialog and removes the card + toasts "QR-Code gelöscht"; a failed delete keeps the card and toasts the failure copy.
- All confirm-dialog/toast copy is the Link-delete copy with "Link" -> "QR-Code" (no invented wording).
- QR uniqueness / migration logic untouched (WR-09 stays out of scope).
</success_criteria>

<output>
Create `.planning/quick/260724-d6y-add-delete-path-for-qr-codes/260724-d6y-01-SUMMARY.md` when done.
</output>
