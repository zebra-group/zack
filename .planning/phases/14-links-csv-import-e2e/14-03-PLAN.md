---
phase: 14-links-csv-import-e2e
plan: 03
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - apps/e2e/tests/authed/csv-import-happy.spec.ts
autonomous: true
requirements: [LINKS-E2E-02]

must_haves:
  truths:
    - "Uploading a valid two-row CSV through the real hidden file input renders a preview of exactly 2 valid rows (`2 gültig`, zero skipped) — the backend-computed ImportPreviewResult, which this view only renders"
    - "Committing writes exactly the two previewed rows — the commit response importedCount is 2 AND a direct-Prisma findMany scoped to the two slugs returns exactly 2 rows, with the matching target URLs on the baseline domain (no silent extras)"
  artifacts:
    - apps/e2e/tests/authed/csv-import-happy.spec.ts
  key_links:
    - "the default domain is selected in the dropdown BEFORE the file is uploaded, so the defaultDomainId watch fires exactly one preview (14-RESEARCH.md Pitfall 3); every assertion waits on the specific preview/commit POST response"
    - "the CSV is built with buildImportCsv (14-01) so its header is `ziel_url,slug,domain` by construction (Pitfall 4); the DB assertion uses createE2ePrisma scoped to the two per-test random slugs, proving commit == preview against the real database"
---

<objective>
Prove the CSV bulk-import happy path (LINKS-E2E-02) end-to-end through the real upload → preview → commit UI: a valid two-row CSV previews as two valid rows, and commit writes exactly those rows — asserted directly against PostgreSQL, with no silent extras. Commit re-sends the same CSV text (there is no server-side preview cache or preview ID — 14-RESEARCH.md Summary point 1), so the spec keeps the built CSV string in scope across both calls; the view does the same.

This spec asserts REAL, currently-passing behaviour (routes/links.ts + lib/links.ts read in full; no changes anticipated). If a genuine app-code gap surfaces, STOP and flag it — do not weaken an assertion.

Purpose: Close LINKS-E2E-02 with a preview-then-commit flow that is DB-asserted to write exactly the previewed rows.
Output: `apps/e2e/tests/authed/csv-import-happy.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-links-csv-import-e2e/14-RESEARCH.md
@apps/web/src/views/LinksImportView.vue
@apps/web/src/api.ts
@packages/shared/src/index.ts
@apps/e2e/src/csv.ts
@apps/e2e/src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LINKS-E2E-02 — valid CSV preview + commit, DB-asserted (chromium-admin)</name>
  <read_first>
    - apps/web/src/views/LinksImportView.vue (VERIFIED: hidden `<input type="file" class="hidden-file-input">`; the default-domain `<select>` whose first option is `— keine —` (value "") then one option per active domain by hostname; uploading fires readFile → csvText → loadPreview → POST /api/links/import/preview; preview renders `.preview-summary` with `.valid-count` text `N gültig` and `.skipped-count` only when skippedCount>0, and one `.preview-row` per row; commit button text is `Importieren (N)` and is disabled until validCount>0; handleCommit → POST /api/links/import/commit → toast `N Links importiert` → router.push to /links after 900ms; the `watch(defaultDomainId,…)` re-fires preview when the default domain changes AFTER a file is loaded — Pitfall 3)
    - apps/web/src/api.ts (previewImport(csvText, defaultDomainId) → POST /api/links/import/preview {csv, defaultDomainId}; commitImport(csvText, defaultDomainId) → POST /api/links/import/commit — the SAME body, re-run with mutate=true)
    - packages/shared/src/index.ts (ImportPreviewResult: validCount/skippedCount/rows; ImportRowResult uses `zielUrl` NOT targetUrl — type the response bodies via these if inspecting them; ImportCommitResult: importedCount/skippedCount/rows/partial)
    - apps/e2e/src/csv.ts (buildImportCsv + IMPORT_CSV_HEADER from 14-01)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME)
    - 14-RESEARCH.md (Code Examples: setInputFiles on the hidden input, waitForResponse the preview POST, DB findMany after commit; Pitfall 3 select-domain-before-upload; Pitfall 4 exact header)
  </read_first>
  <files>apps/e2e/tests/authed/csv-import-happy.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` (documented db-isolation truncate-race resilience, as in 14-02) and a `test.beforeEach` skip so the spec runs only under chromium-admin (member/domain-scoped import authz is Phase 17 — CONTEXT.md Deferred Ideas), since testMatch registers every tests/authed spec under both authenticated projects.

    Single test "valid CSV previews two rows and commit writes exactly those rows":
    1. Mint two per-test random valid-shape slugs `e2e-import-a-<hex>` and `e2e-import-b-<hex>` and two distinct targets `https://example.com/import-a-<hex>` / `…-b-<hex>`. Build the CSV via buildImportCsv of two rows with those zielUrl+slug and a BLANK domain (so both rows fall back to the UI's selected default domain).
    2. page.goto("/links/import"); expect the URL "/links/import".
    3. SELECT the baseline domain in the default-domain dropdown FIRST — before any upload — using selectOption by the baseline hostname label. (Selecting before upload means the defaultDomainId watch does not fire a second preview; csvText is still empty at this point — Pitfall 3.)
    4. setInputFiles on the `input[type="file"]` with a Buffer of the built CSV (name links-import-valid.csv, mimeType text/csv). waitForResponse the POST /api/links/import/preview (method POST).
    5. PREVIEW assertions: `.valid-count` shows `2 gültig`; exactly two `.preview-row` elements render; none carries the `.preview-row.invalid` class; the skipped-count span is absent (skippedCount 0). (Optionally also assert the preview response body typed as ImportPreviewResult has validCount 2 and rows length 2.)
    6. COMMIT: click the commit button (name starts with `Importieren`); waitForResponse the POST /api/links/import/commit. Assert the commit response's importedCount is 2 (proves commit processed exactly the previewed valid rows).
    7. DB assertion (LINKS-E2E-02 "asserted directly against the database", no silent extras): createE2ePrisma(); prisma.link.findMany where slug in [slugA, slugB] → length exactly 2; each row's targetUrl equals the CSV target for that slug; each row's domainId is the baseline domain's id. $disconnect in a finally.

    Do NOT extract or reuse any "preview id" from the preview response — none exists (14-RESEARCH.md anti-pattern); commit re-sends the CSV text.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/csv-import-happy.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the CSV is uploaded through the real hidden file input and the preview/commit are driven through the real UI buttons
    - preview asserts exactly 2 valid rows / zero skipped; commit asserts importedCount 2
    - the DB findMany is scoped to the two per-test slugs and asserts exactly 2 rows with matching targets on the baseline domain (no silent extras)
    - default domain selected BEFORE upload (single preview fire); every assertion waits on the specific preview/commit POST
    - scoped to chromium-admin, per-test random slugs; passes green; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>LINKS-E2E-02 proves a valid CSV previews correctly and commit writes exactly the previewed rows, verified at the database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) → /api/links/import/* | untrusted CSV text crosses into the bulk-import parser/validator |
| test runner → E2E Postgres :5433 | direct-Prisma row-count/target assertion after commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-04 | Tampering | commit writing more/fewer rows than previewed | medium | mitigate | The commit-response importedCount AND a DB findMany scoped to the two slugs (exactly 2, matching targets/domain) prove commit == preview with no silent extras |
| T-14-05 | Denial of Service | CSV row-limit / size exhaustion (MAX_IMPORT_ROWS=500, CSV_MAX_LENGTH) | low | accept | Enforced server-side and already unit-tested; this spec uses a 2-row fixture and does not re-test the boundary (out of scope per REQUIREMENTS.md Out-of-Scope) |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/csv-import-happy.spec.ts --project=chromium-admin` passes.
- Re-run at `--workers=1` and default parallelism; the DB assertion stays scoped to the per-test slugs so it is independent of any other file's links.
</verification>

<success_criteria>
LINKS-E2E-02 passes: a valid CSV previews as two valid rows and commit writes exactly those two rows, asserted directly against PostgreSQL.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/csv-import-happy.spec.ts` — LINKS-E2E-02 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/14-links-csv-import-e2e/14-03-SUMMARY.md` when done.
</output>
