---
phase: 14-links-csv-import-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/links-crud.spec.ts
autonomous: true
requirements: [LINKS-E2E-01]

must_haves:
  truths:
    - "An admin creates a link through the REAL LinkFormModal (POST /api/links → 201) and a subsequent server GET (via the search box) returns it — proving persistence, not just the optimistic local list unshift"
    - "Editing the link through the real modal (PATCH /api/links/:id → 200) changes its target, confirmed by a server refetch showing the NEW target URL"
    - "Searching the link's unique slug narrows a multi-link list to exactly that one row (2 seeded decoys excluded) — proving real server-side filtering — and the domain-filter tab re-queries scoped to the baseline domain"
    - "Deleting through the real confirm dialog (DELETE /api/links/:id → 204) removes the row, confirmed by a server refetch that no longer returns it"
  artifacts:
    - apps/e2e/tests/authed/links-crud.spec.ts
  key_links:
    - "every mutation is re-verified by a subsequent server round-trip (the create/edit/delete HTTP status AND a fresh GET /api/links refetch), never by the optimistic local list mutation alone"
    - "per-test crypto-random slugs keep the spec collision-free under fullyParallel AND under the dual chromium-admin/chromium-member project execution of every tests/authed spec; the journey is scoped to chromium-admin because member/domain-scoped authz is Phase 17's job (AUTHZ-E2E-01, CONTEXT.md Deferred Ideas)"
---

<objective>
Prove the canonical dashboard link lifecycle (LINKS-E2E-01) end-to-end through the real UI and the real API: create a link via the actual LinkFormModal, see it in the list, edit it via the real modal, find it via the real search box and domain-filter tab, then delete it via the real confirm dialog — each step re-verified against the server, not just the optimistic local list. This establishes the link-creation-through-the-real-UI pattern Phases 15/16 reuse (CONTEXT.md: "this phase's own subject IS link creation through the UI").

This spec asserts REAL, currently-passing behaviour — routes/links.ts and lib/links.ts were read in full and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-13 TDD RED→GREEN precedent: a real bug is fixed, not papered over).

Purpose: Close LINKS-E2E-01 with a server-verified create→list→edit→search/filter→delete journey.
Output: `apps/e2e/tests/authed/links-crud.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-links-csv-import-e2e/14-RESEARCH.md
@apps/web/src/views/LinksView.vue
@apps/web/src/components/LinkFormModal.vue
@apps/web/src/api.ts
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/tests/authed/storage-state.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LINKS-E2E-01 — canonical create→list→edit→search/filter→delete journey (chromium-admin)</name>
  <read_first>
    - apps/web/src/views/LinksView.vue (VERIFIED selectors: `+ Neuer Link` primary button; search input placeholder `Suchen…` with SEARCH_DEBOUNCE_MS=250 firing GET /api/links; domain tabs are buttons whose text is the hostname e.g. `e2e.kurzly.local`; each `.table-row` shows `/<slug>` and has row-action buttons title=`Bearbeiten`/`Löschen` with @click.stop; row body click navigates to detail — click the action buttons, never the row; `.delete-dialog` confirm button is `.delete-confirm-button` text `Löschen`; empty-search state is `.no-match`; create is optimistic via links.value.unshift)
    - apps/web/src/components/LinkFormModal.vue (VERIFIED: container `.modal-dialog`; Ziel-URL input placeholder `https://example.com/sehr/lange/url` in BOTH modes; slug input placeholder `leer lassen = automatisch` only in create mode; create-mode domain <select> defaults to the first active domain; submit button text is `Link erstellen` in create mode and `Speichern` in edit mode; edit title is `Link bearbeiten`)
    - apps/web/src/api.ts (createLink → POST /api/links returns 201; updateLink → PATCH /api/links/:id; deleteLink → DELETE /api/links/:id → 204; listLinks → GET /api/links?q=&domainId=)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME = e2e.kurzly.local; the seeded admin is accountRole "admin" and reaches the baseline domain via the account-admin scope bypass in lib/authorization.ts)
    - apps/e2e/src/links.ts (createE2eLink for the two direct-Prisma decoy links — sets createdBy=admin, domainId=baseline)
    - apps/e2e/tests/authed/storage-state.spec.ts (the role-aware authed-spec precedent: branch on testInfo.project.name; here we scope the journey to chromium-admin)
    - 14-RESEARCH.md (Pitfall 1 search-debounce race → always waitForResponse the specific GET; Pitfall 2 delete-button accessible-name collision → scope the confirm to .delete-dialog; OQ-1 per-test random slugs; OQ-2 decoys prove narrowing)
  </read_first>
  <files>apps/e2e/tests/authed/links-crud.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` with a doc-comment explaining the reason — apps/e2e/tests/smoke/db-isolation.spec.ts truncates the Link table concurrently during the FULL-suite phase gate (documented cross-file race that Phase 12 handled with fetchWithFixtureRaceRetry); a whole-journey retry with fresh per-test random slugs is the UI-flow equivalent and is collision-free. Also add a `test.beforeEach(async ({}, testInfo) => test.skip(testInfo.project.name !== "chromium-admin", "LINKS-E2E-01 canonical journey is admin-scoped; member/domain-scoped link authz is Phase 17 (AUTHZ-E2E-01), per CONTEXT.md Deferred Ideas"))` so the spec runs only under chromium-admin even though testMatch registers every tests/authed spec under both authenticated projects.

    Single test "canonical link lifecycle through the real UI and API":
    1. Mint per-test crypto-random valid-shape identifiers: target slug `e2e-crud-<hex>`, and two distinct targets `https://example.com/crud-a-<hex>` (create) and `https://example.com/crud-b-<hex>` (edit). Use lowercase-alphanumeric-hyphen slugs so they never trip SLUG_INVALID_SHAPE.
    2. createE2ePrisma(); seed TWO decoy links via createE2eLink with their own random slugs `e2e-decoy-<hex>` and distinct targets — so the later search assertion proves genuine narrowing (a one-item list cannot, per OQ-2). Keep the client for the final assertion; $disconnect in a finally.
    3. page.goto("/links"); expect the URL to be "/links" (storageState is already authenticated — no login here).
    4. CREATE: click the `+ Neuer Link` button; within `.modal-dialog` fill the Ziel-URL placeholder input with target-A and the create-mode slug placeholder input with the target slug; click `Link erstellen`. waitForResponse the POST /api/links returning 201. Assert a `.table-row` containing the slug becomes visible.
    5. EDIT: within the target `.table-row` (locate `.table-row` filtered by the slug), click the row-action with title `Bearbeiten`; in the `.modal-dialog` (title `Link bearbeiten`) clear+fill the Ziel-URL placeholder input with target-B; click `Speichern`. waitForResponse the PATCH /api/links/:id returning ok. (Toast `Änderungen gespeichert` is a secondary check.)
    6. SEARCH (narrowing + edit-persistence proof): fill the `Suchen…` input with the unique target slug; waitForResponse the matching GET /api/links (method GET, url contains /api/links). Assert exactly ONE `.table-row` is present, it contains the slug, and its target cell shows target-B (proving the edit persisted server-side AND the two decoys are excluded — real server-side narrowing).
    7. DOMAIN FILTER: clear the search input and waitForResponse the GET; click the domain tab button whose name is the baseline hostname; waitForResponse the GET /api/links (url contains domainId). Assert the target `.table-row` (by slug) is present — the domainId-scoped query path works.
    8. DELETE: within the target `.table-row` click the row-action title `Löschen`; assert `.delete-dialog` is visible; click the confirm button scoped to `.delete-dialog` (`.delete-confirm-button`, text `Löschen`). waitForResponse the DELETE /api/links/:id returning 204. Assert the target row is gone.
    9. DELETE persistence proof: fill the `Suchen…` input with the target slug again; waitForResponse the GET; assert zero `.table-row` for that slug (the `.no-match` empty result), proving the row is gone from the SERVER, not just the local list.

    Always waitForResponse the specific GET before asserting on rendered rows (never assert immediately after .fill — Pitfall 1). Never click a `.table-row` body (navigates to detail) — only its title-scoped action buttons.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/links-crud.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the journey drives the REAL create modal, edit modal, search box, domain tab, and delete confirm dialog (no API-only shortcuts for the create/edit/delete steps)
    - create/edit/delete are each re-verified by both the HTTP status (201/200/204) and a fresh server GET refetch
    - the search assertion proves narrowing against ≥2 seeded decoys (exactly one row returned)
    - the spec is scoped to chromium-admin (skipped under chromium-member) and uses per-test random slugs
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>LINKS-E2E-01 proves the create→list→edit→search/filter→delete lifecycle end-to-end through the real UI and the real database.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) → /api/links/* | authenticated dashboard create/edit/delete/list mutations |
| test runner → E2E Postgres :5433 | direct-Prisma decoy seeding + final delete assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-02 | Elevation of Privilege | link create/edit/delete IDOR guard (resolveOwnedLink, scopedDomainIds) | low | accept | Exercised incidentally under the admin session; the full role × resource denial matrix is Phase 17 (AUTHZ-E2E-01) plus the existing v1.0 integration Denial-Suite — this phase deliberately does not re-prove it |
| T-14-03 | Tampering | optimistic UI reporting a server write that did not persist | medium | mitigate | Every create/edit/delete is re-verified by a fresh GET /api/links refetch (and the mutation's HTTP status), so a silently-failed write cannot pass as green |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/links-crud.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (Playwright registers the spec under chromium-member too, where it is skipped) — confirm no search-debounce/DB race this spec introduces.
</verification>

<success_criteria>
LINKS-E2E-01 passes: the full link lifecycle works through the real dashboard UI, with each mutation confirmed against the server and search proven to narrow a multi-link list.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/links-crud.spec.ts` — LINKS-E2E-01 canonical journey (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/14-links-csv-import-e2e/14-02-SUMMARY.md` when done.
</output>
