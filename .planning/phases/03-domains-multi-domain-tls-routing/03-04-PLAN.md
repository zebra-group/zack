---
phase: 03-domains-multi-domain-tls-routing
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - apps/web/src/api.ts
  - apps/web/src/views/DomainsView.vue
  - apps/web/src/router/index.ts
  - apps/web/test/DomainsView.test.ts
autonomous: true
requirements: [DOMAIN-01, DOMAIN-02, DOMAIN-04]
must_haves:
  truths:
    - "The /domains route renders DomainsView (replacing ComingSoonView) with a domain list, add-domain row (Subdomain/Apex toggle), verify action, DNS-instructions accordion, delete confirmation, and an empty state."
    - "Adding a domain calls POST /api/domains and shows the new pending row plus a toast; verifying calls the verify endpoint and flips the status badge to 'Aktiv' on success."
    - "The DNS instructions panel shows the CNAME (subdomain) or A/ALIAS (apex) record and copies it to the clipboard with a toast."
    - "The TLS hint states TLS is issued by the operator's own reverse proxy (Caddy/Traefik), not Kurzly (D-01 wording)."
  artifacts:
    - "apps/web/src/api.ts — createDomain / listDomains / verifyDomain / deleteDomain / getDomainInstructions typed client fns"
    - "apps/web/src/views/DomainsView.vue — the Domains screen (860px container)"
    - "apps/web/src/router/index.ts — /domains mapped to DomainsView"
    - "apps/web/test/DomainsView.test.ts — @vue/test-utils component suite"
  key_links:
    - "DomainsView consumes DomainDTO from @kurzly/shared and the typed api client fns; the router swaps ComingSoonView -> DomainsView only for /domains."
  prohibitions:
    - "The screen container must use 860px max-width (NOT the generic 1060px .screen-container)."
    - "The TLS hint must NOT claim Kurzly issues or terminates certificates."
    - "The delete action must NOT delete immediately — it must open a confirmation dialog first (irreversible: affects links)."
---

<objective>
Deliver the Domains frontend slice: replace the `ComingSoonView` placeholder at `/domains` with the real `DomainsView` (list / add / verify / DNS-instructions / copy / delete-confirm / empty state) per 03-UI-SPEC.md, and extend the typed API client so the screen drives the Phase-3 backend endpoints.

Purpose: makes DOMAIN-01/02/04 user-facing — an admin registers, verifies, reads DNS instructions for, and removes domains from the dashboard, matching the locked design tokens (Geist fonts, lime accent, 860px container).

Output: extended `api.ts`, `DomainsView.vue`, router swap, and a `@vue/test-utils` component suite.
</objective>

## Phase Goal (user story)

**As an** admin, **I want** a Domains screen where I can add a domain, see its status, copy the exact DNS record, click "Jetzt prüfen", and remove a domain, **so that** I can manage my custom domains end-to-end from the dashboard. (Derived from Phase 3 Success Criteria 1-3 + UI-03, DOMAIN-01/02/04.)

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-domains-multi-domain-tls-routing/03-UI-SPEC.md
@.planning/phases/03-domains-multi-domain-tls-routing/03-PATTERNS.md
@.planning/phases/03-domains-multi-domain-tls-routing/03-01-SUMMARY.md
@.planning/phases/03-domains-multi-domain-tls-routing/03-02-SUMMARY.md
@apps/web/src/api.ts
@apps/web/src/views/ComingSoonView.vue
@apps/web/src/router/index.ts
@apps/web/test/LoginView.test.ts
@packages/shared/src/index.ts
</context>

## Artifacts this plan produces

- **API client:** `createDomain({hostname,type})`, `listDomains()`, `verifyDomain(id)`, `deleteDomain(id)`, `getDomainInstructions(id)` in `apps/web/src/api.ts` (each same-origin `fetch` + `parseJsonOrThrow`, per the existing canary/auth client shape).
- **View:** `apps/web/src/views/DomainsView.vue` (860px container; list/add/verify/instructions/delete/empty per 03-UI-SPEC.md).
- **Routing:** `apps/web/src/router/index.ts` — `/domains` → `DomainsView` (was `ComingSoonView`).
- **New files:** `apps/web/src/views/DomainsView.vue`, `apps/web/test/DomainsView.test.ts`.

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend the typed API client with domain CRUD/verify/instructions functions</name>
  <read_first>
    - apps/web/src/api.ts (existing shape: parseJsonOrThrow, getCanary/getSession/logout — mirror these)
    - packages/shared/src/index.ts (DomainDTO added in 03-01)
    - .planning/phases/03-domains-multi-domain-tls-routing/03-PATTERNS.md ("apps/web/src/api.ts" lines 1703-1761)
  </read_first>
  <files>apps/web/src/api.ts</files>
  <action>
    Add five functions to apps/web/src/api.ts mirroring the existing client pattern (same-origin `fetch`, `parseJsonOrThrow<T>` for JSON responses): `createDomain(data: { hostname: string; type: "subdomain" | "apex" }): Promise<DomainDTO>` — `POST /api/domains` with a JSON body and `Content-Type: application/json`; `listDomains(): Promise<DomainDTO[]>` — `GET /api/domains`; `verifyDomain(domainId: string): Promise<DomainDTO>` — `POST /api/domains/${domainId}/verify`; `deleteDomain(domainId: string): Promise<void>` — `DELETE /api/domains/${domainId}` (throw on non-ok, no JSON parse since it returns 204); `getDomainInstructions(domainId: string): Promise<{ hostname: string; type: string; verificationTarget: string; instructions: string; alternativeForApex: string | null }>` — `GET /api/domains/${domainId}/instructions`. Import the `DomainDTO` type from "@kurzly/shared". Keep `credentials` behavior identical to the existing same-origin calls (the httpOnly session cookie auto-attaches).
  </action>
  <acceptance_criteria>
    - The five functions are exported and typed against DomainDTO / the instructions shape.
    - `pnpm --filter @kurzly/web exec tsc --noEmit` succeeds (DomainDTO resolves from the rebuilt shared package).
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kurzly/web exec tsc --noEmit</automated>
  </verify>
  <done>Typed domain client functions available for the view.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build DomainsView.vue (860px, list/add/verify/instructions/delete/empty) and swap the /domains route</name>
  <read_first>
    - .planning/phases/03-domains-multi-domain-tls-routing/03-UI-SPEC.md (full Layout Contract lines 212-358, Copywriting Contract lines 170-208, Color lines 146-166 — 860px container, Failed-status text #e5484d on chip bg, accent reserved list)
    - .planning/phases/03-domains-multi-domain-tls-routing/03-PATTERNS.md ("DomainsView.vue" lines 1027-1698 — reference structure, tokens, copy)
    - apps/web/src/views/ComingSoonView.vue (screen-container/screen-header boilerplate to adapt; note it uses 1060px — DomainsView must use 860px)
    - apps/web/src/router/index.ts (the /domains route entry lines 56-61)
  </read_first>
  <files>apps/web/src/views/DomainsView.vue, apps/web/src/router/index.ts</files>
  <action>
    Create apps/web/src/views/DomainsView.vue as a Vue 3 `<script setup lang="ts">` SFC + scoped CSS using the existing CSS custom-property tokens (`--bg`/`--panel`/`--border`/`--accent`/`--mut`/`--chip`/`--text`/`--hover`) — NO Tailwind/shadcn. Implement per 03-UI-SPEC.md: a 860px-max-width `.screen-container` (NOT the generic 1060px), screen header ("Domains" / "Domains & Subdomains, die auf die Instanz zeigen"), an add-domain row (Geist Mono input with placeholder "z.B. s.meinefirma.de", a Subdomain/Apex pill toggle with the accent-active style, and a "Hinzufügen" accent button), the domain list card (Geist Mono hostname ellipsised at 230px, uppercase type badge on chip bg, status badge — Aktiv = accent pill/`#1b1b18`, DNS ausstehend = chip/`--mut`, Fehlgeschlagen = chip bg with `#e5484d` text), a "Jetzt prüfen" verify button shown only when status is not active, an "Anleitung anzeigen/ausblenden" accordion toggle, a "🗑" delete icon, the per-domain DNS instructions panel (typabhängiger Body, `--chip` code block with ⧉ copy button, apex ALIAS alt line, the D-01 TLS hint, and a "Zuletzt geprüft" line), an empty state ("🌐" / "Noch keine Domain registriert" / body), a delete confirmation modal (380px, overlay click closes, "Domain entfernen?" + body + Abbrechen/Entfernen), and a bottom-center toast pill (Geist Mono, ~1.7s). Wire actions to the api.ts functions from Task 1: `loadDomains` on setup, `handleAddDomain` (validate non-empty → createDomain → push row + toast "{domain} hinzugefügt — DNS ausstehend"; map 409 to "Diese Domain ist bereits registriert."), `handleVerify` (set loading label "Prüfe …" disabled/opacity .6 → verifyDomain → merge result; toast "{domain} verifiziert ✓" on success, inline error + red "Zuletzt geprüft" on failure, and the 429 copy "Zu viele Prüfungen. Bitte warte kurz, bevor du es erneut versuchst." on rate limit), `toggleInstructions` (getDomainInstructions on first open), `handleDelete` (open confirm dialog, only delete on confirm), `copyToClipboard` (navigator.clipboard + toast "DNS-Eintrag kopiert"), and an `autoPreselect` heuristic on input that pre-selects Subdomain/Apex (overridable). Use EXACT copy from the Copywriting Contract; the TLS hint MUST use the D-01 wording ("… TLS terminiert dein eigener Proxy, nicht Kurzly."). In router/index.ts, import DomainsView and change ONLY the `/domains` route's `component` from `ComingSoonView` to `DomainsView` (leave its `meta.requiresAuth`/label and every other route untouched). Escape any literal "@" in template text per the vue-i18n/CLAUDE.md rule (none expected here, but verify).
  </action>
  <acceptance_criteria>
    - `/domains` route's component is `DomainsView` (not `ComingSoonView`); all other routes unchanged.
    - The `.screen-container` max-width is 860px.
    - The TLS hint text contains "nicht Kurzly" (D-01 wording) and does not claim Kurzly issues certificates.
    - `pnpm --filter @kurzly/web exec tsc --noEmit` and `pnpm --filter @kurzly/web build` succeed.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kurzly/web exec tsc --noEmit && pnpm --filter @kurzly/web build</automated>
  </verify>
  <done>DomainsView renders the full Domains screen at 860px with the locked tokens/copy; /domains serves it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: @vue/test-utils component suite for DomainsView</name>
  <read_first>
    - apps/web/test/LoginView.test.ts (the @vue/test-utils + vi.mock("../src/api") pattern to mirror)
    - apps/web/src/views/DomainsView.vue (the component under test from Task 2)
    - .planning/phases/03-domains-multi-domain-tls-routing/03-VALIDATION.md (Wave 0: Domains screen component tests — list/add/verify/instructions/empty)
  </read_first>
  <files>apps/web/test/DomainsView.test.ts</files>
  <action>
    Create apps/web/test/DomainsView.test.ts using @vue/test-utils, mocking the api module (`vi.mock("../src/api", ...)`) so no real network happens (mirror LoginView.test.ts). Cover: (1) empty state renders "Noch keine Domain registriert" when listDomains resolves to []; (2) an existing domain renders its hostname, type badge, and a status badge with the correct label ("Aktiv"/"DNS ausstehend"/"Fehlgeschlagen"); (3) adding a domain calls createDomain with { hostname, type } and appends the new pending row + shows a toast; (4) clicking "Jetzt prüfen" calls verifyDomain and, on a resolved active result, updates the badge to "Aktiv"; (5) toggling the instructions accordion calls getDomainInstructions and renders the returned record + a copy button; (6) the delete icon opens the confirmation dialog and does NOT call deleteDomain until "Entfernen" is confirmed. Await Vue's `nextTick`/flushed promises after async actions before asserting.
  </action>
  <acceptance_criteria>
    - Tests assert createDomain, verifyDomain, getDomainInstructions, and deleteDomain are called with the expected arguments (and deleteDomain only after confirm).
    - Test asserts the empty state and each status-badge label render.
    - `pnpm --filter @kurzly/web test -- test/DomainsView.test.ts` green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- test/DomainsView.test.ts</automated>
  </verify>
  <done>DomainsView behavior (list/add/verify/instructions/delete-confirm/empty) proven with mocked api; component suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DomainsView → /api/domains* | Client calls the session-gated domain endpoints; the server re-authorizes every request (the UI is not the security boundary) |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Elevation of Privilege | DomainsView actions | low | mitigate | The client is a convenience only; every create/verify/delete/instructions call is independently authorized server-side (03-01/03-02) — hiding/showing a button is never the access control. |
| T-03-10 | Information Disclosure | DNS-instructions panel + TLS hint | low | mitigate | Panel shows only the operator's own CNAME/A target + the domain's hostname; the TLS hint states TLS is the operator proxy's job (D-01), never implying Kurzly holds keys/certs. |
| T-03-SC | Tampering | npm installs | n/a | N/A | No new packages — DomainsView is a hand-written SFC using existing tokens; @vue/test-utils already installed. |
</threat_model>

<verification>
- `pnpm --filter @kurzly/web test -- test/DomainsView.test.ts` — green.
- `pnpm --filter @kurzly/web exec tsc --noEmit && pnpm --filter @kurzly/web build` — green.
- Manual (end-of-phase, per 03-VALIDATION.md): pixel-fidelity of the Domains screen at 1440px Light + Dark against the prototype.
</verification>

<success_criteria>
The /domains screen is the real DomainsView at 860px with locked tokens/copy; an admin can add, verify, read DNS instructions for, copy, and remove domains from the dashboard, driving the Phase-3 API; component suite green. DOMAIN-01/02/04 are user-facing.
</success_criteria>

<output>
Create `.planning/phases/03-domains-multi-domain-tls-routing/03-04-SUMMARY.md` when done.
</output>
