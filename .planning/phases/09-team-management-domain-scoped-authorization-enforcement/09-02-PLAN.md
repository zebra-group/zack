---
phase: 09-team-management-domain-scoped-authorization-enforcement
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - apps/api/src/lib/authorization.ts
  - apps/api/test/authorization.test.ts
autonomous: true
requirements: [TEAM-06]
user_setup: []

must_haves:
  truths:
    - "requireDomainAccess grants access to an account-admin on ANY domain regardless of DomainMembership, at any minRole (D-09-02)"
    - "scopedDomainIds returns EVERY domain id for an account-admin, even domains they hold no membership on (D-09-02)"
    - "A plain member's behaviour is completely UNCHANGED: requireDomainAccess still denies a member on an unassigned domain, and scopedDomainIds still returns only their membership domains (D-09-02 regression guard)"
    - "The frozen (prisma,userId,domainId,minRole) / (prisma,userId) signatures are preserved — no route caller changes"
  artifacts:
    - apps/api/src/lib/authorization.ts
    - apps/api/test/authorization.test.ts
  key_links:
    - "lib/accountRole.ts isAccountAdmin -> requireDomainAccess bypass branch -> every Link/QR/Analytics call site inherits the admin bypass with zero route edits"
    - "isAccountAdmin -> scopedDomainIds bypass branch -> prisma.domain.findMany all ids"
---

## Phase Goal

**As a** Kurzly admin, **I want to** invite teammates with a chosen role, assign members specific domains, change roles, and remove people — while trusting that every member can only see and edit the Links, QR codes, and Analytics of their assigned domains, enforced server-side — **so that** I can safely delegate link management without ever exposing another domain's data.

> Derived from the ROADMAP Phase 9 `**Goal:**` line into user-story shape (MVP framing), matching the Phase 7/8 precedent.

<objective>
Teach the two frozen authorization primitives a single new rule (D-09-02): an account-admin reaches every domain. Because `requireDomainAccess`/`scopedDomainIds` are already called from `lib/links.ts`, `lib/qrCodes.ts`, and `routes/analytics.ts`, adding the bypass here makes TEAM-06's admin half true across every Link/QR/Analytics endpoint at once — no route touched, no forgotten call site. This changes already-shipped behaviour, so a regression test proving a plain Member's scoping is unchanged is mandatory.

Purpose: the admin-can-reach-everything half of TEAM-06, applied structurally in one place.
Output: two small bypass branches and an expanded real-Postgres unit suite.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-CONTEXT.md
@apps/api/src/lib/authorization.ts
@apps/api/src/lib/accountRole.ts
@apps/api/test/authorization.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Account-admin bypass inside requireDomainAccess and scopedDomainIds</name>
  <files>apps/api/src/lib/authorization.ts, apps/api/test/authorization.test.ts</files>
  <read_first>
    - apps/api/src/lib/authorization.ts (the full file — requireDomainAccess lines 29-62, scopedDomainIds lines 70-80, the frozen-signature header comment lines 3-15)
    - apps/api/src/lib/accountRole.ts (isAccountAdmin — created in 09-01)
    - apps/api/test/authorization.test.ts (the existing seedUser/seedDomain helpers and the deny-by-default + CR-01 regression cases — extend this file, keep every existing case green)
    - .planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-CONTEXT.md (D-09-02 — bypass lives in the helpers, not routes)
  </read_first>
  <behavior>
    - requireDomainAccess resolves (grants) for an accountRole="admin" user on a domain they have NO DomainMembership on, at minRole "admin" and at minRole "member".
    - requireDomainAccess still throws ForbiddenError for an accountRole="member" user on a domain they have no membership on (deny-by-default preserved).
    - requireDomainAccess still resolves for a member on a domain where their DomainMembership meets minRole, and still throws when their membership rank is below minRole (existing cases stay green).
    - scopedDomainIds for an accountRole="admin" user returns ALL domain ids in the DB, including domains they hold no membership on.
    - scopedDomainIds for an accountRole="member" user returns EXACTLY their membership domain ids (unchanged) — never all domains.
    - The CR-01 fail-closed regression case (unknown role value denies) still passes unchanged.
  </behavior>
  <action>In apps/api/src/lib/authorization.ts import `isAccountAdmin` from `./accountRole.js`. At the very start of `requireDomainAccess` (before the membership lookup), add: if `isAccountAdmin(prisma, userId)` resolves true, return immediately (grant, regardless of domainId/minRole) — cite D-09-02 in a `///`/`//` comment explaining an account-admin reaches every domain so no route needs to change. Leave the entire existing membership + CR-01 rank logic untouched below it. At the start of `scopedDomainIds`, add: if `isAccountAdmin(prisma, userId)` resolves true, return `(await prisma.domain.findMany({ select: { id: true } })).map((d) => d.id)` — every domain id — then leave the existing membership query as the member/non-admin path. Preserve both frozen signatures exactly; do not change any caller. Update the file header comment to note the new account-admin bypass (D-09-02) while reaffirming the deny-by-default posture for non-admins. Extend apps/api/test/authorization.test.ts with a new `describe("account-admin bypass (D-09-02)")` block using the existing seedUser/seedDomain helpers plus an admin-seeding variant (a user created with `accountRole: "admin"`): cover every behavior case above, and INCLUDE the explicit member-unchanged regression assertions (a member on an unassigned domain is still denied; a member's scopedDomainIds still returns only their memberships, never all domains). Commit the failing tests first (`test(09-02): failing account-admin bypass + member-unchanged regression cases`), then the implementation (`feat(09-02): account-admin bypass in requireDomainAccess/scopedDomainIds`).</action>
  <verify>
    <automated>pnpm --filter @kurzly/api test -- test/authorization.test.ts</automated>
  </verify>
  <done>Admin bypass grants on any domain and returns all domain ids; every pre-existing member/deny-by-default/CR-01 case stays green; the member-unchanged regression is proven; signatures unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller identity → domain authorization | requireDomainAccess/scopedDomainIds decide, for every Link/QR/Analytics operation, which domains a caller may touch |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-BYPASS-OVERREACH | Elevation of Privilege | requireDomainAccess admin bypass | high | mitigate | The bypass is gated strictly on isAccountAdmin (accountRole="admin", read fresh from the DB per call) — a member never enters the grant branch; the member-unchanged regression test pins this |
| T-09-SCOPE-LEAK | Information Disclosure | scopedDomainIds admin branch | high | mitigate | Only an account-admin receives all domain ids; a member still receives exactly their membership set — asserted directly against real Postgres |
| T-09-FAILOPEN | Elevation of Privilege | isAccountAdmin lookup | medium | mitigate | isAccountAdmin returns false for an unknown user (09-01), so an absent/deleted user can never satisfy the bypass; the deny-by-default membership path remains the fallback |
| T-09-SIGDRIFT | Tampering | frozen helper signatures | low | mitigate | Signatures are preserved; no route caller edited, so no accidental authorization gap is introduced at a call site |
</threat_model>

<verification>
- `pnpm --filter @kurzly/api test` full suite green — critically, the existing links/qr/analytics/domains integration suites must still pass, proving the bypass did not break member scoping at real call sites.
- `pnpm tsc --noEmit` clean across the workspace.
- Manual read-through: requireDomainAccess/scopedDomainIds signatures are byte-identical to before; only additive bypass branches were introduced.
</verification>

<success_criteria>
- An account-admin transparently reaches every domain's Links/QR/Analytics while a plain member's domain scoping is provably unchanged — the admin half of TEAM-06, enforced in one place.
</success_criteria>

<output>
Create `.planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-02-SUMMARY.md` when done.
</output>
