---
phase: 09-team-management-domain-scoped-authorization-enforcement
plan: 05
type: execute
wave: 3
depends_on: [09-02]
files_modified:
  - apps/api/test/team-domain-denial.integration.test.ts
autonomous: true
requirements: [TEAM-06]
user_setup: []

must_haves:
  truths:
    - "A Member with NO membership on a target domain is rejected on EVERY Link/QR/Analytics endpoint even with a genuine, correctly-identified resource id (D-09-08)"
    - "The suite reads as one exhaustive endpoint x expectation table, so a reviewer can see at a glance nothing was skipped (09-CONTEXT Specifics)"
    - "Response shapes match what each surface already guarantees: Link routes 403, QR routes 404, Analytics scopes silently and returns zero foreign rows (D-09-08)"
    - "An account-admin reaches the SAME foreign resources the member is denied (the admin-bypass positive half of TEAM-06)"
  artifacts:
    - apps/api/test/team-domain-denial.integration.test.ts
  key_links:
    - "member session cookie + foreign resource id -> each endpoint -> asserted rejection status AND zero foreign rows leaked"
    - "admin session cookie -> same endpoints -> success (D-09-02 bypass proven positively)"
---

## Phase Goal

**As a** Kurzly admin, **I want to** invite teammates with a chosen role, assign members specific domains, change roles, and remove people — while trusting that every member can only see and edit the Links, QR codes, and Analytics of their assigned domains, enforced server-side — **so that** I can safely delegate link management without ever exposing another domain's data.

> Derived from the ROADMAP Phase 9 `**Goal:**` line into user-story shape (MVP framing), matching the Phase 7/8 precedent.

<objective>
Prove TEAM-06 by exhaustion, not by inspection (D-09-08). Build the phase's headline evidence: a single, obviously-complete suite that, for a Member with no membership on a target domain, walks every Link, QR-code, and Analytics endpoint with a genuinely existing, correctly-guessed resource id and asserts the rejection — plus the positive half, that an account-admin reaches those same resources (proving the 09-02 bypass end to end).

Purpose: the success-criterion demands proof that a direct API request for a foreign resource is rejected even with a valid id; this suite is that proof.
Output: one exhaustive denial integration suite, structured as a readable endpoint x expectation table.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-CONTEXT.md
@apps/api/src/routes/links.ts
@apps/api/src/routes/qrCodes.ts
@apps/api/src/routes/analytics.ts
@apps/api/src/lib/authorization.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exhaustive TEAM-06 domain-scoped denial suite (member denied, admin allowed)</name>
  <files>apps/api/test/team-domain-denial.integration.test.ts</files>
  <read_first>
    - apps/api/src/routes/links.ts (every Link-by-id endpoint and its status: GET/PATCH/DELETE /api/links/:id return 404 via resolveOwnedLink; POST /api/links + the import endpoints reject a foreign domainId with UNAUTHORIZED_DOMAIN->403; GET /api/links scopes silently)
    - apps/api/src/routes/qrCodes.ts and apps/api/src/lib/qrCodes.ts statusForQrError (QR routes answer 404 for UNAUTHORIZED_DOMAIN — a QR's domain boundary is never client-visible; enumerate EVERY /api/qr-codes* endpoint: create/list/get/patch/remap/remap-history/render.png/render.svg)
    - apps/api/src/routes/analytics.ts (GET /api/links/:id/analytics -> 404 via resolveOwnedLink; GET /api/analytics scopes silently to scopedDomainIds and must return zero rows attributable to the foreign domain)
    - apps/api/test/links.integration.test.ts lines 29-150 and apps/api/test/qrCodes.integration.test.ts (signInAs / seed-owner-domain / seed-link / seed-qr harness — reuse verbatim; seed the FOREIGN resources as a DIFFERENT owner, then attempt access as an unassigned member)
    - .planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-CONTEXT.md (D-09-08 — response shapes follow each surface; the suite must assert zero foreign rows, not just a status code)
  </read_first>
  <behavior>
    - Setup: an owner account creates an ACTIVE domain, a Link on it, a static QR and a dynamic QR bound to that Link, and some analytics-visible state; a separate account-admin exists; and a plain Member exists with NO membership on that domain.
    - For the Member, every one of these with the genuine foreign resource id is rejected and leaks zero foreign data: GET/PATCH/DELETE /api/links/:id -> 404; POST /api/links to the foreign domainId -> 403 UNAUTHORIZED_DOMAIN; POST /api/links/import/{preview,commit} targeting the foreign domain -> the row is skipped as domain_unauthorized (zero rows written); GET /api/links and GET /api/qr-codes -> the foreign link/QR is ABSENT from the array; GET/PATCH/DELETE + remap + remap-history + render.png + render.svg on /api/qr-codes/:id -> 404; GET /api/links/:id/analytics -> 404; GET /api/analytics -> the foreign domain contributes zero clicks/links to the totals and top-lists.
    - For the account-admin, the SAME foreign resource ids succeed (GET link 200, GET qr 200, analytics 200 including the foreign domain) — the 09-02 bypass proven positively.
    - The Member assertions include at least one explicit "zero rows" / "resource absent from list" check per resource type, not only a status code, so a silent leak cannot pass.
  </behavior>
  <action>Create apps/api/test/team-domain-denial.integration.test.ts as the phase's headline TEAM-06 evidence (D-09-08). Reuse the established buildApp({ prisma }) + signInAs + mailer-mock harness and the seed helpers from links.integration.test.ts / qrCodes.integration.test.ts (seed an ACTIVE owner domain + Link + static & dynamic QrCode as an OWNER identity; seed a separate account-admin via a user with accountRole:"admin"; seed a plain Member with emailVerified:true, accountRole:"member", and NO DomainMembership on the target domain). Structure the Member-denial cases as a single data-driven table (an array of { method, url, expectedStatus, note } iterated with it.each, or a clearly-sectioned describe) so the suite reads as one obviously-exhaustive endpoint x expectation matrix covering EVERY Link, QR-code and Analytics endpoint — enumerate them from the three route files, do not sample. For each Member case assert BOTH the documented status (Link 403/404 per endpoint, QR 404, Analytics silent-scope) AND that zero foreign rows are returned (resource absent from list responses; foreign domain contributes nothing to /api/analytics totals; import writes nothing). Add a parallel admin-allowed section proving the account-admin reaches the same resources. Because this suite is pure verification of already-implemented behavior (09-02 + prior phases), there is no production code to write RED-first; commit the suite in one commit `test(09-05): exhaustive TEAM-06 domain-scoped denial + admin-bypass suite`. If any endpoint is found to leak (a real gap rather than a test bug), STOP and report it as a gap for a follow-up plan rather than weakening the assertion.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api test -- test/team-domain-denial.integration.test.ts</automated>
  </verify>
  <done>Every Link/QR/Analytics endpoint is covered by one exhaustive table; a Member is denied on each with zero foreign rows leaked; an account-admin reaches the same resources; no endpoint is sampled or skipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| member request → foreign-domain resource | A member holding a valid, correctly-guessed id for a resource on a domain they are not assigned to must be refused at every endpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-09-IDOR-ENUM | Information Disclosure | all Link/QR/Analytics endpoints | high | mitigate | The suite proves every endpoint denies a member on a foreign domain even with a genuine id, asserting zero foreign rows — the D-09-08 no-existence-oracle guarantee, verified by exhaustion not inspection |
| T-09-ANALYTICS-LEAK | Information Disclosure | GET /api/analytics | high | mitigate | The suite asserts the foreign domain contributes zero clicks/links to the silently-scoped totals, not merely that the call returns 200 |
| T-09-IMPORT-BYPASS | Elevation of Privilege | CSV import to a foreign domain | medium | mitigate | Import rows targeting the foreign domain are asserted skipped (domain_unauthorized) with zero rows written — the single validated write path holds under a member identity |
| T-09-COVERAGE-GAP | Repudiation | denial-suite completeness | medium | mitigate | Endpoints are enumerated from the route files into one table, not sampled; a discovered leak halts the plan as a reported gap rather than being asserted away |
</threat_model>

<verification>
- `pnpm --filter @kurzly/api test -- test/team-domain-denial.integration.test.ts` green.
- `pnpm --filter @kurzly/api test` full suite green.
- Manual read-through: the endpoint list in the suite matches the union of endpoints declared in routes/links.ts, routes/qrCodes.ts, and routes/analytics.ts — nothing omitted.
</verification>

<success_criteria>
- A single exhaustive suite proves a Member's direct API request for any Link/QR/Analytics resource on an unassigned domain is rejected server-side (even with a valid id and zero rows leaked), while an account-admin reaches the same resources — TEAM-06 proven both ways.
</success_criteria>

<output>
Create `.planning/phases/09-team-management-domain-scoped-authorization-enforcement/09-05-SUMMARY.md` when done.
</output>
