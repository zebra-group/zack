---
phase: 15-qr-studio-e2e
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/e2e/package.json
  - apps/e2e/src/qr.ts
  - apps/e2e/tests/smoke/qr-fixture.spec.ts
autonomous: false
requirements: [QR-E2E-01, QR-E2E-02, QR-E2E-03]

must_haves:
  truths:
    - "createE2eQrCode raw-inserts a QrCode row that satisfies the schema's non-nullable columns (variant, linkId, name, color) — a dynamic variant gets a random unique `code`, a static variant gets `code: null` — so QR-E2E-02/03 can seed a pre-existing QR to remap/export without depending on the non-deterministic real-UI create ordering (15-RESEARCH.md Pattern 3, Alternatives Considered)"
    - "decodeQrImage is the sharp(bytes).ensureAlpha().raw() -> jsQR recipe ported VERBATIM from apps/api/test/qrDecode.test.ts's proven decode() helper — only the byte SOURCE differs (real HTTP fetch vs. in-process render) — so every feature-spec decode assertion uses the exact decode path already trusted against this renderer's output, including logo-composited symbols (15-RESEARCH.md Pattern 1)"
    - "jsqr and sharp resolve as apps/e2e devDependencies against the shared pnpm-lock.yaml (both already resolved there via @kurzly/api) — the sharp [SUS] freshness-heuristic flag is acknowledged as a documented false-positive at a blocking-human checkpoint BEFORE install, never substituted (15-RESEARCH.md Package Legitimacy Audit)"
    - "The helper is proven RED->GREEN by a dedicated contract spec before any feature spec (15-02/03/04) consumes it — honouring CLAUDE.md's TDD mandate for genuinely new function logic (14-01/12-02 links-fixture precedent)"
  artifacts:
    - apps/e2e/src/qr.ts
    - apps/e2e/tests/smoke/qr-fixture.spec.ts
    - apps/e2e/package.json
  key_links:
    - "15-03 (dynamic remap) and 15-04 (export) both import createE2eQrCode from ../../src/qr.js; 15-02 and 15-04 both import decodeQrImage from the same module — the SINGLE source of the fixture-insert shape and the decode recipe"
    - "the contract spec lives under tests/smoke/ and runs in the `smoke` project (dependencies:[setup], E2E_DATABASE_URL exported by scripts/e2e-compose.sh), mirroring apps/e2e/tests/smoke/links-fixture.spec.ts's RED->GREEN proof of apps/e2e/src/links.ts exactly"
---

<objective>
Create the two genuinely-new assets this phase needs before any feature spec can run: a direct-Prisma QR fixture helper `createE2eQrCode` and the `decodeQrImage` (jsQR + sharp) decode util, both in a new `apps/e2e/src/qr.ts`, plus the two new `apps/e2e` devDependencies (`jsqr`, `sharp`) they rest on. `createE2eQrCode` lets QR-E2E-02/03 seed a pre-existing QR deterministically (the real "+ Dynamischer QR" UI create always binds to `links[0]`, too non-deterministic to set up "starts bound to target A"); `decodeQrImage` is the ONE decode path every content-round-trip assertion in QR-E2E-01/03 uses, ported verbatim from the already-passing `apps/api/test/qrDecode.test.ts`.

Follow the project's RED->GREEN discipline (CLAUDE.md TDD mandate; 14-01/12-02 links-fixture precedent): a blocking-human checkpoint acknowledges sharp's documented [SUS] false-positive, then the failing contract spec is written, then the implementation that turns it green. Zero application code (apps/api, apps/web) is touched.

Purpose: A single source of truth for the QR fixture-insert shape and the QR decode recipe, proven by a unit-level contract spec.
Output: `apps/e2e/src/qr.ts`, its RED->GREEN contract spec `apps/e2e/tests/smoke/qr-fixture.spec.ts`, and `jsqr`/`sharp` added to `apps/e2e/package.json`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qr-studio-e2e/15-RESEARCH.md
@apps/e2e/src/links.ts
@apps/e2e/src/db.ts
@apps/e2e/tests/smoke/links-fixture.spec.ts
@apps/api/test/qrDecode.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Checkpoint: acknowledge sharp [SUS] false-positive before devDependency install</name>
  <action>Blocking-human legitimacy gate before `pnpm --filter @kurzly/e2e add -D jsqr@^1.4.0 sharp@^0.35.3`. Do not run the install until this checkpoint is approved; do not substitute either package. See what-built / how-to-verify below.</action>
  <what-built>Nothing yet — this gate precedes the `pnpm add -D sharp` step. `sharp@^0.35.3` is flagged [SUS] by the automated package-legitimacy freshness heuristic ("too-new" — it ships frequent patch releases wrapping native libvips bindings). 15-RESEARCH.md's Package Legitimacy Audit documents this as a FALSE-POSITIVE: sharp is a 10+-year-old, ~76M-weekly-download package that has been an APPROVED, in-production dependency of `@kurzly/api` since Phase 7 and is already resolved in this monorepo's single `pnpm-lock.yaml`. Adding it to `apps/e2e` adds a new consumer of an existing lockfile entry, not a new version resolution. `jsqr@^1.4.0` audited OK (also already an `apps/api` devDependency).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/sharp and confirm it is the established `lovell/sharp` image library (~76M weekly downloads, repo github.com/lovell/sharp).
    2. Open https://www.npmjs.com/package/jsqr and confirm it is `cozmo/jsQR` (~1.8M weekly downloads).
    3. Confirm both already appear in the repo's `pnpm-lock.yaml` (grep `jsqr@`/`sharp@`) as `@kurzly/api` dependencies.
    Expected outcome: both are legitimate, already-trusted packages; approve adding them as `apps/e2e` devDependencies WITHOUT substitution.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize `pnpm --filter @kurzly/e2e add -D jsqr@^1.4.0 sharp@^0.35.3`, or describe a concern.</resume-signal>
</task>

<task type="auto">
  <name>Task 1: RED — add jsqr/sharp devDeps + contract spec for qr.ts</name>
  <read_first>
    - apps/e2e/tests/smoke/links-fixture.spec.ts (the exact RED->GREEN precedent to mirror: it imports helpers from ../../src/links.js and its module-load fails until the module exists — that IS the intended RED state; createE2eLink is proven via a withResetDbLock DB round-trip, pure helpers via in-memory assertions)
    - apps/e2e/src/db.ts (createE2ePrisma, withResetDbLock, BASELINE_DOMAIN_HOSTNAME, ADMIN_EMAIL — the truncate list ALREADY covers QrRemapHistory + QrCode, so no db.ts change is needed here)
    - apps/e2e/src/links.ts (createE2eLink — used to seed the parent Link a QrCode's linkId FK requires; also the module-style precedent: header doc-comment, named exports consumed via ../../src/<name>.js)
    - apps/api/prisma/schema.prisma QrCode model (lines 339-371 — non-nullable columns variant/linkId/name/color, `code String? @unique` dynamic-only, roundedModules/logoEnabled/lifetimeScans defaults, createdBy is String? optional)
    - apps/api/test/qrDecode.test.ts (the decode() helper lines 86-90 — the sharp(bytes).ensureAlpha().raw().toBuffer({resolveWithObject:true}) -> jsQR(new Uint8ClampedArray(data), info.width, info.height) recipe decodeQrImage must port verbatim; also its `sharp({create:{...}}).png().toBuffer()` idiom for generating a solid-color PNG in-memory)
    - 15-RESEARCH.md (Pattern 1 decode recipe, Pattern 3 createE2eQrCode shape + Assumption A2 randomBytes(8).toString('hex') collision-free-enough, Package Legitimacy Audit)
  </read_first>
  <files>apps/e2e/package.json, apps/e2e/tests/smoke/qr-fixture.spec.ts</files>
  <action>
    First add the two devDependencies (checkpoint already approved): run `pnpm --filter @kurzly/e2e add -D jsqr@^1.4.0 sharp@^0.35.3`, which updates apps/e2e/package.json and reuses the existing shared-lockfile entries. Confirm `pnpm --filter @kurzly/e2e exec tsc --noEmit` still passes (types resolve).

    Then create the contract spec importing `createE2eQrCode` and `decodeQrImage` from `../../src/qr.js` (the module Task 2 creates — until then the import is unresolved and every test fails at module-load, the intended RED state, exactly as links-fixture.spec.ts documents for its own module). Also import createE2ePrisma/withResetDbLock/BASELINE_DOMAIN_HOSTNAME from ../../src/db.js, createE2eLink from ../../src/links.js, and `sharp` directly (now installed).

    Assert createE2eQrCode's contract via a withResetDbLock DB round-trip (mirror links-fixture.spec.ts's createE2eLink cases — seed the parent Link first via createE2eLink inside the same tx with a per-test crypto-random slug):
    - a `dynamic` QrCode read back has a non-null `code` matching a 16-char lowercase-hex shape, `linkId` equal to the seeded Link's id, and `variant` "dynamic".
    - a `static` QrCode read back has `code: null` and `variant` "static".
    - the stored `color` equals the option passed (or the helper's documented default when omitted) and `roundedModules` defaults to false when omitted.

    Assert decodeQrImage's contract with a self-contained unit assertion (no app, no QR encoder available in apps/e2e): generate a 64x64 solid-color PNG via `sharp({create:{width:64,height:64,channels:4,background:{r:20,g:58,b:95,alpha:1}}}).png().toBuffer()` and assert `decodeQrImage(thatBuffer)` resolves to `null` — proving the sharp `.ensureAlpha().raw()` -> jsQR pipeline runs end-to-end and returns the `?? null` branch for a non-QR image without throwing. (The POSITIVE decode round-trip — a real QR image decoding back to its exact short URL — is proven by the feature specs 15-02/15-04 against real authenticated app renders, the only place a genuine QR image exists, since apps/e2e ships no QR ENCODER.)

    Do NOT create apps/e2e/src/qr.ts in this task — its absence is what makes this spec RED.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/qr-fixture.spec.ts --project=smoke</automated>
  </verify>
  <acceptance_criteria>
    - apps/e2e/package.json lists jsqr@^1.4.0 and sharp@^0.35.3 under devDependencies; `pnpm --filter @kurzly/e2e exec tsc --noEmit` passes
    - the spec imports createE2eQrCode + decodeQrImage from ../../src/qr.js and encodes the dynamic/static insert contract (DB round-trip) plus the decode-null-for-non-QR contract
    - run against the built stack it is RED — failing only because apps/e2e/src/qr.ts does not yet exist (module unresolved), not because any assertion is wrong
    - no apps/api or apps/web file is touched
  </acceptance_criteria>
  <done>The failing contract spec fully specifies createE2eQrCode + decodeQrImage before the implementation exists (RED), and the two devDependencies resolve.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement apps/e2e/src/qr.ts</name>
  <read_first>
    - apps/e2e/src/links.ts (module-style precedent: E2ePrismaLike = PrismaClient | Prisma.TransactionClient type alias, header doc-comment convention, named exports consumed via ../../src/<name>.js; createE2eLink's option-object shape to mirror)
    - apps/e2e/tests/smoke/qr-fixture.spec.ts (Task 1's contract — the implementation must satisfy every assertion in it)
    - apps/api/test/qrDecode.test.ts lines 86-90 (the decode recipe to port verbatim)
    - apps/api/prisma/schema.prisma QrCode model (non-nullable variant/linkId/name/color; code String? @unique)
  </read_first>
  <files>apps/e2e/src/qr.ts</files>
  <action>
    Implement the helper so Task 1's contract spec passes GREEN. Add a header doc-comment in the apps/e2e/src/links.ts style explaining WHY this module exists (createE2eQrCode: deterministic pre-existing-QR seeding for the remap/export specs, since the real "+ Dynamischer QR" UI binds to links[0] non-deterministically; decodeQrImage: the single decode path, ported from apps/api/test/qrDecode.test.ts, so the feature specs reuse a recipe already proven against this renderer's output). Export:
    - a `randomQrCode()` internal helper returning `randomBytes(8).toString("hex")` (16 hex chars — well within routes/qrRedirect.ts's `QR_CODE_PARAM` /^[0-9A-Za-z]{1,32}$/ gate; E2E scale needs no collision-retry loop, per Assumption A2). Do NOT reach for @kurzly/api's generateSlug — it is not in the exports map and is structurally unreachable from apps/e2e (mirrors createE2eLink's own note).
    - `CreateE2eQrCodeOptions` type: `{ variant: "static" | "dynamic"; linkId: string; name: string; color?: string; roundedModules?: boolean }`.
    - `createE2eQrCode(prisma, opts)` — a raw `prisma.qrCode.create` that sets `variant`, `linkId`, `name`, `code` (randomQrCode() for dynamic, null for static), `color: opts.color ?? "#000000"` (schema has no color default — a value MUST always be supplied), and `roundedModules: opts.roundedModules ?? false`. Accept the shared `E2ePrismaLike` (PrismaClient or a withResetDbLock transaction client), exactly like createE2eLink. Do not set createdBy — it is nullable and the render/redirect IDOR scope resolves through the bound Link's domain, not createdBy.
    - `decodeQrImage(bytes: Buffer): Promise<string | null>` — the verbatim port: `const { data, info } = await sharp(bytes).ensureAlpha().raw().toBuffer({ resolveWithObject: true }); const result = jsQR(new Uint8ClampedArray(data), info.width, info.height); return result?.data ?? null;` with a source comment pointing at apps/api/test/qrDecode.test.ts.

    Touch no application code (apps/api, apps/web).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/qr-fixture.spec.ts --project=smoke</automated>
  </verify>
  <acceptance_criteria>
    - apps/e2e/src/qr.ts exports createE2eQrCode (dynamic -> non-null 16-hex unique code; static -> null code) and decodeQrImage (verbatim sharp+jsQR port) with the signatures above
    - the Task 1 contract spec passes GREEN against the built compose image
    - zero apps/api / apps/web diffs (test-authoring only)
  </acceptance_criteria>
  <done>createE2eQrCode + decodeQrImage are implemented and their contract spec is green — 15-02/03/04 can now seed QR fixtures and decode real renders.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry / pnpm-lock.yaml -> apps/e2e devDependencies | two new binary-shipping packages (sharp native libvips, jsqr) enter the E2E harness's dependency tree |
| test runner -> E2E Postgres :5433 | direct-Prisma QrCode/Link fixture inserts + read-back assertions |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-SC | Tampering | `pnpm add -D jsqr sharp` supply chain | medium | mitigate | sharp flagged [SUS] by the freshness heuristic (documented false-positive: 10+yr, ~76M/wk, already an approved apps/api dependency + lockfile-resolved). A blocking-human checkpoint verifies both packages at npmjs.com and confirms they add a new consumer of an existing lockfile entry, not a new resolution — approve, never substitute (15-RESEARCH.md Package Legitimacy Audit) |
| T-15-01 | Tampering | createE2eQrCode fixture-insert shape | low | mitigate | A wrong nullable/non-nullable column (e.g. a non-null code on a static QR, or an omitted color the schema has no default for) would fail loudly at insert or mis-drive the feature specs; the contract spec asserts the dynamic/static code nullability and the color default at the fixture source |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/smoke/qr-fixture.spec.ts --project=smoke` is RED after Task 1 (module unresolved) and GREEN after Task 2.
- `pnpm --filter @kurzly/e2e exec tsc --noEmit` passes.
- `git diff --stat` shows changes only under apps/e2e/ (no apps/api, no apps/web).
</verification>

<success_criteria>
createE2eQrCode and decodeQrImage exist, are contract-correct by construction, and are proven by a green contract spec; jsqr/sharp resolve as apps/e2e devDependencies — the assets 15-02/03/04 depend on.
</success_criteria>

<artifacts_produced>
- `apps/e2e/src/qr.ts` — createE2eQrCode + decodeQrImage
- `apps/e2e/tests/smoke/qr-fixture.spec.ts` — RED->GREEN contract spec
- `apps/e2e/package.json` — jsqr + sharp devDependencies
</artifacts_produced>

<output>
Create `.planning/phases/15-qr-studio-e2e/15-01-SUMMARY.md` when done.
</output>
