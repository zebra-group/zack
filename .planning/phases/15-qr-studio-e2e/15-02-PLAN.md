---
phase: 15-qr-studio-e2e
plan: 02
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - apps/e2e/tests/authed/qr-static-customize-decode.spec.ts
autonomous: true
requirements: [QR-E2E-01]

must_haves:
  truths:
    - "A static QR is created through the REAL UI (LinkDetailView's `QR-Code` button -> POST /api/qr-codes -> deep-link into the Studio at /qr-codes?selected={id}), then customized through the REAL QrStudioPanel controls: a color swatch, the Runde Module toggle, and a logo upload — each persisting via its own PATCH /api/qr-codes/:id before the next (mutationSeq-race-safe, 15-RESEARCH.md Pitfall 4)"
    - "The server-rendered PNG bytes (fetched via page.request.get, which carries the chromium-admin storageState session cookie the 401-gated render endpoint requires) decode via decodeQrImage back to the EXACT constructed short URL `https://{BASELINE_DOMAIN_HOSTNAME}/{slug}?qr={qrId}` — never Link.targetUrl (15-RESEARCH.md Summary point 1, Pitfall 1) — a content round-trip through a real rasterized render WITH the logo composited (error-correction level H), not merely 'an image rendered'"
    - "The render response's content-type is image/png"
  artifacts:
    - apps/e2e/tests/authed/qr-static-customize-decode.spec.ts
  key_links:
    - "the decode assertion builds its expected string from the fixture's known BASELINE_DOMAIN_HOSTNAME + per-test slug + the created qrId and the literal `qr` scan-attribution param (QR_SCAN_PARAM, apps/api/src/lib/redirectEngine.ts) — never the fixture's targetUrl"
    - "spec is scoped to chromium-admin (skipped under chromium-member): nothing here exercises role-differentiated behaviour — member/domain-scoped QR authz is Phase 17's job (CONTEXT.md Deferred Ideas), so running under member too would be pure duplication"
---

<objective>
Prove QR-E2E-01 end-to-end through the real UI and the real render pipeline: create a static QR via LinkDetailView's `QR-Code` entry-point button, customize it (color swatch + rounded-modules toggle + logo upload) via the real QrStudioPanel controls, then fetch the server-rendered PNG and decode it back to the exact short URL Kurzly constructs. The crux correction from 15-RESEARCH.md: a QR NEVER encodes the raw destination — a static QR encodes its bound Link's own short URL `https://{hostname}/{slug}?qr={id}`, so the decode assertion is built against that constructed string, never `Link.targetUrl`.

This spec asserts REAL, currently-passing behaviour — routes/qrCodes.ts, lib/qr.ts, LinkDetailView.vue, and QrStudioPanel.vue were read in full and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-14 TDD RED->GREEN precedent: a real bug is fixed, not papered over).

Purpose: Close QR-E2E-01 with a real-UI creation + customization + server-decode content round-trip.
Output: `apps/e2e/tests/authed/qr-static-customize-decode.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qr-studio-e2e/15-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/src/qr.ts
@apps/e2e/tests/authed/links-crud.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QR-E2E-01 — static QR create + customize (color/rounded/logo) + decode round-trip (chromium-admin)</name>
  <read_first>
    - apps/web/src/views/LinkDetailView.vue (VERIFIED entry point: `<button class="action-button" @click="handleQrCode">QR-Code</button>` at line ~399; handleQrCode looks up an existing static QR for THIS link via listQrCodes()+client filter and, finding none for a fresh fixture Link, calls createQrCode({variant:"static", linkId, name:`QR für /${slug}`}) then router.push({name:"qr-codes", query:{selected: created.id}}) — so clicking it on a fresh Link reliably CREATES a static QR and deep-links to /qr-codes?selected={id})
    - apps/web/src/components/QrStudioPanel.vue (VERIFIED selectors: color swatches are `.color-swatch` buttons, the currently-applied one carries `.color-swatch.selected` (PRODUCT_COLORS #17170f/#1e3a5f/#14532d/#7c2d4f); the rounded toggle is `.rounded-toggle` (role="switch"); the logo file input is the hidden `input.hidden-file-input` type=file accept image/png,image/svg+xml — Playwright setInputFiles drives it directly despite display:none; every control change calls persistStyle -> PATCH /api/qr-codes/:id and the mutationSeq guard drops a stale earlier response, so successive changes must each await their own PATCH — Pitfall 4)
    - apps/web/src/router/index.ts (route `/links/:id` name "link-detail"; `/qr-codes` name "qr-codes")
    - apps/api/src/routes/qrCodes.ts (VERIFIED: resolveQrPayload builds a static QR's payload as `https://${qr.link.domain.hostname}/${qr.link.slug}?${QR_SCAN_PARAM}=${qr.id}`; the GET /api/qr-codes/:id/render.png handler is 401-gated via resolveUserId/resolveOwnedQrCode and replies type "image/png")
    - apps/api/src/lib/redirectEngine.ts (VERIFIED: `export const QR_SCAN_PARAM = "qr"` — the exact scan-attribution query key; apps/e2e cannot import it, so the spec hardcodes the literal `qr` with a source comment)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME = "e2e.kurzly.local" — import this constant, do not hardcode the hostname)
    - apps/e2e/src/links.ts (createE2eLink — seed the parent Link with a per-test crypto-random slug and NO password/expiry/UTM)
    - apps/e2e/src/qr.ts (decodeQrImage — the decode util from 15-01)
    - apps/e2e/tests/authed/links-crud.spec.ts (the authed-spec precedent: `test.describe.configure({ retries: 2 })` for the documented db-isolation.spec.ts cross-file truncate race, the chromium-admin-only `test.skip` on testInfo.project.name, page.request against the same BrowserContext cookie jar, per-test random identifiers)
    - 15-RESEARCH.md (Pitfall 1 wrong-string decode; Pitfall 2 authenticated page.request; Pitfall 3 tiny logo under MAX_LOGO_BYTES; Pitfall 4 mutationSeq race; Pitfall 5 do NOT literally fetch the decoded https:// URL — string-equality only)
  </read_first>
  <files>apps/e2e/tests/authed/qr-static-customize-decode.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` with a doc-comment (mirror links-crud.spec.ts) — apps/e2e/tests/smoke/db-isolation.spec.ts truncates QrCode/QrRemapHistory/Link concurrently during the full-suite phase gate; a whole-journey retry with fresh per-test random slugs is the collision-free UI-flow equivalent. Add a `test.beforeEach` that `test.skip`s when testInfo.project.name !== "chromium-admin" (reason: QR-E2E-01 is admin-scoped; member/domain-scoped QR authz is Phase 17, CONTEXT.md Deferred Ideas), so the spec runs only under chromium-admin even though testMatch registers every tests/authed spec under both authenticated projects.

    Single test "static QR: create via UI, customize color/rounded/logo, PNG decodes to its short URL":
    1. Mint a per-test crypto-random valid-shape slug `e2e-qr-<hex>` (lowercase-alphanumeric-hyphen). createE2ePrisma(); createE2eLink with that slug and a distinct targetUrl `https://example.com/qr-target-<hex>` (no password, no expiry, no UTM). $disconnect in a finally.
    2. page.goto(`/links/${link.id}`); assert the detail view loaded (storageState is already authenticated — no login).
    3. CREATE: click the `QR-Code` button (getByRole button name "QR-Code", or the `.action-button` with that text). waitForResponse the POST /api/qr-codes returning 201. Assert the URL becomes /qr-codes?selected=<id> (regex). Read qrId from the `selected` query param of page.url().
    4. CUSTOMIZE — color: click `.color-swatch:not(.selected)` first() (guarantees a real change, so setColor does not early-return); waitForResponse a PATCH /api/qr-codes/<qrId>. rounded: click `.rounded-toggle`; waitForResponse a second PATCH /api/qr-codes/<qrId> (roundedModules default is false, so this always changes state). logo: generate a tiny in-memory PNG via `sharp({create:{width:64,height:64,channels:4,background:{r:20,g:58,b:95,alpha:1}}}).png().toBuffer()` (well under MAX_LOGO_BYTES, Pitfall 3), set it on `input.hidden-file-input` via setInputFiles({name:"logo.png", mimeType:"image/png", buffer}); waitForResponse the third PATCH /api/qr-codes/<qrId> (the logoData/logoEnabled upload). Await each PATCH before the next control (Pitfall 4 — never fire the three in immediate succession).
    5. DECODE round-trip: const response = await page.request.get(`/api/qr-codes/${qrId}/render.png`); assert response.status() 200 and response.headers()["content-type"] === "image/png". const decoded = await decodeQrImage(Buffer.from(await response.body())). Build the expected string from the imported BASELINE_DOMAIN_HOSTNAME, the per-test slug, and qrId: `https://${BASELINE_DOMAIN_HOSTNAME}/${slug}?qr=${qrId}` (the literal `qr` = QR_SCAN_PARAM; a source comment must note this). Assert decoded === that expected string.

    Never assert decoded === link.targetUrl (Pitfall 1 — structurally wrong by design). Do NOT literally fetch the decoded https:// URL (Pitfall 5 — no TLS listener in the E2E stack); string-equality is the mandatory content proof. Always await the specific PATCH response before the next control and before the render fetch — never a fixed sleep.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/qr-static-customize-decode.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the journey drives the REAL QR-Code entry button, the REAL color swatch, rounded toggle, and logo file input (no API-only shortcut for create/customize)
    - each of the three customization PATCHes is awaited before the next control (mutationSeq-race-safe)
    - the PNG is fetched via authenticated page.request (content-type image/png) and decodes via decodeQrImage to the exact `https://{BASELINE_DOMAIN_HOSTNAME}/{slug}?qr={qrId}` string, built from fixture values — never targetUrl
    - the spec is scoped to chromium-admin (skipped under chromium-member) and uses a per-test random slug with retries: 2
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>QR-E2E-01 proves a static QR created and customized (color/rounded/logo) through the real Studio decodes back to its exact constructed short URL — a real content round-trip, not just an image rendered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) -> POST/PATCH /api/qr-codes/* | authenticated QR create + style/logo mutations |
| host browser (admin session) -> GET /api/qr-codes/:id/render.png | 401-gated, IDOR-guarded server render fetched via the shared cookie jar |
| test runner -> E2E Postgres :5433 | direct-Prisma parent-Link fixture seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-02-IDOR | Elevation of Privilege | render.png resolveOwnedQrCode / createQrCode resolveLinkDomainAccess | low | accept | Exercised incidentally under the admin session; the full role x resource QR denial matrix is Phase 17 (AUTHZ-E2E-01) plus the v1.0 integration suite — this phase deliberately does not re-prove it |
| T-15-02-LEAK | Information disclosure | decoded QR payload | low | mitigate | The assertion pins the decoded payload to the constructed short URL (never Link.targetUrl), directly encoding the QR-01/03 architectural guarantee that a QR never embeds the raw destination; a regression that encoded targetUrl would fail this assertion loudly |
| T-15-02-INJ | Tampering | SVG/hex color + logo magic-byte validation | low | accept | The spec uses only the LOCKED product color swatches and a genuinely-valid tiny PNG; the injection/magic-byte rejection paths are already unit-tested (apps/api/test/qrDecode.test.ts) and are out of this happy-path spec's scope |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/qr-static-customize-decode.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (Playwright registers the spec under chromium-member too, where it is skipped) — confirm no new db-isolation truncate race.
</verification>

<success_criteria>
QR-E2E-01 passes: a static QR created + customized through the real Studio decodes back to its exact constructed short URL via an authenticated server render — a content round-trip.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/qr-static-customize-decode.spec.ts` — QR-E2E-01 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/15-qr-studio-e2e/15-02-SUMMARY.md` when done.
</output>
