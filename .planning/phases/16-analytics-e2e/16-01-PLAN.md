---
phase: 16-analytics-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/analytics-real-click.spec.ts
autonomous: true
requirements: [ANALYTICS-E2E-01]

must_haves:
  truths:
    - "A real HTTP GET against `/${slug}` (headers host=BASELINE_DOMAIN_HOSTNAME + user-agent=BROWSER_UA, maxRedirects:0) on a tracking-enabled, ungated fixture Link returns 302 — and because recordClickHook is fully awaited before the 302 is sent (16-RESEARCH.md Summary), a ClickEvent row + a lifetimeClicks increment are ALREADY committed the moment the response resolves (no polling/wait helper)"
    - "A FRESH authenticated navigation to /links/:id performed AFTER the click (never a page opened before it — LinkDetailView.loadAnalytics() is a one-shot mount fetch, 16-RESEARCH.md Pitfall 1) renders the data-branch `.stat-card` 'Klicks gesamt' `.stat-value` as the incremented count (1)"
    - "A direct-Prisma cross-check confirms exactly 1 ClickEvent for the fixture link AND Link.lifetimeClicks === 1 — the real tracked write, not a seeded row (ANALYTICS-E2E-01 locked decision: click via the public endpoint, never a DB insert)"
  artifacts:
    - apps/e2e/tests/authed/analytics-real-click.spec.ts
  key_links:
    - "the click-count assertion scopes `.stat-value` via `.stat-card` filtered by hasText 'Klicks gesamt' — NEVER a bare `.stat-value` with `.first()`/`.nth()` (three identical `.stat-value` cards render side by side, 16-RESEARCH.md Pitfall 4)"
    - "the fixture Link has NO password and NO expiry and leaves trackingEnabled at its column default (true) — a gated or bot-UA request never reaches recordClickHook (Pitfalls 2/3), and a tracking-off link makes loadAnalytics early-return"
---

<objective>
Prove ANALYTICS-E2E-01 end-to-end through the real network stack + real authenticated dashboard: a genuine redirect-handler click — generated by a real HTTP GET against the public `/${slug}` endpoint, not by seeding a ClickEvent row — surfaces as an incremented "Klicks gesamt" count in the per-link analytics view. The crux from 16-RESEARCH.md: the tracking write is FULLY SYNCHRONOUS relative to the HTTP response (recordClickHook is awaited before the 302 is sent), so the ONLY timing discipline needed is on the frontend read side — the click must be generated BEFORE navigating to /links/:id, because loadAnalytics() fires exactly once on mount with no polling.

This spec asserts REAL, currently-passing Phase 6 behaviour — routes/redirect.ts, lib/analytics.ts, routes/analytics.ts, and LinkDetailView.vue were read in full this research pass and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-15 TDD RED->GREEN precedent: a real bug is fixed, never papered over).

Purpose: Close ANALYTICS-E2E-01 with a real-HTTP tracked click surfacing through the real per-link view + a DB cross-check.
Output: `apps/e2e/tests/authed/analytics-real-click.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-analytics-e2e/16-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ANALYTICS-E2E-01 — real HTTP click surfaces in the per-link analytics view (chromium-admin)</name>
  <read_first>
    - apps/api/src/routes/redirect.ts (VERIFIED via 16-RESEARCH.md: `GET /:slug` awaits recordClickHook — a `$transaction([clickEvent.create, link.update{lifetimeClicks:{increment:1}}])` — BEFORE `reply.code(302).redirect(target)`; the write is committed by the time Playwright's response promise resolves. Bot / expired / password branches return BEFORE recordClickHook, so a fixture with a real BROWSER_UA and no gates is mandatory)
    - apps/web/src/views/LinkDetailView.vue (VERIFIED selectors: the data section renders exactly one of four states; with tracking on + totalClicks>0 the `v-else` data branch renders three `.stat-card`s — labels "Klicks gesamt" / "Letzte 7 Tage" / "Top Referrer" — each with its own `.stat-value`; "Klicks gesamt" `.stat-value` = `{{ totalClicks }}`. `load()` calls `loadAnalytics()` ONCE on mount; loadAnalytics early-returns if `!link.trackingEnabled`. Route name "link-detail", path `/links/:id`)
    - apps/api/src/lib/analytics.ts (VERIFIED: getLinkAnalytics reads totalClicks from Link.lifetimeClicks — the pruning-resistant counter incremented atomically in the SAME $transaction as the ClickEvent insert, never a live COUNT — so the tile and the row can never drift)
    - apps/e2e/src/db.ts (createE2ePrisma; BASELINE_DOMAIN_HOSTNAME = "e2e.kurzly.local" — import this constant, never hardcode the hostname; ClickEvent is already in withResetDbLock's truncate list, so no new isolation code is needed)
    - apps/e2e/src/links.ts (createE2eLink — seed the parent Link with a per-test crypto-random slug and NO password/expiry/UTM; BROWSER_UA — the real browser UA that avoids isbot bot-classification, Pitfall 3)
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts (the DIRECT precedent for this phase's shape: an authed-project spec that straddles an unauthenticated `request.get` click and an authenticated `page` navigation in ONE file, using whole-test `test.describe.configure({ retries: 2 })` + a chromium-admin-only `test.skip` + a testInfo.retry attribution `console.warn`, and a per-test random slug. This is the correct pattern here — NOT fetchWithFixtureRaceRetry, whose single closure cannot wrap the create->click->navigate->DB-assert multi-step shared fixture, for the identical reason qr-dynamic-remap documents in its WR-01 comment)
    - 16-RESEARCH.md (Pattern 1 real click generation with host + BROWSER_UA headers; Pattern 2 generate the click FIRST then navigate; Pitfall 1 one-shot mount fetch; Pitfall 4 `.stat-value` class collision — always scope via `.stat-card` hasText the label)
  </read_first>
  <files>apps/e2e/tests/authed/analytics-real-click.spec.ts</files>
  <action>
    At the top of a single `test.describe`: `test.describe.configure({ retries: 2 })` with a doc-comment (mirror qr-dynamic-remap.spec.ts) — apps/e2e/tests/smoke/db-isolation.spec.ts truncates ClickEvent/Link concurrently during the full-suite phase gate; because the fixture Link is created outside withResetDbLock and shared across the click->navigate->DB-assert steps, a whole-test retry with a fresh per-test random slug is the collision-free equivalent (fetchWithFixtureRaceRetry cannot wrap this multi-step shared fixture — same WR-01 tradeoff qr-dynamic-remap documents). Add a `test.beforeEach(async ({}, testInfo) => {...})` that `test.skip`s when `testInfo.project.name !== "chromium-admin"` (reason: ANALYTICS-E2E-01 is admin-scoped; member/domain-scoped analytics authz is Phase 17 AUTHZ-E2E-01, per 16-CONTEXT.md Deferred Ideas), and emits the same testInfo.retry>0 attribution `console.warn` qr-dynamic-remap uses.

    Single test "real redirect click increments lifetimeClicks and surfaces in the per-link view":
    1. Mint a per-test crypto-random valid-shape slug `e2e-analytics-<hex>` (lowercase-alphanumeric-hyphen, hex from randomUUID().slice(0,8)). createE2ePrisma() inside a try/finally that always `$disconnect`s.
    2. createE2eLink(prisma, { slug, targetUrl: `https://example.com/analytics-target-<hex>` }) — NO password, NO expiry, NO UTM, trackingEnabled OMITTED (Prisma column default true; loadAnalytics needs tracking on to fetch at all).
    3. GENERATE THE CLICK FIRST (before any navigation): `const response = await request.get(`/${slug}`, { headers: { host: BASELINE_DOMAIN_HOSTNAME, "user-agent": BROWSER_UA }, maxRedirects: 0 })`. Assert `response.status()` === 302 (a 200/OG or 410 here means the fixture accidentally hit the bot/expired/password branch — Pitfalls 2/3, not a tracking regression). The awaited server-side transaction guarantees the ClickEvent + lifetimeClicks are already committed now — do NOT add any wait/poll.
    4. FRESH navigation: `await page.goto(`/links/${link.id}`)` (chromium-admin storageState is already authenticated — no login). loadAnalytics() now fires fresh, AFTER the click landed.
    5. UI assertion (scoped, Pitfall 4): `await expect(page.locator(".stat-card", { hasText: "Klicks gesamt" }).locator(".stat-value")).toHaveText("1")`. Never a bare `.stat-value` positional locator.
    6. DB cross-check (deterministic, scoped to this link's id): `expect(await prisma.clickEvent.count({ where: { linkId: link.id } })).toBe(1)` and `expect((await prisma.link.findUniqueOrThrow({ where: { id: link.id } })).lifetimeClicks).toBe(1)`.

    Generate the click strictly BEFORE `page.goto` (Pattern 2 — asserting against an already-mounted page reads stale one-shot-fetch data). Never assert on a page opened before the click. Use no fixed `waitForTimeout` anywhere — Playwright's locator auto-wait covers the render, and the DB write is already committed at step 3.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/analytics-real-click.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the click is a REAL HTTP GET against the public `/${slug}` redirect endpoint (host + BROWSER_UA headers, maxRedirects:0, asserted 302), never a direct ClickEvent insert
    - the per-link view is navigated FRESH after the click (page.goto, not an already-open page) and the `.stat-card` "Klicks gesamt" `.stat-value` reads "1", scoped via hasText (no bare `.stat-value` positional locator)
    - the direct-Prisma cross-check confirms exactly 1 ClickEvent for the link and lifetimeClicks === 1
    - the fixture Link has no password/expiry and tracking left at default (on); the spec uses a per-test random slug + retries:2 and is scoped to chromium-admin (skipped under chromium-member)
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>ANALYTICS-E2E-01 proves a real redirect-handler click (public endpoint, not a seeded row) increments the tracked counter and surfaces as the "Klicks gesamt" value in the real per-link analytics view — DB-and-UI cross-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> GET /:slug (redirect handler) | intentionally-unauthenticated real click that triggers the tracking write |
| host browser (admin session) -> GET /api/links/:id/analytics | 401-gated, IDOR-guarded (resolveOwnedLink) per-link analytics read |
| test runner -> E2E Postgres :5433 | direct-Prisma fixture seeding + ClickEvent/lifetimeClicks assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01-IDOR | Elevation of Privilege | resolveOwnedLink on GET /api/links/:id/analytics | low | accept | Exercised incidentally under the admin session; the full role x resource analytics denial matrix is Phase 17 (AUTHZ-E2E-01) plus the v1.0 integration Denial-Suite — this phase deliberately does not re-prove it |
| T-16-01-PII | Information disclosure | ClickEvent row written by recordClickHook | low | accept | ClickEvent structurally has no raw-IP column (only a derived visitorHash + coarse country) — the spec asserts row existence + lifetimeClicks only, never a raw IP, so no PII is introduced or asserted (privacy-by-design, 16-RESEARCH.md Security Domain) |
| T-16-01-SQLI | Tampering | lib/analytics.ts getLinkAnalytics raw SQL | low | accept | Every query is a parameterized Prisma.sql tagged template (T-06-SQLI), already unit/integration-tested in Phase 6 and unmodified by this test-authoring phase |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/analytics-real-click.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (the spec is registered under chromium-member too, where it is skipped) — confirm no new db-isolation ClickEvent/Link truncate race beyond what retries:2 absorbs.
</verification>

<success_criteria>
ANALYTICS-E2E-01 passes: a real redirect-handler click generated over HTTP surfaces as the incremented "Klicks gesamt" value in the real per-link analytics view, cross-checked at the database (1 ClickEvent, lifetimeClicks === 1).
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/analytics-real-click.spec.ts` — ANALYTICS-E2E-01 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/16-analytics-e2e/16-01-SUMMARY.md` when done.
</output>
