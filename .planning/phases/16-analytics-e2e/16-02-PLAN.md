---
phase: 16-analytics-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/analytics-tracking-off.spec.ts
autonomous: true
requirements: [ANALYTICS-E2E-02]

must_haves:
  truths:
    - "Tracking is turned OFF through the REAL LinkDetailView `.toggle` (role=switch) — a real updateLink PATCH round-trip that persists trackingEnabled=false — proving a GENUINELY SETTABLE state (16-CONTEXT.md locked decision), not merely a fixture column value"
    - "A real HTTP GET against the now-tracking-off link STILL returns 302 — tracking-off suppresses only the tracking write (recordClickHook's structural early-return `if (!link.trackingEnabled) return;`), never the redirect itself"
    - "prisma.clickEvent.count for the link is EXACTLY 0 and Link.lifetimeClicks is unchanged (0) — a true DB-level zero-rows guarantee, asserted at the database, not a display-time filter"
  artifacts:
    - apps/e2e/tests/authed/analytics-tracking-off.spec.ts
  key_links:
    - "the toggle PATCH must be awaited (trackingEnabled=false committed) BEFORE the click is generated — the redirect handler loads the link fresh from DB per request, so an un-awaited toggle would leave recordClickHook seeing tracking still enabled"
    - "the fixture Link has NO password and NO expiry and the click uses BROWSER_UA + host=BASELINE_DOMAIN_HOSTNAME — so the 302 proves the request reached (and passed through) the state==='ok' branch where the tracking guard actually fires, not a gate/bot early-return (Pitfalls 2/3)"
---

<objective>
Prove ANALYTICS-E2E-02 end-to-end: with tracking toggled OFF, a real redirect-handler click still succeeds (302) but provably writes ZERO tracking rows — asserted directly at the database, never merely absent from a UI list. The mechanism under test is recordClickHook's structural early-return (`if (!link.trackingEnabled) return;`, BEFORE any Prisma call, 16-RESEARCH.md), so the zero-rows guarantee is structural, not a display-time filter.

Per 16-CONTEXT.md's locked decision ("tracking-off must be a genuinely settable state") and 16-RESEARCH.md's Assumption A1 / recommended path, tracking is turned off through the REAL `.toggle` UI (a live updateLink PATCH round-trip), not just the fixture column — this proves the full toggle-through-UI path, a strictly stronger proof than the fixture-only guard (which Phase 6's own integration tests already cover).

This spec asserts REAL, currently-passing behaviour — LinkDetailView.vue's toggleTracking and routes/redirect.ts's guard were read in full this research pass and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-15 TDD RED->GREEN precedent).

Purpose: Close ANALYTICS-E2E-02 with a real-UI toggle-off + real-HTTP click + DB-level zero-rows proof.
Output: `apps/e2e/tests/authed/analytics-tracking-off.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-analytics-e2e/16-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: ANALYTICS-E2E-02 — tracking-off via real toggle, real click, DB-asserted zero rows (chromium-admin)</name>
  <read_first>
    - apps/web/src/views/LinkDetailView.vue (VERIFIED selectors + behaviour: the tracking card is `.tracking-card`; its toggle is `<div class="toggle" :class="{ active: link.trackingEnabled }" role="switch" :aria-checked="link.trackingEnabled" @click="toggleTracking">`. toggleTracking() flips optimistically then `await updateLink(id, { trackingEnabled: next })` — a PATCH /api/links/:id round-trip. With tracking ON by default, ONE click turns it OFF: `.toggle` loses `.active` and aria-checked becomes "false", and the data section switches to the `.dashed-empty` state with copy "Tracking ist für diesen Link deaktiviert — es werden keine Klickdaten gespeichert.". Route name "link-detail", path `/links/:id`)
    - apps/api/src/routes/redirect.ts (VERIFIED via 16-RESEARCH.md: recordClickHook's FIRST line is `if (!link.trackingEnabled) return;` — before any Prisma call; the handler loads the link fresh from DB per request, so the toggle's PATCH must be committed before the click. The 302 is still returned regardless — tracking-off never breaks the redirect)
    - apps/e2e/src/db.ts (createE2ePrisma; BASELINE_DOMAIN_HOSTNAME = "e2e.kurzly.local" — import, never hardcode; ClickEvent already in withResetDbLock's truncate list)
    - apps/e2e/src/links.ts (createE2eLink — seed the parent Link with a per-test crypto-random slug, NO password/expiry/UTM, trackingEnabled OMITTED so it starts ON and the toggle has something to turn off; BROWSER_UA)
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts (the DIRECT precedent: authed spec straddling a real `page` UI mutation + PATCH waitForResponse and an unauthenticated `request.get`, whole-test `retries: 2`, chromium-admin-only skip, testInfo.retry attribution warn, per-test random slug. Note the `Promise.all([page.waitForResponse(...), <trigger>])` shape used to await a PATCH triggered by a UI interaction)
    - 16-RESEARCH.md (Pattern 3 DB-level zero-rows assertion + the two toggle mechanisms — this plan uses the real-UI toggle, the stronger "genuinely settable" proof; Pitfall 2/3 gate/bot precedence; Pitfall 1 one-shot mount fetch is not relevant here since the assertion is DB-level, but the toggle PATCH ordering IS)
  </read_first>
  <files>apps/e2e/tests/authed/analytics-tracking-off.spec.ts</files>
  <action>
    Top of a single `test.describe`: `test.describe.configure({ retries: 2 })` with a doc-comment (mirror qr-dynamic-remap.spec.ts) for the documented db-isolation ClickEvent/Link cross-file truncate race; the shared fixture spans toggle->click->DB-assert so a whole-test retry with a fresh per-test random slug is the collision-free equivalent (fetchWithFixtureRaceRetry cannot wrap this multi-step shared fixture). Add a `test.beforeEach(async ({}, testInfo) => {...})` that `test.skip`s when `testInfo.project.name !== "chromium-admin"` (reason: ANALYTICS-E2E-02 is admin-scoped; member/domain-scoped analytics authz is Phase 17 AUTHZ-E2E-01, per 16-CONTEXT.md Deferred Ideas) and emits the testInfo.retry>0 attribution `console.warn`.

    Single test "tracking off via real toggle: redirect still 302s, zero ClickEvent rows written":
    1. Mint a per-test crypto-random slug `e2e-track-off-<hex>`. createE2ePrisma() in a try/finally that always `$disconnect`s.
    2. createE2eLink(prisma, { slug, targetUrl: `https://example.com/no-tracking-<hex>` }) — NO password/expiry/UTM, trackingEnabled OMITTED (starts ON so the toggle can turn it OFF).
    3. `await page.goto(`/links/${link.id}`)` (chromium-admin storageState — no login). Assert the toggle starts ON: `await expect(page.locator(".tracking-card .toggle")).toHaveClass(/active/)` (or aria-checked="true").
    4. TURN TRACKING OFF via the REAL toggle, awaiting the PATCH: `const [patch] = await Promise.all([ page.waitForResponse(r => r.request().method() === "PATCH" && new URL(r.url()).pathname === `/api/links/${link.id}`), page.locator(".tracking-card .toggle").click() ]); expect(patch.ok()).toBe(true)`. Then confirm the persisted OFF state in the UI: `await expect(page.locator(".tracking-card .toggle")).not.toHaveClass(/active/)` AND `await expect(page.locator(".dashed-empty")).toBeVisible()`.
    5. DB guard belt-and-suspenders: reload the link via `prisma.link.findUniqueOrThrow` and assert `trackingEnabled === false` — confirming the PATCH committed BEFORE the click (the redirect handler reads a fresh link row per request).
    6. GENERATE THE REAL CLICK: `const response = await request.get(`/${slug}`, { headers: { host: BASELINE_DOMAIN_HOSTNAME, "user-agent": BROWSER_UA }, maxRedirects: 0 })`. Assert `response.status()` === 302 (the redirect still works with tracking off; a 200/OG or 410 means a gate/bot early-return — Pitfalls 2/3, wrong branch).
    7. DB-LEVEL ZERO-ROWS assertion (the requirement's core): `expect(await prisma.clickEvent.count({ where: { linkId: link.id } })).toBe(0)` AND `expect((await prisma.link.findUniqueOrThrow({ where: { id: link.id } })).lifetimeClicks).toBe(0)`. Both are DB-asserted true zero-rows, not a display-time filter.

    The toggle PATCH (step 4/5) MUST be committed before the click (step 6) or the guard would still see tracking enabled. Use the `Promise.all([waitForResponse, click])` shape — never a fixed sleep.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/analytics-tracking-off.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - tracking is turned off through the REAL `.tracking-card .toggle` (a live PATCH /api/links/:id round-trip, awaited via waitForResponse), and the UI confirms the off state (toggle not .active + `.dashed-empty` visible)
    - the real HTTP click against `/${slug}` (host + BROWSER_UA, maxRedirects:0) still returns 302
    - `prisma.clickEvent.count` for the link is exactly 0 AND Link.lifetimeClicks is 0 — DB-asserted zero-rows
    - the toggle PATCH is committed (verified via a fresh Prisma read of trackingEnabled===false) before the click is generated
    - the spec uses a per-test random slug + retries:2 and is scoped to chromium-admin (skipped under chromium-member); passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>ANALYTICS-E2E-02 proves that after turning tracking off through the real UI toggle, a real redirect click still 302s yet writes zero ClickEvent rows and never increments lifetimeClicks — a genuine, DB-asserted zero-rows guarantee.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) -> PATCH /api/links/:id | authenticated tracking-toggle mutation (updateLink single write path, D-15) |
| public internet -> GET /:slug (redirect handler) | intentionally-unauthenticated real click whose tracking write must be suppressed |
| test runner -> E2E Postgres :5433 | direct-Prisma fixture seeding + zero-rows / lifetimeClicks assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-02-TRACKOFF | Information disclosure | recordClickHook trackingEnabled early-return guard | low | mitigate | The assertion pins clickEvent.count===0 AND lifetimeClicks===0 at the database directly — a regression that moved or removed the structural guard (writing a row despite tracking-off) would fail this assertion loudly, encoding the privacy guarantee the toggle promises |
| T-16-02-IDOR | Elevation of Privilege | updateLink PATCH ownership check | low | accept | Exercised incidentally under the admin session; the full role x resource denial matrix is Phase 17 (AUTHZ-E2E-01) plus the v1.0 integration Denial-Suite |
| T-16-02-REDIRECT | Denial of Service | redirect hot path with tracking off | low | accept | The spec asserts the 302 still fires with tracking off (tracking-off must never break the core redirect) — recordClickHook's own try/catch resilience is already Phase 6-tested and unmodified here |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/analytics-tracking-off.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (registered under chromium-member too, where it is skipped) — confirm no new db-isolation ClickEvent/Link truncate race beyond what retries:2 absorbs.
</verification>

<success_criteria>
ANALYTICS-E2E-02 passes: after a real-UI toggle-off, a real redirect click still 302s but produces exactly zero ClickEvent rows and no lifetimeClicks increment, asserted at the database.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/analytics-tracking-off.spec.ts` — ANALYTICS-E2E-02 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/16-analytics-e2e/16-02-SUMMARY.md` when done.
</output>
