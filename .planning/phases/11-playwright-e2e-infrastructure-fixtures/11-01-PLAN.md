---
phase: 11-playwright-e2e-infrastructure-fixtures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - apps/e2e/package.json
  - apps/e2e/tsconfig.json
  - apps/e2e/playwright.config.ts
  - apps/e2e/tests/smoke/prisma-import.spike.spec.ts
  - .gitignore
autonomous: true
requirements: [INFRA-01]
must_haves:
  truths:
    - "pnpm install resolves @kurzly/e2e as a workspace member with @playwright/test installed"
    - "PrismaClient imported from @kurzly/api/prisma-client resolves and is a constructor under Playwright's runtime (closes RESEARCH Open Question 1)"
  artifacts:
    - "apps/e2e/package.json"
    - "apps/e2e/tsconfig.json"
    - "apps/e2e/playwright.config.ts"
    - "apps/e2e/tests/smoke/prisma-import.spike.spec.ts"
    - "apps/api/package.json exports map with a ./prisma-client subpath"
  key_links:
    - "apps/api/package.json exports['./prisma-client'] -> ./src/generated/prisma/client.ts, consumed by apps/e2e as its only @kurzly/api import"
---

<objective>
Scaffold the `apps/e2e` pnpm workspace package (`@kurzly/e2e`) and prove the single genuinely-unverified mechanical risk in this phase up front: that `apps/api`'s Prisma 7 generated client resolves as a raw-`.ts` subpath export under Playwright's own runtime.

Purpose: This is the Wave-0 foundation. Every downstream plan (compose boot, DB reset/seed, Mailpit wiring, auth fixture) imports the Prisma client through this subpath export and runs under this Playwright config. RESEARCH Open Question 1 (A1) flags the raw-`.ts` export as asserted-but-not-executed in this repo — so it must be the very first thing proven, before any fixture code is built on top of it.
Output: A resolvable `@kurzly/e2e` package with a Playwright config, the `apps/api` `./prisma-client` subpath export, and a passing spike spec that imports and instantiates the client type under Playwright.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-CONTEXT.md
@.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-RESEARCH.md
@apps/api/package.json
@apps/api/src/db.ts
@pnpm-workspace.yaml
@tsconfig.base.json
@apps/api/tsconfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the ./prisma-client subpath export to apps/api/package.json</name>
  <files>apps/api/package.json</files>
  <read_first>
    - apps/api/package.json (currently has "main": "dist/server.js" and NO "exports" field — must be added from scratch, not extended)
    - apps/api/src/generated/prisma/client.ts (the actual entry point — exports PrismaClient, Prisma namespace, model types; there is no index.js/index.ts and no compiled .js/.d.ts anywhere in this tree)
    - apps/api/src/db.ts (shows the internal import convention: `import { PrismaClient } from "./generated/prisma/client.js"`)
    - packages/shared/package.json (the ONLY existing cross-package exports precedent — points at compiled dist/index.js; this new export deliberately diverges by pointing at raw .ts source, RESEARCH "Structure Deviation")
  </read_first>
  <action>
    Add an `exports` field to apps/api/package.json alongside the existing `"main": "dist/server.js"`. Map `"."` to `"./dist/server.js"` (preserving current bare-import behavior) and add `"./prisma-client"` pointing at `"./src/generated/prisma/client.ts"`. Because Prisma 7's `prisma-client` provider ships self-describing TypeScript source (no separate declaration file), point both the `types` and `import`/`default` conditions at the same `client.ts` file. Do NOT copy packages/shared's `dist/index.js` + `.d.ts` shape — that would point at a compiled artifact that does not exist for the generated Prisma client. Leave the `"main"` field in place unchanged. Do not add `@prisma/adapter-pg` here (that is a later plan's concern) and do not export anything beyond this one subpath — apps/e2e must never be able to import API route/business-logic modules.
  </action>
  <verify>
    <automated>node -e "const p=require('./apps/api/package.json'); if(!p.exports||p.exports['./prisma-client']!=='./src/generated/prisma/client.ts'){console.error('missing/incorrect ./prisma-client export');process.exit(1)} if(p.exports['.']!=='./dist/server.js'){console.error('. export must remain dist/server.js');process.exit(1)} console.log('exports OK')"</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json has an `exports` object with key `"./prisma-client"` whose value is `"./src/generated/prisma/client.ts"`.
    - The `"."` export resolves to `"./dist/server.js"` and the existing `"main"` field is unchanged.
    - `pnpm --filter @kurzly/api typecheck` still passes (no regression from the package.json change).
  </acceptance_criteria>
  <done>The apps/api package exposes exactly one new subpath, `@kurzly/api/prisma-client`, resolving to the generated Prisma client source.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold the apps/e2e package, Playwright config, tsconfig, and .gitignore entries</name>
  <files>apps/e2e/package.json, apps/e2e/tsconfig.json, apps/e2e/playwright.config.ts, .gitignore</files>
  <read_first>
    - pnpm-workspace.yaml (the `apps/*` glob already covers apps/e2e — no workspace-config change needed)
    - tsconfig.base.json and apps/api/tsconfig.json (the extension pattern apps/e2e's tsconfig must mirror)
    - .gitignore (current entries — new lines are additive)
    - RESEARCH.md "Recommended Project Structure" and "Standard Stack" (Playwright version, install command)
  </read_first>
  <action>
    Create apps/e2e/package.json: name `@kurzly/e2e`, `"private": true`, `"type": "module"`, `"version": "0.0.0"`, a `"test": "playwright test"` script and a `"typecheck": "tsc --noEmit"` script, a devDependency on `@playwright/test` at `^1.61.1` (verified OK in RESEARCH Package Legitimacy Audit — no legitimacy checkpoint required), and a `"@kurzly/api": "workspace:*"` dependency (used ONLY for the `@kurzly/api/prisma-client` subpath — never route/business-logic imports). Create apps/e2e/tsconfig.json extending `../../tsconfig.base.json` with `"types": ["node"]` and `"include": ["."]`. Create apps/e2e/playwright.config.ts: `testDir: './tests'`, `fullyParallel: true`, top-level `use.baseURL` from `process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000'`, `use.trace: 'on-first-retry'`, reporter `[['list'], ['html', { open: 'never' }]]`, and a single `smoke` project (name `smoke`, `testMatch: /smoke\/.*\.spec\.ts$/`, no storageState). Do NOT reference a globalSetup/globalTeardown or add the setup/chromium-admin/chromium-member projects yet — later plans add those. Install the Chromium browser by running `pnpm install` at the repo root then `pnpm --filter @kurzly/e2e exec playwright install --with-deps chromium`. Append three lines to .gitignore: `apps/e2e/playwright/.auth/`, `apps/e2e/playwright-report/`, `apps/e2e/test-results/`.
  </action>
  <verify>
    <automated>pnpm install --frozen-lockfile=false >/dev/null 2>&1; pnpm --filter @kurzly/e2e list @playwright/test && grep -q "apps/e2e/playwright/.auth/" .gitignore && grep -q "apps/e2e/test-results/" .gitignore && echo "scaffold OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @kurzly/e2e list @playwright/test` prints an installed `@playwright/test` at a 1.61.x version.
    - apps/e2e/playwright.config.ts exports a config whose `use.baseURL` defaults to `http://localhost:3000` and defines a `smoke` project matching `tests/smoke/*.spec.ts`.
    - .gitignore contains all three new lines (`apps/e2e/playwright/.auth/`, `apps/e2e/playwright-report/`, `apps/e2e/test-results/`).
    - `pnpm --filter @kurzly/e2e exec playwright --version` runs successfully (Chromium installed).
  </acceptance_criteria>
  <done>`@kurzly/e2e` resolves as a workspace member with Playwright installed and a config that a smoke spec can run against.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the Prisma-client subpath resolves under Playwright's runtime (RESEARCH OQ-1 spike)</name>
  <files>apps/e2e/tests/smoke/prisma-import.spike.spec.ts</files>
  <read_first>
    - RESEARCH.md "Prisma Client Subpath Export — Verified Mechanics" and "Open Questions" #1 (the raw-.ts export is asserted from general TypeScript/Playwright behavior, not yet executed in this repo — this spike executes it)
    - apps/api/src/generated/prisma/client.ts (confirm `PrismaClient` is a named export)
  </read_first>
  <action>
    Create apps/e2e/tests/smoke/prisma-import.spike.spec.ts: a single Playwright test that does a static `import { PrismaClient } from '@kurzly/api/prisma-client'` at the top of the file and asserts inside the test body that `typeof PrismaClient === 'function'` (i.e. it resolved to a constructor). Do NOT instantiate the client or open a database connection here — this spike proves ONLY module resolution + TypeScript transform under Playwright's runtime, which is the actual unverified risk; DB connectivity is proven in a later plan once the compose stack publishes port 5433. If this import fails to resolve, the documented fallback (RESEARCH "Recommendation for the plan") is a dedicated `tsc --emitDeclarationOnly`/esbuild step in apps/api emitting a compiled `.js`+`.d.ts` for this subpath — record that in the SUMMARY if the raw-.ts path does not resolve, do not silently pre-build the fallback.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/e2e exec playwright test tests/smoke/prisma-import.spike.spec.ts --project=smoke --reporter=list</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @kurzly/e2e exec playwright test tests/smoke/prisma-import.spike.spec.ts --project=smoke` exits 0.
    - The spec imports from the bare specifier `@kurzly/api/prisma-client` (not a relative path into apps/api/src) and asserts `PrismaClient` is a function/constructor.
    - No database connection is opened by this spec (no `new PrismaClient(...)`, no `.connect()`).
  </acceptance_criteria>
  <done>The raw-`.ts` Prisma-client subpath export provably resolves and typechecks under Playwright's runtime in this repo — RESEARCH Open Question 1 / Assumption A1 is closed green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> repo | New devDependency `@playwright/test` is installed into the workspace |
| apps/e2e -> apps/api | New cross-package import surface via the `./prisma-client` subpath export |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-SC | Tampering | npm install of `@playwright/test` | high | mitigate | Package verified OK in RESEARCH Package Legitimacy Audit (official microsoft/playwright, ~48M weekly downloads) — no `[ASSUMED]`/`[SUS]` verdict, so no blocking-human checkpoint required; pin `^1.61.1`, install via `pnpm --filter @kurzly/e2e add -D` only. |
| T-11-01a | Elevation of Privilege | `./prisma-client` export surface | medium | mitigate | Export exactly ONE subpath (the generated client); do not add a `"."`-wildcard or a subpath that would let apps/e2e import API route/business-logic modules (keeps apps/e2e a black-box HTTP consumer for everything except DB seed/reset). |
</threat_model>

## Artifacts this plan produces

- `apps/e2e/package.json` — new package manifest for `@kurzly/e2e` (devDep `@playwright/test@^1.61.1`, dep `@kurzly/api@workspace:*`); npm scripts `test` (`playwright test`) and `typecheck`.
- `apps/e2e/tsconfig.json` — TS config extending `tsconfig.base.json`.
- `apps/e2e/playwright.config.ts` — Playwright config; exports the `smoke` project and `PLAYWRIGHT_BASE_URL`-driven `baseURL` (default `http://localhost:3000`).
- `apps/e2e/tests/smoke/prisma-import.spike.spec.ts` — spike spec proving the Prisma-client subpath import.
- `apps/api/package.json` `exports` map — new subpath `@kurzly/api/prisma-client` -> `./src/generated/prisma/client.ts`.
- `.gitignore` additions: `apps/e2e/playwright/.auth/`, `apps/e2e/playwright-report/`, `apps/e2e/test-results/`.

<verification>
- `pnpm install` resolves the new `@kurzly/e2e` workspace member with no lockfile errors.
- `pnpm --filter @kurzly/e2e exec playwright test tests/smoke/prisma-import.spike.spec.ts --project=smoke` exits 0.
- `pnpm --filter @kurzly/api typecheck` still passes after the package.json exports change.
</verification>

<success_criteria>
- `@kurzly/e2e` is an installable workspace package with Playwright + Chromium ready.
- The `@kurzly/api/prisma-client` subpath resolves and instantiates as a constructor under Playwright's runtime (OQ-1 closed).
- No credential/session artifacts can be committed (.gitignore entries in place).
</success_criteria>

<output>
Create `.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-01-SUMMARY.md` when done. Record whether the raw-`.ts` subpath resolved directly or the compiled-artifact fallback was needed.
</output>
