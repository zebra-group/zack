---
phase: 04
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
autonomous: false
requirements: [LINK-01, LINK-08]
must_haves:
  truths:
    - "csv-parse and nanoid are installed in apps/api at the RESEARCH-pinned versions ONLY after an explicit human supply-chain sign-off (T-04-SC-Gate)."
    - "Both packages import cleanly under the repo's ESM ('type: module') setup: `csv-parse/sync` and `nanoid`'s `customAlphabet` resolve without a CJS-only trap."
  artifacts:
    - "apps/api/package.json — csv-parse + nanoid as dependencies"
    - "pnpm-lock.yaml — resolved lockfile entries for both packages"
  key_links:
    - "The supply-chain human checkpoint (T-04-SC-Gate) gates the FIRST `pnpm add` of csv-parse/nanoid — mirrors Phase 2's 02-01 T-02-SC-Gate precedent; never auto-approvable."
    - "nanoid enables the D-02 Base62 auto-slug generator (LINK-01); csv-parse enables the D-05 two-phase CSV import parser (LINK-08) — both consumed in 04-02 and 04-04 respectively."
  prohibitions:
    - "No `pnpm add` runs before the human supply-chain sign-off is recorded in this plan's SUMMARY."
    - "No [SLOP]-verdict package is installed; both packages carry only a false-positive 'too-new' SUS verdict per the RESEARCH Package Legitimacy Audit."
    - "No package with a postinstall lifecycle script is auto-approved (RESEARCH confirmed neither has one)."
---

## Phase Goal

**As a** team member, **I want to** the two new libraries this phase needs (CSV parsing + secure slug generation) vetted and installed, **so that** the single authorized link-creation path and the CSV bulk importer can be built on trusted dependencies. (Enabling slice for Phase 4 Success Criteria 1 & 3.)

<objective>
Vet and install this phase's two new packages — `csv-parse` (server-side CSV parsing, LINK-08/D-05) and `nanoid` (Base62 auto-slug generation, LINK-01/D-02) — behind a blocking human supply-chain checkpoint, mirroring Phase 2's 02-01 supply-chain gate.

Purpose: Both later backend plans (04-02's slug generator, 04-04's CSV importer) import these packages; installing them first unblocks the wave chain, and the human gate satisfies the Package Legitimacy protocol for the two "too-new" SUS verdicts (both are massive, long-established packages — 15.6M and 197.7M weekly downloads — flagged only because a recent patch release tripped the automated "too-new" heuristic).
Output: `csv-parse` and `nanoid` as `apps/api` dependencies at the RESEARCH-verified versions, plus the resolved lockfile.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-links-management-bulk-import/04-CONTEXT.md
@.planning/phases/04-links-management-bulk-import/04-RESEARCH.md
@.claude/CLAUDE.md
</context>

## Artifacts this plan produces

- **Dependencies:** `csv-parse` (^7.0.1), `nanoid` (^5.1.16) in `apps/api/package.json`.
- **Lockfile:** resolved `pnpm-lock.yaml` entries.
- **Approval record:** the supply-chain sign-off, captured in this plan's SUMMARY so 04-02/04-04 proceed.

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain legitimacy sign-off for csv-parse + nanoid (T-04-SC-Gate)</name>
  <read_first>
    - .planning/phases/04-links-management-bulk-import/04-RESEARCH.md (## Package Legitimacy Audit table + the disposition notes on the "too-new" SUS false positives + the "No postinstall scripts" confirmation)
    - .planning/phases/02-magic-link-auth-app-shell-domain-authorization-core/02-01-PLAN.md (the T-02-SC-Gate blocking-human precedent this task mirrors exactly)
    - .claude/CLAUDE.md (## Technology Stack — qrcode/sharp appear there for Phase 7; for THIS phase confirm csv-parse + nanoid only)
  </read_first>
  <what-built>
    Nothing is built yet — this is a review gate. The RESEARCH Package Legitimacy Audit lists `csv-parse` and `nanoid`, both with a `SUS` verdict whose sole reason is `too-new` (a recent patch-version publish date), not package age. csv-parse is the `node-csv` project (github.com/adaltas/node-csv, 15.6M weekly downloads); nanoid is github.com/ai/nanoid (197.7M weekly downloads). No [SLOP] verdict exists; neither package has a postinstall script.
  </what-built>
  <how-to-verify>
    1. Open the `## Package Legitimacy Audit` table in 04-RESEARCH.md.
    2. For `csv-parse`: confirm on npmjs.com/package/csv-parse the name matches exactly, the source repo is github.com/adaltas/node-csv, and weekly downloads are in the millions.
    3. For `nanoid`: confirm on npmjs.com/package/nanoid the name matches exactly, the source repo is github.com/ai/nanoid, and weekly downloads are in the hundred-millions.
    4. Confirm both verdicts are `SUS (too-new)` false positives (no [SLOP]), and that neither package declares a `postinstall` script.
  </how-to-verify>
  <action>Do NOT run any `pnpm add` in this plan until this gate is approved. Present the RESEARCH.md Package Legitimacy Audit table to the operator and request explicit approval per the Package Legitimacy Gate protocol (Threat T-04-SC-Gate). This gate is never auto-approvable regardless of workflow.auto_advance / yolo mode. On approval, proceed to Task 2. If the operator flags either package, stop and escalate before any install.</action>
  <verify>
    <human-check>Operator types "approved" after confirming csv-parse and nanoid against npmjs.com (exact name, source repo, download counts) and that no [SLOP] verdict and no postinstall script is present.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator has explicitly approved csv-parse and nanoid before any install runs.
    - No [SLOP]-verdict package appears on the install list; both flagged packages are confirmed "too-new" false positives.
    - Approval is recorded in this plan's SUMMARY so downstream plans (04-02, 04-04) proceed.
  </acceptance_criteria>
  <resume-signal>Type "approved" to release the install, or name the package(s) to investigate.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install csv-parse + nanoid and prove ESM resolution</name>
  <files>apps/api/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (current dependency block — the "type": "module" + Prisma-7 ESM setup the new imports must resolve under)
    - .planning/phases/04-links-management-bulk-import/04-RESEARCH.md (## Standard Stack "Installation" + "Version verification" — csv-parse/sync exports an `.import` condition, nanoid resolves via default/browser conditions)
  </read_first>
  <action>After Task 1 approval, run `pnpm --filter @kurzly/api add csv-parse nanoid`, resolving to the RESEARCH-pinned versions (csv-parse ^7.0.1, nanoid ^5.1.16). Do NOT install any other package. Then prove both resolve under the repo's ESM setup by writing a one-off throwaway check (or a temporary Vitest assertion) that imports `parse` from "csv-parse/sync" and `customAlphabet` from "nanoid", calls `customAlphabet("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz", 7)()` (expect a 7-char string) and `parse("a,b\n1,2", { columns: true })` (expect one row object) — delete the throwaway after confirming, OR fold the assertion into the 04-02 core test file rather than committing a scratch file. Confirm neither package pulled a build-requiring transitive dep that needs a pnpm-workspace allowBuilds entry (per STATE Phase-1 precedent for cpu-features/protobufjs); if one appears, leave it unapproved (allowBuilds:false) and note it in the SUMMARY.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec node -e "import('csv-parse/sync').then(m=>{if(typeof m.parse!=='function')process.exit(1)}); import('nanoid').then(m=>{if(typeof m.customAlphabet!=='function')process.exit(1)})" && pnpm --filter @kurzly/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/package.json` lists `csv-parse` and `nanoid` as dependencies at the pinned versions.
    - `pnpm-lock.yaml` contains resolved entries for both.
    - The ESM resolution check passes: `parse` and `customAlphabet` are callable functions; `tsc --noEmit` is green.
    - No unexpected build-script-requiring transitive dependency was auto-approved.
  </acceptance_criteria>
  <done>csv-parse and nanoid are installed post-approval at pinned versions and proven to import under the repo's ESM configuration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build/image | Third-party package code is executed at install and runtime; untrusted until vetted |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-SC-Gate | Tampering | npm installs (csv-parse, nanoid) | high | mitigate | Blocking-human supply-chain checkpoint (Task 1) verifies each package identity/version/source-repo against npmjs.com before the first `pnpm add`; never auto-approvable; both are confirmed "too-new" SUS false positives with no postinstall script. |
</threat_model>

<verification>
- Human sign-off recorded before any install.
- `pnpm --filter @kurzly/api exec tsc --noEmit` passes with the new deps present.
- ESM resolution check green (csv-parse/sync + nanoid customAlphabet callable).
</verification>

<success_criteria>
- csv-parse and nanoid installed post-approval at RESEARCH-pinned versions; no [SLOP] package; no unapproved build script.
- Both packages proven to import under the repo's ESM setup; the supply-chain approval is recorded for downstream plans.
</success_criteria>

<output>
Create `.planning/phases/04-links-management-bulk-import/04-01-SUMMARY.md` when done (record the supply-chain approval).
</output>
