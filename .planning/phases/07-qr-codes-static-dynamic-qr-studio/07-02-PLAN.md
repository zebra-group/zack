---
phase: 07-qr-codes-static-dynamic-qr-studio
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - apps/api/package.json
  - apps/api/prisma/schema.prisma
  - apps/api/test/qrRenderSmoke.test.ts
autonomous: true
requirements: [QR-02, QR-03, QR-04, QR-07]
user_setup: []

must_haves:
  truths:
    - "qrcode + sharp are installed as prod deps of @kurzly/api; jsqr + @types/qrcode as dev deps"
    - "A QrCode row (static→bound Link, or dynamic→own /q code + current target Link + optional logo bytes) and a QrRemapHistory row can be persisted in a real Postgres testcontainer after migration"
    - "sharp can rasterize an SVG string to PNG and return raw RGBA pixels (.raw().toBuffer({resolveWithObject})) on this host's platform"
  artifacts:
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations
    - apps/api/test/qrRenderSmoke.test.ts
  key_links:
    - "QrCode.variant discriminates static vs dynamic; dynamic owns a globally-unique code column"
    - "QrRemapHistory.qrCodeId FK cascade-deletes with its QrCode (mirrors ClickEvent→Link)"
    - "sharp SVG→PNG + .raw() pixel path is the exact seam the 07-03 decode-round-trip test depends on"
---

## Phase Goal

**As a** dashboard user, **I want to** create static and dynamic QR codes for my short links — with a centered logo, custom color and rounded-module styling, re-pointable dynamic targets that keep a printed code valid, and a visible scan count — **so that** I can print scannable codes that keep working even after I change where they point.

> Derived from the ROADMAP Phase 7 `**Goal:**` line into user-story shape (MVP framing).

<objective>
Install the phase's approved dependencies and lay down the QrCode + QrRemapHistory data model with a Prisma migration, then prove sharp's image pipeline works on this host before any renderer depends on it. This is the [BLOCKING] foundation wave: 07-03 (render core) needs the installed binaries, and 07-04 (service) needs the generated Prisma client.

Purpose: A single migration + a verified sharp pipeline de-risks the two Assumptions Log items (A4 sharp `.raw()` shape, A5 Alpine SVG rasterization) before the decode-round-trip test is written.
Output: Updated `apps/api/package.json` + lockfile, new QrCode/QrRemapHistory models + migration + regenerated client, and a passing sharp smoke test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qr-codes-static-dynamic-qr-studio/07-RESEARCH.md
@.planning/phases/07-qr-codes-static-dynamic-qr-studio/07-PATTERNS.md
@apps/api/prisma/schema.prisma
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install qrcode + sharp (prod) and jsqr + @types/qrcode (dev)</name>
  <files>apps/api/package.json</files>
  <action>Only proceed if 07-01's supply-chain checkpoint is approved. Run `pnpm --filter @kurzly/api add qrcode sharp` then `pnpm --filter @kurzly/api add -D jsqr @types/qrcode`. Pin the versions RESEARCH.md verified on 2026-07-20 (qrcode 1.5.4, sharp 0.35.3, jsqr 1.4.0, @types/qrcode 1.5.6) unless the registry now serves a newer patch — re-check `sharp` specifically given its frequent point-release cadence. Do NOT add @fastify/multipart or any other package (logo upload is handled as base64 in JSON per 07-05, Claude's discretion per CONTEXT). Do NOT add a client-side qrcode bundle to apps/web (CONTEXT locks server-only rendering).</action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec node -e "require('qrcode'); require('sharp'); require('jsqr'); console.log('deps-ok')"</automated>
  </verify>
  <done>qrcode + sharp appear under dependencies and jsqr + @types/qrcode under devDependencies of apps/api/package.json; the require smoke prints deps-ok; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add QrCode + QrRemapHistory models and QrCodeVariant enum, migrate, generate</name>
  <files>apps/api/prisma/schema.prisma</files>
  <action>Mirror the Link + ClickEvent model conventions (schema.prisma lines 165-246) exactly. Add a top-level `enum QrCodeVariant { static dynamic }` in the same lowercase-value style as `ScanSource`. Add `model QrCode` with: `id String @id @default(cuid())`; `variant QrCodeVariant`; `linkId String` (the bound Link for static, or the current target Link for dynamic — a single Link relation used for both since a static QR's bound link IS its target); `code String? @unique` (the /q/:code short code — present only for dynamic; globally unique flat namespace, NOT domain-scoped, so a plain `@unique`, unlike Link's composite `@@unique([domainId, slug])`); style fields `color String`, `roundedModules Boolean @default(false)`, `logoEnabled Boolean @default(false)`, `logoData Bytes?`, `logoMimeType String?` (logo stored as PNG bytes on the row per RESEARCH Open-Question 3 resolution — reuses the Postgres persistent volume, no new infra); `name String`; a pruning-resistant `lifetimeScans Int @default(0)` counter documented exactly like `Link.lifetimeClicks` (incremented ONLY by the /q scan hook in 07-06, never a live COUNT); `createdBy String?`; `createdAt DateTime @default(now())`; `updatedAt DateTime @updatedAt`. Relation: `link Link @relation(fields: [linkId], references: [id], onDelete: Cascade)` and add the inverse `qrCodes QrCode[]` on Link. Add explicit `@@index([linkId])` and `@@index([code])` (never rely on the FK/unique alone — the file's own ClickEvent comment states this convention). Add `model QrRemapHistory` mirroring ClickEvent's per-event-row shape: `id @id @default(cuid())`, `qrCodeId String`, `fromLinkId String`, `toLinkId String`, `createdAt DateTime @default(now())`, relation `qrCode QrCode @relation(fields: [qrCodeId], references: [id], onDelete: Cascade)` with inverse `remapHistory QrRemapHistory[]` on QrCode, plus `@@index([qrCodeId])` and `@@index([createdAt])`. Write a doc-comment header on QrCode naming `apps/api/src/lib/qrCodes.ts` as the sole write path (mirrors the Link model's header). Then run the project's migration command (`pnpm --filter @kurzly/api prisma migrate dev --name add_qr_codes`) and `prisma generate` against the generated client path.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec prisma validate && pnpm --filter @kurzly/api exec prisma generate</automated>
  </verify>
  <done>schema.prisma has QrCode + QrRemapHistory models + QrCodeVariant enum with explicit indexes and cascade rules; a new migration exists under apps/api/prisma/migrations; prisma validate + generate succeed and the generated client exposes qrCode + qrRemapHistory delegates.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: sharp image-pipeline smoke test (de-risk A4/A5)</name>
  <files>apps/api/test/qrRenderSmoke.test.ts</files>
  <behavior>
    - Test 1: sharp renders a trivial inline SVG string (a single colored rect) to a PNG buffer via `sharp(Buffer.from(svg)).png().toBuffer()` without throwing (confirms bundled SVG rasterization works on this platform — Assumption A5).
    - Test 2: `sharp(pngBuffer).ensureAlpha().raw().toBuffer({ resolveWithObject: true })` resolves to an object whose `info.width`/`info.height` match the SVG viewBox and whose `data` length equals width*height*4 (confirms the exact RGBA shape the 07-03 decode test feeds jsQR — Assumption A4).
  </behavior>
  <action>Write a Vitest test (no DB, no testcontainer needed) that exercises the two sharp calls the decode-round-trip test will rely on. Keep the SVG a tiny hand-written string so the test is fast and deterministic. This is a pure smoke test — its only job is to fail loudly here (in Wave 2) rather than deep inside the 07-03 renderer if sharp's SVG/raw pipeline is unavailable on the host.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api test -- test/qrRenderSmoke.test.ts</automated>
  </verify>
  <done>Both smoke assertions pass; if sharp lacks SVG rasterization on this host the test fails with a clear message pointing at the Dockerfile/librsvg follow-up (A5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Newly installed native `sharp` binary executes at build/test time |
| Prisma schema → Postgres | New tables/columns change the persisted data shape |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-SC | Tampering | qrcode/sharp/jsqr installs | high | mitigate | Gated by 07-01's blocking-human sign-off; this plan only installs the approved set |
| T-07-SCHEMA | Tampering | QrCode.code / lifetimeScans columns | medium | mitigate | `code` is a plain `@unique` server-generated column; `lifetimeScans` is documented as counter-only (never client-settable) — enforced by the Zod allowlist in 07-05, seeded here as a server-owned default |
| T-07-LOGO-BLOB | Denial of Service | QrCode.logoData Bytes | low | accept | Row-bloat bounded by the 2 MB upload cap (enforced in 07-05); reuses the existing Postgres volume, acceptable per RESEARCH Open-Question 3 |
</threat_model>

<verification>
- `prisma validate` + `prisma generate` succeed; migration applies cleanly.
- Full API suite still green after the schema change: `pnpm --filter @kurzly/api test`.
</verification>

<success_criteria>
- Approved deps installed; QrCode + QrRemapHistory persistable; sharp SVG→PNG→raw pipeline confirmed on this host.
</success_criteria>

<output>
Create `.planning/phases/07-qr-codes-static-dynamic-qr-studio/07-02-SUMMARY.md` when done.
</output>
