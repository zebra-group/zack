---
phase: 07-qr-codes-static-dynamic-qr-studio
plan: 08
type: execute
wave: 6
depends_on: [07-05, 07-07]
files_modified:
  - apps/web/src/components/QrStudioPanel.vue
  - apps/web/src/views/QrCodesView.vue
  - apps/web/src/components/QrStudioPanel.test.ts
autonomous: true
requirements: [QR-01, QR-05, QR-06]
user_setup: []

must_haves:
  truths:
    - "The Studio panel shows a live server-rendered preview of the selected QR on a white card, updating on every control change via a 300ms-debounced render call"
    - "Choosing one of the four product colors and toggling rounded modules restyles the QR (persisted via updateQrCode) and refreshes the preview"
    - "Toggling the logo on / uploading a PNG or SVG logo (max 2MB, validated) shows the centered overlay; export stays scannable (server forces level H)"
    - "PNG ⬇ and SVG ⬇ trigger a real file download of the current code"
  artifacts:
    - apps/web/src/components/QrStudioPanel.vue
    - apps/web/src/components/QrStudioPanel.test.ts
  key_links:
    - "Studio panel binds to the selected QR from QrCodesView and calls updateQrCode + the render endpoints from api.ts (07-07)"
    - "logo upload sends validated base64 to updateQrCode; server rasterizes + forces EC-H"
    - "export downloads pull bytes from the server render endpoints (no client redraw)"
---

## Phase Goal

**As a** dashboard user, **I want to** create static and dynamic QR codes for my short links — with a centered logo, custom color and rounded-module styling, re-pointable dynamic targets that keep a printed code valid, and a visible scan count — **so that** I can print scannable codes that keep working even after I change where they point.

> Derived from the ROADMAP Phase 7 `**Goal:**` line into user-story shape (MVP framing). This slice adds the styling + export capability (QR-01/QR-05/QR-06) to the list surface from 07-07.

<objective>
Build Surface A's Studio column (`QrStudioPanel.vue`) and wire it into QrCodesView: a live preview with debounced server rendering, the color/rounded/logo controls, logo upload with validation, and real PNG/SVG downloads — all to the locked UI-SPEC. This completes the QR Studio: styling (QR-06), logo overlay (QR-05), and export (QR-01).

Purpose: The user-visible styling+export capability, rendered exclusively server-side (CONTEXT single-code-path lock) so the preview and the exported file are byte-identical geometry.
Output: QrStudioPanel.vue, its wiring into QrCodesView, and component tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-qr-codes-static-dynamic-qr-studio/07-UI-SPEC.md
@apps/web/src/views/QrCodesView.vue
@apps/web/src/api.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Studio preview + color/rounded/logo controls with debounced render</name>
  <files>apps/web/src/components/QrStudioPanel.vue, apps/web/src/views/QrCodesView.vue</files>
  <read_first>
    - .planning/phases/07-qr-codes-static-dynamic-qr-studio/07-UI-SPEC.md (Studio panel 360px/18px; preview frame + white preview card ALWAYS; 196px preview; controls block; 4 product-color swatches #17170f/#1e3a5f/#14532d/#7c2d4f; 38x21 toggles; "opacity:.6 while reloading, no skeleton" rule)
  </read_first>
  <behavior>
    - Panel receives the selected QR (prop) and renders a live preview <img> from the server render.png endpoint on a white card (always #fff, even dark mode).
    - Color swatches (exactly the four locked product colors) and the rounded-module toggle update the QR via updateQrCode; a 300ms-debounced render call refreshes the preview; the previous image stays visible at opacity .6 while the new one loads (no skeleton/spinner).
    - Logo toggle enables/disables the centered overlay via updateQrCode.
    - The active swatch shows the accent border; active toggles use the accent background (locked binary-toggle exception).
  </behavior>
  <action>Create QrStudioPanel.vue (`<script setup>`, scoped CSS, tokens.css only, exact LOCKED values) and embed it into QrCodesView's studio slot (from 07-07), bound to the selected card. All control changes persist via updateQrCode (api.ts) then trigger a 300ms-debounced re-fetch of the server render — never a client-side QR redraw. Keep the preview card white in both themes (the single documented theme exception). Emit an event so QrCodesView refreshes the matching list thumbnail after a style change. Commit as `feat(07-08): QR Studio preview + color/rounded/logo controls with debounced server render`.</action>
  <verify>
    <automated>pnpm --filter @kurzly/web exec vue-tsc --noEmit</automated>
  </verify>
  <done>Studio panel renders the selected QR preview on a white card; color/rounded/logo controls persist + debounce-refresh the preview at opacity .6; wired into QrCodesView; typecheck clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Logo upload dropzone (validated) + PNG/SVG export downloads</name>
  <files>apps/web/src/components/QrStudioPanel.vue</files>
  <read_first>
    - .planning/phases/07-qr-codes-static-dynamic-qr-studio/07-UI-SPEC.md (dropzone accept image/png,image/svg+xml, max 2MB; inline errors "Nur PNG oder SVG erlaubt." / "Datei zu groß (max. 2 MB)." / generic; filename chip + "Logo entfernen"; export buttons "PNG ⬇"/"SVG ⬇" real download; "Export fehlgeschlagen" toast; default overlay = BRAND_NAME initial)
  </read_first>
  <behavior>
    - Dropzone accepts PNG/SVG only, rejects other types inline ("Nur PNG oder SVG erlaubt.") and >2MB inline ("Datei zu groß (max. 2 MB)."); a generic failure shows "Logo-Upload fehlgeschlagen…".
    - A valid file is read to base64 and sent via updateQrCode; on success the dropzone becomes a filename chip + "Logo entfernen" link, and the logo toggle auto-enables.
    - With the logo toggle on but no custom upload, the overlay shows the BRAND_NAME initial (accent tile).
    - "PNG ⬇" / "SVG ⬇" fetch the respective render endpoint and trigger a browser file download; a failed export toasts "Export fehlgeschlagen. Bitte erneut versuchen." (no success toast — the download is the feedback).
  </behavior>
  <action>Add the logo dropzone + export row to QrStudioPanel.vue per the locked UI-SPEC. Validate type + size client-side for fast inline feedback, but rely on the server (07-05) as the real gate — surface server rejections via mapQrFormError. Send the logo as base64 through updateQrCode; the server normalizes/rasterizes + forces EC-H. Implement real downloads by fetching the render endpoints and saving the blob. Commit as `feat(07-08): validated logo upload + real PNG/SVG export downloads`.</action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/components/QrStudioPanel.test.ts</automated>
  </verify>
  <done>Invalid/oversized logos rejected inline; valid logo uploads + auto-enables the toggle + shows filename chip; default overlay uses BRAND_NAME initial; PNG/SVG buttons download real files; export failure toasts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: QrStudioPanel component tests</name>
  <files>apps/web/src/components/QrStudioPanel.test.ts</files>
  <read_first>
    - apps/web/src/components/LinkFormModal.test.ts (prop-driven mount + emit assertions + vi.mock('../api') shape)
  </read_first>
  <behavior>
    - Color/rounded/logo control changes call updateQrCode and schedule a debounced render (assert the debounced call fires once after the window).
    - Logo upload of a fake >2MB file shows the size error and does NOT call updateQrCode; a valid file calls updateQrCode with base64.
    - Export button click fetches the correct render endpoint and initiates a download.
  </behavior>
  <action>Write QrStudioPanel.test.ts with @vue/test-utils, mocking api.ts and timers for the 300ms debounce. Assert control→updateQrCode wiring, logo validation branches, and export-fetch behavior. Commit as `test(07-08): QR Studio panel control/upload/export tests`.</action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/components/QrStudioPanel.test.ts</automated>
  </verify>
  <done>Control, logo-validation, and export behaviors are covered and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Studio panel → /api/qr-codes* | Style updates + logo bytes (base64) + render/export requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-LOGO-MIME | Tampering | logo upload | high | mitigate | Client validation is UX-only; the server (07-05) re-validates size + magic bytes + rasterizes SVG→PNG before storage — client checks never trusted as the gate |
| T-07-DOS-RENDER | Denial of Service | debounced preview requests | medium | mitigate | 300ms client debounce + the dedicated server QR_RENDER_RATE_LIMIT bucket (07-05) |
| T-07-EXPORT-XSS | Tampering | rendered SVG download | low | mitigate | SVG bytes come from the server's own module-matrix renderer (not user markup); uploaded SVG logos are rasterized server-side, never echoed into the export SVG as markup |
</threat_model>

<verification>
- `pnpm --filter @kurzly/web test -- src/components/QrStudioPanel.test.ts` green; `vue-tsc --noEmit` clean.
- No client-side QR-module drawing anywhere (preview + export both come from server render endpoints).
- Visual fidelity to the locked UI-SPEC confirmed at end-of-phase human verification.
</verification>

<success_criteria>
- Users can style (color + rounded), add a validated centered logo, live-preview, and export scannable PNG/SVG — all server-rendered.
</success_criteria>

<output>
Create `.planning/phases/07-qr-codes-static-dynamic-qr-studio/07-08-SUMMARY.md` when done.
</output>
