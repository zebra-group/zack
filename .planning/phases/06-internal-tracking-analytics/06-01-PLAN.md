---
phase: 06-internal-tracking-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - pnpm-workspace.yaml
  - apps/api/src/env.ts
  - .env.example
autonomous: false
requirements: [TRACK-03]
user_setup: []

must_haves:
  truths:
    - "`maxmind` (^5.0.6) is a resolvable dependency of @kurzly/api after install"
    - "`GEOIP_DB_PATH` and `CLICK_RETENTION_DAYS` are optional keys on the Zod env schema"
    - ".env.example documents the two new keys so the drift guard passes"
  artifacts:
    - apps/api/package.json (maxmind dependency)
    - apps/api/src/env.ts (GEOIP_DB_PATH, CLICK_RETENTION_DAYS)
    - .env.example (two new documented keys)
  key_links:
    - "env-example-drift.test.ts couples envSchema keys ↔ .env.example — both must move together"
  prohibitions:
    - "No maxmind postinstall/lifecycle script is auto-approved without an explicit pnpm-workspace allowBuilds entry"
    - "Neither new env key is made required (both optional) — a fresh instance must still boot with zero tracking config"
---

<objective>
Land the single new runtime dependency this phase needs (`maxmind`, the local `.mmdb` GeoIP reader for TRACK-03 country resolution) behind the project's established operator supply-chain sign-off gate (T-0X-SC convention, Phases 2/4/5), and add the two optional operator env keys the phase introduces: `GEOIP_DB_PATH` (D-03 override of the bundled DB) and `CLICK_RETENTION_DAYS` (D-12 retention window).

Purpose: give downstream plans (06-03 GeoIP helper, 06-04 retention) their dependency + config surface without any code that consumes them yet — a clean, verifiable foundation.
Output: `maxmind` installed, two optional env keys documented and drift-clean.

## Phase Goal (user story, derived from ROADMAP §Phase 6 goal)
**As a** link owner, **I want to** get privacy-first internal click analytics per link and account-wide with a true zero-rows-when-off guarantee, **so that** I understand my links' reach without any third-party tracking. (This plan is the enabling supply-chain + config foundation for that capability.)
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-internal-tracking-analytics/06-RESEARCH.md
@apps/api/src/env.ts
@.env.example
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Operator supply-chain sign-off for maxmind (T-06-SC-Gate)</name>
  <files>(none — approval gate, no file writes)</files>
  <action>Present the `maxmind@^5.0.6` supply-chain evidence to the operator and BLOCK on explicit approval before any install runs, matching the project's T-02-SC / T-04-SC / T-05-SC convention (STATE.md decisions log). Do not run `pnpm add` until approval is given.</action>
  <what-built>No code yet — this is the blocking supply-chain gate before the first install of this phase. The package to approve is `maxmind@^5.0.6` (MIT, github.com/runk/node-maxmind, ~757k weekly downloads, no postinstall script). RESEARCH.md's Package Legitimacy Audit records the verdict `OK` / `[VERIFIED: npm registry]`.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/maxmind and confirm: MIT license, active maintenance, source repo github.com/runk/node-maxmind, weekly download count in the hundreds-of-thousands range.
    2. Confirm no install-time lifecycle (postinstall) script is declared.
    3. This is the ONLY new runtime dependency in Phase 6 — hashing and referrer parsing use Node builtins (`node:crypto`, `URL`); no other package is being added.
  </how-to-verify>
  <verify>Operator types "approved" (or names a package to re-investigate) — this is a blocking-human gate, never auto-approved.</verify>
  <resume-signal>Type "approved" to authorize the `maxmind` install, or name any package you want re-investigated.</resume-signal>
  <done>Operator has explicitly approved the maxmind install (or the plan is halted pending re-investigation).</done>
</task>

<task type="auto">
  <name>Task 2: Install maxmind + add GEOIP_DB_PATH / CLICK_RETENTION_DAYS env keys</name>
  <files>apps/api/package.json, pnpm-lock.yaml, pnpm-workspace.yaml, apps/api/src/env.ts, .env.example</files>
  <read_first>
    - apps/api/src/env.ts (envSchema shape; follow the exact optional-key + doc-comment convention used for BRAND_NAME / PASSWORD_HASH_COST / CNAME_TARGET)
    - .env.example (existing documented key layout — the drift guard parses `KEY=` lines)
    - apps/api/test/env-example-drift.test.ts (asserts envSchema keys ↔ .env.example exactly, minus COMPOSE_ONLY_KEYS)
    - pnpm-workspace.yaml (allowBuilds gate — STATE.md notes csv-parse/nanoid needed no entry; confirm maxmind likewise ships no build script)
  </read_first>
  <action>
    Install with `pnpm --filter @kurzly/api add maxmind` (resolves to ^5.0.6). Do NOT add a `pnpm-workspace.yaml` allowBuilds entry unless the install surfaces an ignored build-script warning for maxmind or a new transitive dep — verify first, matching the Phase-4 csv-parse/nanoid decision. Add two OPTIONAL keys to `envSchema` in env.ts, each with a doc-comment mirroring the existing optional-key style: `GEOIP_DB_PATH` as `z.string().min(1).optional()` (D-03 — operator override path for a bind-mounted `.mmdb`; when unset, lib/geoip.ts falls back to the build-baked `/prod/api/geo/dbip-country-lite.mmdb`), and `CLICK_RETENTION_DAYS` as `z.coerce.number().int().positive().optional()` (D-12 — when unset, raw ClickEvent rows are never pruned; when set, retention deletes events older than N days). Document both keys in `.env.example` (commented, showing they are optional and their default-behavior-when-unset) so the drift guard stays green. Neither key gets a `.default()` — absence must mean "feature off", not a silent default.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec node -e "require.resolve('maxmind'); console.log('maxmind resolved')" && pnpm --filter @kurzly/api test -- test/env-example-drift.test.ts test/env.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `require.resolve('maxmind')` succeeds from the @kurzly/api workspace
    - `pnpm --filter @kurzly/api test -- test/env-example-drift.test.ts` passes (both new keys documented, no drift)
    - `envSchema.shape` includes `GEOIP_DB_PATH` and `CLICK_RETENTION_DAYS`, both optional with no `.default()`
    - `pnpm --filter @kurzly/api exec tsc --noEmit` passes
  </acceptance_criteria>
  <done>maxmind is installed and resolvable; both optional env keys exist on the schema and are documented in .env.example; the drift guard and env tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | A newly installed package's code runs in the API image and, transitively, on the redirect hot path |
| operator → env config | `GEOIP_DB_PATH` is operator-supplied filesystem input (deployment-time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-SC | Tampering | maxmind install | high | mitigate | Blocking operator sign-off (Task 1) + RESEARCH legitimacy audit verdict OK; no auto-approved lifecycle script (no allowBuilds entry unless verified needed) |
| T-06-CFG | Tampering | GEOIP_DB_PATH env | low | accept | Operator-controlled deployment setting, same trust level as DATABASE_URL — not attacker-reachable input (RESEARCH Security Domain) |
</threat_model>

## Artifacts this plan produces
- **Dependency:** `maxmind@^5.0.6` on `@kurzly/api`
- **Env vars:** `GEOIP_DB_PATH` (optional string), `CLICK_RETENTION_DAYS` (optional positive int) on `envSchema` (apps/api/src/env.ts)
- **Docs:** two new documented keys in `.env.example`

<verification>
- `pnpm --filter @kurzly/api test -- test/env-example-drift.test.ts test/env.test.ts` green
- `require.resolve('maxmind')` succeeds
- `pnpm --filter @kurzly/api exec tsc --noEmit` green
</verification>

<success_criteria>
maxmind is approved + installed; the two optional env keys exist and are drift-clean; nothing consumes them yet (pure foundation).
</success_criteria>

<output>
Create `.planning/phases/06-internal-tracking-analytics/06-01-SUMMARY.md` when done.
</output>
