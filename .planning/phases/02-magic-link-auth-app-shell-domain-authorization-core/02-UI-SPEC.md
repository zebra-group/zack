---
phase: 02
slug: magic-link-auth-app-shell-domain-authorization-core
status: draft
shadcn_initialized: false
preset: none
created: 2026-07-11
---

# Phase 02 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

**Deckt ab:** Login-Seite (Magic Link, Idle/Sent), Magic-Link-Fehler-Statusseite, App-Shell (Sidebar + Content), Theme-Toggle, "Coming soon"-Platzhalter für Links/QR-Codes/Analytics/Domains/Team, Dashboard-Landingscreen.

**Nicht in diesem Contract:** Vollständige Feature-Screens (Links-Liste, QR-Studio, Analytics, Domains, Team-Tabelle) — diese kommen in ihren eigenen Phasen (3/4/6/7/9) mit eigenen UI-SPECs. Dieser Contract legt nur die App-Shell fest, in der sie später andocken, plus deren "Coming soon"-Zustand.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — kein shadcn/shadcn-vue init |
| Preset | not applicable |
| Component library | none — handgeschriebene Vue 3 `<script setup>`-SFCs + scoped CSS mit CSS Custom Properties fürs Theming (spiegelt den Inline-Style-Ansatz des Prototyps); kein Tailwind, kein Radix |
| Icon library | none — Unicode/Emoji-Glyphen als Inline-Icon-Zeichen gemäß Prototyp (`✉` `⏻` `⚠`); keine Icon-Font/SVG-Library |
| Font | **Geist** (400/500/600/700, UI-Text) + **Geist Mono** (400/500/600, Code/URLs/E-Mail-Inputs/Zahlen) — Google Fonts: `https://fonts.googleapis.com/css2?family=Geist:wght@400;500;600;700&family=Geist+Mono:wght@400;500;600&display=swap` |

**Begründung "Tool: none":** `components.json` fehlt, `apps/web/package.json` enthält kein Tailwind/shadcn. Der Design-Handoff (`Kurzly Prototyp.dc.html`) ist ein vollständig gesperrtes, eigenständiges Inline-CSS-System mit CSS-Custom-Properties fürs Light/Dark-Theming — keine Tailwind-Utility-Klassen, keine Radix-Data-Attribute. shadcns offizielle Tooling zielt auf React/Radix und würde dem bereits gesperrten Token-System eher entgegenarbeiten als helfen. UI-03 verlangt Pixelgenauigkeit zum Prototyp — die richtige Umsetzung ist eine 1:1-Nachbildung des bestehenden Custom-CSS-Ansatzes (scoped `<style>` pro Component + globale CSS-Variablen `--bg`/`--panel`/`--accent`/… über `body[data-theme]`), nicht die Einführung einer neuen Library. **Registry-Safety-Gate entfällt entsprechend (keine shadcn-Registry im Spiel).**

**Theme-Mechanismus (LOCKED, aus Prototyp extrahiert):**
```css
body { --bg:#f7f7f5; --panel:#ffffff; --border:#e6e6e2; --text:#1b1b18; --mut:#8b8b84; --hover:#f0f0ec; --chip:#f1f1ed; --accent:#d7ff01; --ok:#3a9e5f; color-scheme:light; }
body[data-theme="dark"] { --bg:#121211; --panel:#1a1a19; --border:#2b2b29; --text:#f1f1ec; --mut:#8f8f87; --hover:#232321; --chip:#242422; --ok:#5fc98a; color-scheme:dark; }
```
Theme wird per `data-theme="dark"`-Attribut auf `<body>` (bzw. root-Element) gesetzt/entfernt, in `localStorage` (Schlüssel z. B. `kurzly-theme`) persistiert und beim App-Boot vor dem ersten Paint angewandt (kein Flash of Unstyled Theme). Pinia-Store `theme` (aus CONTEXT.md Claude's Discretion) hält den State, ein `watch` schreibt das Attribut + `localStorage`.

---

## Spacing Scale

**Abweichung vom generischen 8pt-Default:** Das Design-Handoff schreibt UI-03/D-04 Pixelgenauigkeit zum Prototyp vor. Der Prototyp nutzt ein **4px-Basisraster mit zahlreichen Fein-Werten** (nicht strikt 8pt) — diese exakten Werte sind LOCKED und ersetzen die generische Skala für alle Phase-2-Elemente. Erfindet keine gerundeten 8pt-Werte, wo der Prototyp einen Fein-Wert vorgibt.

| Token | Value | Usage (Phase 2) |
|-------|-------|-------|
| 2xs | 2px | Switch-Knob-Inset |
| xs | 4–6px | Icon-Gaps, Nav-Item-Gap (2px), Logo-Zeile-Gap (9px) |
| sm | 7–9px | Nav-Item-Padding (7px 10px), Theme-Toggle-Padding (7px 10px), User-Row-Gap (9px) |
| md | 10–14px | User-Row-Padding (8px 10px), Auth-Card-Gap (14px), Footer-Block-Padding (12px) |
| lg | 16–18px | Nav-List-Padding (6px 10px), Logo-Zeile-Padding (18px 16px 14px), Screen-Gap (18px) |
| xl | 22–26px | Auth-Card-Padding (26px 24px / 30px 26px / 34px 26px) |
| 2xl | 28–36px | Content-Padding (`28px 36px 48px`) |
| 3xl | 48px | Content-Padding-Bottom |

Exceptions (exakte, aus dem Prototyp übernommene Werte, NICHT auf 4/8 runden):
- Sidebar-Breite: **212px** (fix, UI-01)
- Content max-width: **1060px** (Dashboard/Coming-soon-Screens), zentriert (`margin:0 auto`)
- Content-Padding: **`28px 36px 48px`**
- Auth-Card-Breite: **360px** (Login, Magic-Link-Fehlerseite)
- Sidebar-Logo-Mark: **22×22px**, Auth-Logo-Mark: **30×30px**
- Theme-Switch: **30×16px** Track, **12px** Knob, Inset **2px**/**16px** (light/dark)
- User-Avatar: **26×26px**
- Error-/Status-Icon-Kachel: **52×52px**, `border-radius:14px`

---

## Typography

Basistabelle (Template-Pflichtfelder):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 13.5px | 400 | 1.5 |
| Label | 12.5px | 400 | 1.4 |
| Heading | 16px | 600 | 1.2 |
| Display | 21px | 600 | 1.2 |

**Line-Heights sind Claude-Empfehlung (im Prototyp nicht explizit spezifiziert außer DNS-Codeblock 1.7); Größen/Gewichte/Letter-Spacing sind LOCKED aus dem Prototyp.**

Da das Prototyp deutlich mehr als 4 Größen/2 Gewichte definiert (UI-03 verlangt Pixelgenauigkeit statt Vereinfachung), zusätzlich die vollständige **Phase-2-Komponenten-Typografietabelle**:

| Element | Size | Weight | Font | Notes |
|---------|------|--------|------|-------|
| Sidebar Brand-Name | 15px | 600 | Geist | `letter-spacing:-0.01em` |
| Sidebar Logo-Initiale | 13px | 700 | Geist Mono | auf `--accent`-Kachel, Farbe `#1b1b18` |
| Sidebar Nav-Item | 13.5px | 600 aktiv / 400 inaktiv | Geist | aktiv: Farbe `--text`; inaktiv: `--mut` |
| Sidebar Theme-Toggle-Label | 12.5px | 400 | Geist | `--mut` |
| Sidebar Versionstext | 11px | 400 | Geist | `--mut`, Text „v0.1.0 · self-hosted" |
| Sidebar User-Name | 12.5px | 500 | Geist | ellipsis |
| Sidebar User-Rolle | 11px | 400 | Geist | `--mut`, ellipsis |
| Dashboard Seitentitel | 21px | 600 | Geist | `letter-spacing:-0.02em` |
| "Coming soon" Screentitel | 21px | 600 | Geist | identisch zu Seitentitel (echte Nav-Screens fühlen sich final an) |
| "Coming soon" Karten-Heading | 13.5px | 600 | Geist | in dashed Empty-State-Karte |
| "Coming soon" Karten-Body | 12.5px | 400 | Geist | `--mut` |
| Auth-Karte Titel ("Anmelden"/"Link gesendet") | 16px / 15px | 600 | Geist | Idle-State 16px, Sent-State 15px |
| Auth-Karte Body | 12.5px | 400 | Geist | `--mut` |
| E-Mail-Input (Login) | 13.5px | 400 | **Geist Mono** | Placeholder „du@firma.de" |
| Primär-Button (voll breit, Auth) | 13.5px | 600 | Geist | `#1b1b18` auf `--accent` |
| Fehlerseite Titel | 17px | 600 | Geist | z. B. „Dieser Link ist ungültig oder abgelaufen" |
| Fehlerseite Body | 12.5px | 400 | Geist | `--mut` |
| Fehlerseite URL-Chip | 11.5px | 400 | Geist Mono | `--mut`, auf `--chip`-Pille |
| Footer-Mikrotext (Auth) | 11px | 400 | Geist | „Auth via better-auth · self-hosted" |

---

## Color

Template-Pflichtfelder (60/30/10-Framing):

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | Light `#f7f7f5` / Dark `#121211` (`--bg`) | App-Hintergrund, Content-Fläche |
| Secondary (30%) | Light `#ffffff` / Dark `#1a1a19` (`--panel`) | Sidebar, Karten, Modals, Auth-Card |
| Accent (10%) | `#d7ff01` (`--accent`, beide Modi identisch) | siehe „Accent reserved for" unten |
| Destructive | `#e5484d` (statischer Hex, beide Modi identisch — kein Theme-Variable im Prototyp) | Fehlerzustände (falsches Passwort, Input-Border bei Validierungsfehler) |

Accent reserved for (explizit, Phase 2 Scope):
- Sidebar-Logo-Kachel-Hintergrund (Initiale)
- Auth-Logo-Kachel-Hintergrund (Login-/Fehlerseite)
- Primär-Button-Hintergrund („Magic Link senden", „Neuen Link anfordern")
- Aktiver Theme-Switch-Track (Dark-Mode aktiv)
- Sent-State-Icon-Kreis (`✉`) auf der Login-Karte
- Unlocked-State-Icon-Kreis (`✓`) — nur relevant, falls Password-Page als Referenzmuster mitgebaut wird (nicht Phase-2-Scope, nur zur Konsistenz erwähnt)

**Explizit NICHT-Accent** in Phase 2: Sidebar-Nav-Items (aktiv = `--chip`-Hintergrund, NICHT Akzent — Akzent ist für Primäraktionen reserviert, nicht für Navigation), Coming-soon-Icon-Kacheln (`--chip`-Hintergrund, muted).

Vollständige Token-Tabelle (Light/Dark, aus Prototyp, LOCKED):

| Token | Light | Dark | Usage |
|-------|-------|------|-------|
| `--bg` | `#f7f7f5` | `#121211` | App-/Seiten-Hintergrund |
| `--panel` | `#ffffff` | `#1a1a19` | Sidebar, Karten, Auth-Card |
| `--border` | `#e6e6e2` | `#2b2b29` | 1px-Trennlinien, Karten-Border, Input-Border |
| `--text` | `#1b1b18` | `#f1f1ec` | Primärtext |
| `--mut` | `#8b8b84` | `#8f8f87` | Sekundärtext, Labels, Platzhalter |
| `--hover` | `#f0f0ec` | `#232321` | Hover-Hintergrund (Nav, Buttons, Zeilen) |
| `--chip` | `#f1f1ed` | `#242422` | Badge-/Pillen-Hintergrund, aktiver Nav-Item-Hintergrund, Icon-Kacheln |
| `--accent` | `#d7ff01` | `#d7ff01` | siehe oben — Text auf Akzent immer `#1b1b18` |
| `--ok` | `#3a9e5f` | `#5fc98a` | SSO-Status-Punkt (falls OIDC aktiv sichtbar); kein Nutzung sonst in Phase 2 |
| Error (statisch) | `#e5484d` | `#e5484d` | Validierungsfehler, Input-Border bei Fehler |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Login, Idle-State) | „Magic Link senden" |
| Primary CTA (Fehlerseite) | „Neuen Link anfordern" (führt zurück zur Login-Idle-Ansicht) |
| Login Idle — Titel | „Anmelden" |
| Login Idle — Body | „Wir senden dir einen Magic Link an deine E-Mail — kein Passwort nötig." |
| Login Idle — E-Mail-Placeholder | „du@firma.de" |
| Login Sent — Titel | „Link gesendet" |
| Login Sent — Body | „Prüfe **{email}** — der Link ist 15 Minuten gültig." (E-Mail in Geist Mono hervorgehoben; **diese Meldung ist bewusst neutral und erscheint identisch, ob die E-Mail berechtigt ist oder nicht — kein Account-Enumeration-Leak, D-01**) |
| Login Sent — Link zurück | „← andere E-Mail verwenden" |
| Auth-Footer (auf Login- und Fehlerseite) | „Auth via better-auth · self-hosted" |
| Fehlerseite (abgelaufen/benutzt/ungültig) — Titel | „Dieser Link ist ungültig oder abgelaufen" |
| Fehlerseite — Body | „Magic Links sind nur 15 Minuten gültig und können nur einmal verwendet werden. Fordere einfach einen neuen an." |
| Fehlerseite — Icon | `⚠` in 52px-Icon-Kachel (`--chip`-Hintergrund, `border-radius:14px`, `font-size:24px`), analog zum Muster der Passwort-/Ablauf-Seiten |
| Empty state heading ("Coming soon", je Nav-Item) | „{Screen-Name} — bald verfügbar" (z. B. „QR-Codes — bald verfügbar") |
| Empty state body ("Coming soon") | „Dieser Bereich ist noch nicht freigeschaltet. Er kommt in einer der nächsten Phasen." |
| Dashboard-Landing — Titel | „Übersicht" (Seitentitel, 21px/600, Nav-Item-Label „Dashboard") |
| Dashboard-Landing — Welcome-Body | „Willkommen bei Kurzly. Deine Übersicht füllt sich, sobald du Links, Domains und QR-Codes anlegst." (kein Dashed-Empty-State — warmer Ton, da echte Landing-Erfahrung, nicht „fehlend") |
| Error state (allgemein, z. B. Netzwerkfehler beim Senden) | „Der Magic Link konnte nicht gesendet werden. Bitte versuche es erneut." + Retry via erneutem Klick auf „Magic Link senden" (kein separater Fehlerscreen — Inline-Meldung unter dem Formular, `color:#e5484d`, 11.5px, analog Passwort-Fehler-Pattern) |
| Rate-Limit-Fehler (D-07, `@fastify/rate-limit` greift) | „Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst." (gleiche Inline-Fehler-Stelle wie oben) |
| Logout | Icon-Only `⏻` in Sidebar-Footer-User-Zeile, Tooltip/`title`-Attribut „Abmelden" — **kein Bestätigungsdialog** (Logout ist keine destruktive/irreversible Aktion; 1-Klick, analog Prototyp-Verhalten) |
| Destructive confirmation | Keine im Phase-2-Scope (kein Lösch-/Delete-Flow; Logout erfordert bewusst keine Bestätigung — siehe oben) |

**Nav-Item-Labels (App-Shell, vollständige Liste gemäß D-03):** „Dashboard" (NEU, nicht im Prototyp-Nav-Array enthalten — als Phase-2-Ergänzung eingeführt, um D-03 „Landing nach Login = Dashboard/Übersicht" zu erfüllen; siehe Hinweis unten), „Links", „QR-Codes", „Analytics", „Domains", „Team". Reihenfolge: Dashboard zuerst (Landing-Route `/`), danach die 5 Prototyp-Items in Originalreihenfolge.

> **Hinweis (Claude's Discretion, nicht im Prototyp explizit):** Das Prototyp-Nav-Array enthält nur `Links, QR-Codes, Analytics, Domains, Team` (README Zeile 41 nennt sogar nur 4, README Zeile 36 nennt 5 inkl. Team — beide ohne „Dashboard"). CONTEXT.md D-03 verlangt jedoch explizit „Landing nach Login = Dashboard/Übersicht". Da dieser Screen im Handoff nicht existiert, wird er hier als 6. Nav-Item ergänzt (gleiche visuelle Behandlung wie andere Nav-Items: `padding:7px 10px;border-radius:7px;font-size:13.5px`). Planner/Executor sollen dies als Phase-2-spezifische Ergänzung behandeln, nicht als Korrektur am Prototyp.

---

## App-Shell — Layout Contract (LOCKED, UI-01)

```
Root:    display:flex; height:100vh; background:var(--bg); color:var(--text);
         font-family:'Geist',system-ui,sans-serif; overflow:hidden

Sidebar: width:212px; flex:none; display:flex; flex-direction:column;
         border-right:1px solid var(--border); background:var(--panel)

  Logo-Zeile:   display:flex; align-items:center; gap:9px; padding:18px 16px 14px
    Logo-Mark:  22×22px; border-radius:6px; background:var(--accent);
                display:flex; align-items:center; justify-content:center;
                color:#1b1b18; font-weight:700; font-size:13px (Initiale)
    Brand-Name: font-size:15px; font-weight:600; letter-spacing:-.01em

  Nav-Liste:    display:flex; flex-direction:column; gap:2px; padding:6px 10px
    Nav-Item:   padding:7px 10px; border-radius:7px; font-size:13.5px; cursor:pointer
      aktiv:    background:var(--chip); color:var(--text); font-weight:600
      inaktiv:  background:transparent; color:var(--mut); font-weight:400
      hover:    background:var(--hover)

  Footer:       margin-top:auto; padding:12px; display:flex; flex-direction:column; gap:8px
    Theme-Toggle-Zeile: display:flex; align-items:center; justify-content:space-between;
                padding:7px 10px; border:1px solid var(--border); border-radius:8px;
                font-size:12.5px; color:var(--mut); cursor:pointer; hover: background:var(--hover)
      Switch-Track: 30×16px; border-radius:999px;
                background: dark ? var(--accent) : var(--border); transition:background .15s
      Switch-Knob:  12×12px; border-radius:50%; background:var(--panel);
                position:absolute; top:2px; left: dark ? 16px : 2px; transition:left .15s;
                box-shadow:0 1px 2px rgba(0,0,0,.3)
      Label-Text:   dark aktiv → "Light Mode" (Toggle-Ziel anzeigen); light aktiv → "Dark Mode"
    Versionstext: font-size:11px; color:var(--mut); padding:0 4px; Text "v0.1.0 · self-hosted"
    User-Zeile:   display:flex; align-items:center; gap:9px; padding:8px 10px;
                border:1px solid var(--border); border-radius:8px
      Avatar:     26×26px; border-radius:50%; background:var(--chip);
                display:flex; align-items:center; justify-content:center;
                font-size:11px; font-weight:600; color:var(--mut) (Initialen)
      Name:       font-size:12.5px; font-weight:500; ellipsis
      Rolle:      font-size:11px; color:var(--mut); ellipsis
      Logout:     Icon "⏻"; cursor:pointer; color:var(--mut); padding:3px;
                hover: color:var(--text); title="Abmelden"

Content: flex:1; overflow-y:auto; min-width:0
  Screen-Container (Dashboard + jedes Coming-soon-Nav-Ziel):
                max-width:1060px; margin:0 auto; padding:28px 36px 48px;
                display:flex; flex-direction:column; gap:18px
  Screen-Header: display:flex; align-items:center; gap:14px
    Titel:      font-size:21px; font-weight:600; letter-spacing:-.02em
```

**Coming-soon-Placeholder-Karte** (LOCKED-Pattern aus Prototyp, Zeile 176-180, Tracking-Off-Empty-State — wiederverwendet):
```
border:1px dashed var(--border); border-radius:12px; padding:40px;
text-align:center; background:var(--panel)
  Icon (optional, 24px, darüber, margin-bottom ~10px)
  Heading: 13.5px/600, color:var(--text)
  Body:    12.5px/400, color:var(--mut), margin-top:4px
```

---

## Login-Seite & Fehlerseite — Layout Contract (LOCKED, aus Prototyp)

**Gemeinsames Muster (Login Idle/Sent + Magic-Link-Fehlerseite):**
```
Root:    position:fixed (bzw. eigene Route, kein Overlay in Produktion); inset:0;
         background:var(--bg); display:flex; align-items:center; justify-content:center; padding:24px

Wrapper: width:360px; max-width:100%; display:flex; flex-direction:column; gap:18px

  Brand-Zeile (zentriert, oberhalb der Karte):
         display:flex; align-items:center; gap:10px; justify-content:center
    Logo-Mark: 30×30px; border-radius:8px; background:var(--accent);
         color:#1b1b18; font-weight:700; font-size:16px (Initiale)
    Brand-Name: font-size:19px; font-weight:600; letter-spacing:-.01em

  Card:    background:var(--panel); border:1px solid var(--border); border-radius:16px;
         padding:26px 24px; display:flex; flex-direction:column; gap:14px;
         box-shadow:0 10px 40px rgba(0,0,0,.12)   ← Auth-/Public-Card-Schatten,
         NICHT der Modal-Schatten (0 20px 60px rgba(0,0,0,.3)) — eigener, leichterer Wert

  Footer-Mikrotext: font-size:11px; color:var(--mut); text-align:center
         Text „Auth via better-auth · self-hosted"
```

**Login Idle-Zustand (innerhalb Card):**
```
Titel:    font-size:16px; font-weight:600 — „Anmelden"
Body:     font-size:12.5px; color:var(--mut) — Erklärtext
E-Mail-Input: padding:11px 13px; border:1px solid var(--border); border-radius:9px;
         background:var(--bg); color:var(--text); font-size:13.5px; outline:none;
         font-family:'Geist Mono',monospace; placeholder="du@firma.de"
Primär-Button: padding:11px 0 (volle Breite); border:none; border-radius:9px;
         background:var(--accent); color:#1b1b18; font-size:13.5px; font-weight:600;
         cursor:pointer; hover: opacity:.85 — „Magic Link senden"
Inline-Fehler (bei Sende-/Rate-Limit-Fehler): font-size:11.5px; color:#e5484d; margin-top:-6px
```

**Login Sent-Zustand (innerhalb Card, ersetzt Idle):**
```
display:flex; flex-direction:column; gap:10px; align-items:center; text-align:center; padding:8px 0
  Icon-Kreis: 44×44px; border-radius:50%; background:var(--accent);
         display:flex; align-items:center; justify-content:center;
         font-size:20px; color:#1b1b18 — Zeichen „✉"
  Titel:    font-size:15px; font-weight:600 — „Link gesendet"
  Body:     font-size:12.5px; color:var(--mut) —
         „Prüfe {E-Mail in Geist Mono, color:var(--text)} — der Link ist 15 Minuten gültig."
  Zurück-Link: font-size:12px; color:var(--mut); cursor:pointer; hover: color:var(--text);
         margin-top:2px — „← andere E-Mail verwenden"
```

**Magic-Link-Fehlerseite (NEU, D-05 — abgeleitet aus dem Ablauf-/404-Seiten-Muster, nicht 1:1 im Prototyp vorhanden):**
```
Card (statt Login-Idle/Sent): padding:34px 26px; align-items:center; text-align:center;
         gap:14px (identisches Card-Shell wie oben, gleicher Schatten/Radius/Border)
  Icon-Kachel: 52×52px; border-radius:14px; background:var(--chip);
         display:flex; align-items:center; justify-content:center;
         font-size:24px — Zeichen „⚠"
  Titel:    font-size:17px; font-weight:600 — „Dieser Link ist ungültig oder abgelaufen"
  Body:     font-size:12.5px; color:var(--mut) —
         „Magic Links sind nur 15 Minuten gültig und können nur einmal verwendet werden.
         Fordere einfach einen neuen an."
  Primär-Button: identisch zu Login-Primär-Button (padding:11px 0, radius:9px, voll breit) —
         „Neuen Link anfordern" → navigiert zurück zur Login-Idle-Ansicht (Route, kein Overlay-Close)
```

**Wichtig (Security, aus PROJECT.md/CLAUDE.md-Constraint):** Die Fehlerseite darf zu keinem Zeitpunkt preisgeben, *warum* der Link ungültig ist (abgelaufen vs. bereits benutzt vs. nie existent) — immer dieselbe generische Meldung, kein Ziel-/Account-Leak. Serverseitige Antwort auf einen ungültigen/abgelaufenen/benutzten Token muss identisch aussehen (kein Timing-/Statusunterschied, der Enumeration ermöglicht).

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|--------------|
| shadcn official | keine | not applicable — kein shadcn-Init in diesem Projekt |
| third-party | keine | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS (not applicable — kein shadcn)

**Approval:** pending
