---
phase: 13-authentication-session-e2e
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/e2e/src/oidc-mock.ts
  - apps/e2e/src/users.ts
  - apps/e2e/playwright.config.ts
autonomous: true
requirements: [AUTH-E2E-01, AUTH-E2E-02, AUTH-E2E-05, AUTH-E2E-07]
user_setup: []

must_haves:
  truths:
    - "A spec can call setOidcProfile({sub,email,extraClaims}) and resetOidcProfile() against the host-published mock IdP"
    - "A spec can create an allowlisted, verified User row and an invited-but-unverified (emailVerified:false, no Account) User row via typed Prisma helpers"
    - "A standalone `auth` Playwright project runs tests/auth/**.spec.ts with NO dependencies:[\"setup\"] — these specs prove login itself and must not depend on the setup project's own magic-link login"
  artifacts:
    - apps/e2e/src/oidc-mock.ts
    - apps/e2e/src/users.ts
    - apps/e2e/playwright.config.ts
  key_links:
    - "oidc-mock.ts's control URL (OIDC_MOCK_CONTROL_URL, default http://localhost:9000) reaches the same test-control routes server.mjs (13-01) exposes"
    - "users.ts's invited-unverified row shape (emailVerified:false, accountRole default, no Account) mirrors lib/team.ts inviteMember's DB write exactly, so AUTH-E2E-05's fixture reproduces a real invited account"
    - "the `auth` project's testMatch tests/auth/** picks up every spec file this phase's Wave 1/2 plans create"
---

<objective>
Build the reusable E2E harness pieces every AUTH-E2E spec consumes: a thin typed mock-IdP control client (mirroring apps/e2e/src/mailpit.ts's shape), a Prisma fixture helper for allowlisted and invited-unverified User rows (mirroring apps/e2e/src/db.ts / links.ts's raw-Prisma convention), and a standalone `auth` Playwright project that runs the phase's specs WITHOUT the `setup` project dependency — because these specs prove login itself and must not implicitly depend on the exact mechanism half of them test the failure modes of (CONTEXT.md discretion note; D-01 invite-only, D-09-04 invite shape).

Purpose: Give the Wave 1/2 spec plans a stable fixture + control vocabulary so no spec hand-rolls raw fetch/SQL.
Output: `apps/e2e/src/oidc-mock.ts`, `apps/e2e/src/users.ts`, and the `auth` project registration.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentication-session-e2e/13-RESEARCH.md
@apps/e2e/src/mailpit.ts
@apps/e2e/src/db.ts
@apps/api/src/lib/team.ts
@apps/e2e/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: apps/e2e/src/oidc-mock.ts — typed mock-IdP control client</name>
  <read_first>
    - apps/e2e/src/mailpit.ts (the shape to mirror: a thin fetch wrapper over an env-configured base URL, clear thrown errors, no ad hoc scattered fetches)
    - .planning/phases/13-authentication-session-e2e/13-01-PLAN.md (Task 2 — the exact test-control route contract this client must match: PUT/DELETE /__test__/profile with {sub,email,emailVerified?,extraClaims?})
  </read_first>
  <files>apps/e2e/src/oidc-mock.ts</files>
  <action>
    Read the control base URL from `process.env.OIDC_MOCK_CONTROL_URL ?? "http://localhost:9000"` (scripts/e2e-compose.sh exports it in 13-01). Export a typed `OidcProfile` interface `{ sub: string; email: string; emailVerified?: boolean; extraClaims?: Record<string, unknown> }`.

    Export `setOidcProfile(profile: OidcProfile): Promise<void>` — `PUT ${base}/__test__/profile` with a JSON body of the profile; throw a clear error if the response status is not 204. Export `resetOidcProfile(): Promise<void>` — `DELETE ${base}/__test__/profile`; throw on non-204.

    This client's route paths and body shape MUST stay identical to 13-01's server.mjs test-control routes — they are two ends of one contract. Keep it dependency-free (native fetch), matching mailpit.ts's style. Do NOT scatter raw fetch calls in spec files; every mock-claim mutation goes through this module.
  </action>
  <verify>
    <automated>cd apps/e2e && pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/oidc-mock.ts','utf8'); process.exit(/setOidcProfile/.test(s)&&/resetOidcProfile/.test(s)&&/__test__\/profile/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes for the e2e workspace
    - exports `setOidcProfile` and `resetOidcProfile`
    - targets `/__test__/profile` under OIDC_MOCK_CONTROL_URL (default http://localhost:9000)
    - throws a descriptive error on any non-204 response
  </acceptance_criteria>
  <done>Specs can programmatically control the next SSO subject/email/claims through one typed client.</done>
</task>

<task type="auto">
  <name>Task 2: apps/e2e/src/users.ts — allowlisted + invited-unverified User fixtures</name>
  <read_first>
    - apps/e2e/src/db.ts (createE2ePrisma, seedBaseline, the raw-Prisma-consumer convention, the @kurzly/api/prisma-client import, ADMIN_EMAIL/MEMBER_EMAIL naming)
    - apps/api/src/lib/team.ts (inviteMember: a new invited User row is `emailVerified: false`, accountRole from input, name = email local part, and NO Account row — reproduce this exact shape)
    - apps/e2e/src/links.ts (createE2eLink — the established `createE2e*` helper style + fetchWithFixtureRaceRetry for any fixture read-back race)
  </read_first>
  <files>apps/e2e/src/users.ts</files>
  <action>
    Add typed Prisma fixture helpers that write directly through the reused @kurzly/api/prisma-client (never raw SQL, never a second driver — same discipline as db.ts). Accept a Prisma client / transaction client argument like db.ts's helpers.

    Export `createAllowlistedUser(prisma, opts: { email: string; accountRole?: "admin" | "member" })` — upserts a User row with `emailVerified: true` and the given accountRole (default "member"), so the email passes lib/allowlist.ts's existence check (the allowlist IS the User table) and can complete a real magic-link round trip. Use randomUUID() for the id and the email local part as the name, matching admin-seed/inviteMember conventions.

    Export `createInvitedUnverifiedUser(prisma, opts: { email: string; accountRole?: "admin" | "member" })` — creates a User row exactly as lib/team.ts's inviteMember does for a NEW invitee: `emailVerified: false`, accountRole default "member", name = email local part, and NO Account row. This is AUTH-E2E-05's precondition (an invited-but-never-activated account). Return the created row.

    Both helpers must generate unique, caller-supplied emails so specs never collide with the seeded ADMIN_EMAIL/MEMBER_EMAIL (whose magic-link login is owned by the setup project) or with each other. Do not truncate/reseed here — that is db.ts's withResetDbLock's job; these are additive fixture writers. Note: withResetDbLock never truncates the User table, so rows created here persist for the spec's duration.
  </action>
  <verify>
    <automated>cd apps/e2e && pnpm exec tsc --noEmit && node -e "const s=require('fs').readFileSync('src/users.ts','utf8'); process.exit(/createAllowlistedUser/.test(s)&&/createInvitedUnverifiedUser/.test(s)&&/emailVerified/.test(s)?0:1)"</automated>
  </verify>
  <acceptance_criteria>
    - `tsc --noEmit` passes
    - exports `createAllowlistedUser` (emailVerified true) and `createInvitedUnverifiedUser` (emailVerified false, no Account row)
    - both write through @kurzly/api/prisma-client, not raw SQL
    - invited-unverified shape matches lib/team.ts inviteMember (name = email local part, accountRole default member)
  </acceptance_criteria>
  <done>Specs can seed the two User states this phase needs: a login-capable allowlisted user and an invited-but-unactivated user.</done>
</task>

<task type="auto">
  <name>Task 3: Register a standalone `auth` Playwright project (no dependencies:["setup"])</name>
  <read_first>
    - apps/e2e/playwright.config.ts (the existing projects array: smoke, setup, chromium-admin, chromium-member; the testMatch scoping conventions; why smoke depends on setup)
    - .planning/phases/13-authentication-session-e2e/13-VALIDATION.md (Test Infrastructure: a new standalone `auth` project, no setup dependency)
  </read_first>
  <files>apps/e2e/playwright.config.ts</files>
  <action>
    Add a new project `{ name: "auth", testMatch: /auth\/.*\.spec\.ts$/ }` scoped to `tests/auth/**`. It MUST NOT declare `dependencies: ["setup"]` and MUST NOT set `use.storageState` — every spec in this project establishes (or deliberately fails to establish) its own session, because these specs ARE the proof of login itself (CONTEXT.md discretion note; RESEARCH Recommended Project Structure). Leave `use.baseURL` inherited from the top-level `use` (http://localhost:3000).

    Do not change the existing smoke/setup/chromium-admin/chromium-member projects. Confirm the new `auth` testMatch does not also capture tests/authed/** or tests/smoke/** (scope strictly to the `auth/` directory segment). Because fullyParallel is true at the top level, note in a short comment that the SSO specs (tests/auth/sso.spec.ts, added in 13-07/13-08) serialize their own mock-claim access via test.describe.serial so they never race the mock IdP's single global profile state, while the magic-link specs remain freely parallel.
  </action>
  <verify>
    <automated>cd apps/e2e && pnpm exec tsc --noEmit && pnpm exec playwright test --list --project=auth >/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `playwright test --list --project=auth` resolves the project (no "unknown project" error)
    - the `auth` project declares no setup-project dependency and no saved-session state (verify by manual review of the project block)
    - testMatch scopes strictly to tests/auth/** (does not capture authed/ or smoke/)
    - existing projects are unchanged
  </acceptance_criteria>
  <done>The phase's specs have a home project that proves login from a cold, unauthenticated state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → mock control :9000 | claim-control requests (test-only) |
| test runner → E2E Postgres :5433 | direct fixture writes (User rows) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-03 | Elevation of Privilege | users.ts createInvitedUnverifiedUser | low | mitigate | Helper always defaults accountRole to "member" and never writes an Account row; admin rows require an explicit caller opt-in, matching seedBaseline's least-privilege discipline (T-11-03) |
| T-13-04 | Information Disclosure | `auth` project running without a session | low | accept | The project runs unauthenticated by design; specs assert the ABSENCE of a session on negative paths, they never expose protected data |
</threat_model>

<verification>
- `pnpm --filter @kurzly/e2e typecheck` passes with the three new/edited files.
- `pnpm --filter @kurzly/e2e exec playwright test --list --project=auth` lists the (initially empty) project without error.
</verification>

<success_criteria>
The mock-IdP control client, the User fixtures, and the standalone `auth` project all typecheck and are ready for the Wave 1/2 specs to consume.
</success_criteria>

<artifacts_produced>
- `apps/e2e/src/oidc-mock.ts` — setOidcProfile/resetOidcProfile control client
- `apps/e2e/src/users.ts` — createAllowlistedUser / createInvitedUnverifiedUser fixtures
- `apps/e2e/playwright.config.ts` (modified) — standalone `auth` project, no setup dependency
</artifacts_produced>

<output>
Create `.planning/phases/13-authentication-session-e2e/13-02-SUMMARY.md` when done.
</output>
