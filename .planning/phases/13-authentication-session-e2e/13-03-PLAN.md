---
phase: 13-authentication-session-e2e
plan: 03
type: execute
wave: 1
depends_on: [13-02]
files_modified:
  - apps/e2e/tests/auth/magic-link-round-trip.spec.ts
  - apps/e2e/tests/auth/magic-link-token-rejection.spec.ts
autonomous: true
requirements: [AUTH-E2E-01, AUTH-E2E-02]

must_haves:
  truths:
    - "Requesting a magic link for an allowlisted email, reading it from Mailpit, and opening it lands an active, server-verified session (GET /api/auth/get-session returns the user)"
    - "A magic-link token that has already been consumed cannot be reused — a second verify attempt is rejected and creates no session"
    - "An expired magic-link token (past expiresAt) is rejected and creates no session"
    - "A malformed/tampered magic-link token is rejected and creates no session"
  artifacts:
    - apps/e2e/tests/auth/magic-link-round-trip.spec.ts
    - apps/e2e/tests/auth/magic-link-token-rejection.spec.ts
  key_links:
    - "the magic-link request sends the x-e2e-bypass header (like auth.setup.ts) so this spec never contends the shared IP rate-limit bucket that AUTH-E2E-07 deliberately trips"
    - "every negative case independently re-confirms no session via GET /api/auth/get-session (RESEARCH Pitfall 3 — never 'an error appeared' alone)"
---

<objective>
Prove the magic-link token lifecycle end-to-end against the built image: the happy round trip reaches an active session (AUTH-E2E-01), and consumed / expired / malformed tokens are each rejected with NO session created (AUTH-E2E-02). Phase 11's auth.setup.ts already proves the happy path implicitly to snapshot storageState; this phase asserts session state EXPLICITLY and adds the three negative token cases that only better-auth's own verify endpoint (the code under test — no app change) enforces.

Purpose: Close AUTH-E2E-01/02 with explicit session assertions, honoring D-01's invite-only allowlist and the 15-minute (`expiresIn: 900`) magic-link window from lib/auth.ts.
Output: Two spec files under the standalone `auth` project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentication-session-e2e/13-RESEARCH.md
@apps/e2e/tests/auth.setup.ts
@apps/e2e/src/mailpit.ts
@apps/e2e/src/db.ts
@apps/e2e/src/users.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH-E2E-01 — magic-link round trip reaches an active session</name>
  <read_first>
    - apps/e2e/tests/auth.setup.ts (the confirmed round-trip mechanics: POST /api/auth/sign-in/magic-link with x-e2e-bypass, findMagicLinkUrl recipient-scoped retrieval, page.goto(link), waiting for the "Dashboard" nav to render)
    - apps/e2e/src/mailpit.ts (findMagicLinkUrl signature + the recipient-scoped anti-theft guarantee)
    - apps/e2e/src/users.ts (createAllowlistedUser)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME)
  </read_first>
  <files>apps/e2e/tests/auth/magic-link-round-trip.spec.ts</files>
  <action>
    Import test/expect from @playwright/test. In a beforeAll (or the test body), createE2ePrisma() and createAllowlistedUser(prisma, { email }) with a dedicated, never-reused email such as `roundtrip-<short-random>@e2e.kurzly.local` (so it never collides with the seeded ADMIN_EMAIL/MEMBER_EMAIL owned by the setup project, nor with AUTH-E2E-07's rate-limit email). Disconnect the client after seeding.

    In the test: POST /api/auth/sign-in/magic-link via `request` with body `{ email, callbackURL: "/", errorCallbackURL: "/auth/error" }` AND the `x-e2e-bypass` header set to process.env.E2E_RATE_LIMIT_BYPASS_SECRET (mirror auth.setup.ts exactly — this keeps the spec off the shared IP rate-limit bucket AUTH-E2E-07 relies on). Assert the response is ok().

    findMagicLinkUrl(email) → page.goto(magicLinkUrl) → wait for `page.getByRole("link", { name: "Dashboard" })` to be visible (the authenticated App Shell rendered). THEN independently prove the session server-side: `page.request.get("/api/auth/get-session")` returns 200 with a body whose `user.email === email`. Assert both the UI render AND the get-session user — the session is a server-verified artifact, the UI is only its reflection (T-02-13).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/auth/magic-link-round-trip.spec.ts --project=auth</automated>
  </verify>
  <acceptance_criteria>
    - the spec requests the link with the x-e2e-bypass header and a dedicated allowlisted email
    - it asserts the "Dashboard" nav renders after opening the link
    - it asserts GET /api/auth/get-session returns the same email (server-verified session)
    - the spec passes green against the built compose image
  </acceptance_criteria>
  <done>AUTH-E2E-01 proves request→Mailpit→open link→active, server-verified session.</done>
</task>

<task type="auto">
  <name>Task 2: AUTH-E2E-02 — consumed / expired / malformed token each rejected, no session</name>
  <read_first>
    - apps/e2e/src/mailpit.ts (MAGIC_LINK_URL_PATTERN — the verify URL shape /api/auth/magic-link/verify?token=...; extract the token query param to build reuse/expiry attempts)
    - apps/e2e/src/db.ts (createE2ePrisma — used to expire the better-auth Verification row directly)
    - apps/e2e/src/users.ts (createAllowlistedUser)
    - .planning/phases/13-authentication-session-e2e/13-RESEARCH.md (Pitfall 3 — every negative case must re-confirm no session via GET /api/auth/get-session, not merely "an error page appeared")
  </read_first>
  <files>apps/e2e/tests/auth/magic-link-token-rejection.spec.ts</files>
  <action>
    Seed a dedicated allowlisted email `token-reject-<short-random>@e2e.kurzly.local` via createAllowlistedUser. All magic-link requests here send the `x-e2e-bypass` header (stay off the shared rate-limit bucket).

    Three cases, each ending with an explicit GET /api/auth/get-session assertion of UNAUTHENTICATED state (fresh browser context / request context with no prior session), per RESEARCH Pitfall 3:

    (a) Consumed-token reuse: request a link, extract the token from the verify URL. Consume it once (open the link in a first context → session established). Then in a SECOND, fresh context with no cookies, GET the SAME `/api/auth/magic-link/verify?token=<same>&callbackURL=/&errorCallbackURL=/auth/error`. Assert it does NOT establish a session: the final URL is the /auth/error screen (or a non-dashboard route) and `context.request.get("/api/auth/get-session")` returns no user.

    (b) Expired token: request a fresh link and extract its token. Using createE2ePrisma(), set the corresponding better-auth `verification` row's `expiresAt` to a past timestamp (identify the freshest verification row for this request — e.g. order by createdAt desc, or match the identifier better-auth writes for a magic-link token; inspect the Verification model to confirm the column names before writing). Then, in a fresh context, follow the verify URL and assert rejection + no session via get-session. This exercises the `expiresIn: 900` rejection path deterministically without a 15-minute wait.

    (c) Malformed/tampered token: in a fresh context, GET `/api/auth/magic-link/verify?token=not-a-real-token-deadbeef&callbackURL=/&errorCallbackURL=/auth/error`. Assert rejection (routed to /auth/error, no dashboard) and get-session returns no user.

    Disconnect any Prisma client in afterAll. Do not assert on the initial POST response shape (D-01 makes it neutral) — assert on session absence.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/auth/magic-link-token-rejection.spec.ts --project=auth</automated>
  </verify>
  <acceptance_criteria>
    - three cases present: consumed-token reuse, DB-expired token, malformed token
    - each case ends with an explicit GET /api/auth/get-session assertion of no user (not just "an error page rendered")
    - the expiry case manipulates the better-auth verification row's expiresAt via Prisma (no real 15-minute wait)
    - the spec passes green against the built compose image
  </acceptance_criteria>
  <done>AUTH-E2E-02 proves consumed, expired, and malformed tokens are all rejected with zero session created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → app /api/auth/* | untrusted magic-link token crosses here |
| test runner → E2E Postgres :5433 | direct verification-row expiry manipulation (test-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-05 | Spoofing | reused/forged magic-link token → session | high | mitigate | AUTH-E2E-02 proves better-auth rejects consumed/expired/malformed tokens; each case re-verifies session ABSENCE server-side (get-session), closing the "error UI but session still issued" gap (Pitfall 3) |
| T-13-06 | Denial of Service (masking) | rate-limit bypass header leaking into a real 429 spec | low | accept | These specs deliberately use the bypass header to stay OFF the limiter bucket; the real-429 assertion is isolated to AUTH-E2E-07 (13-06), which never sends the bypass |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/auth/magic-link-round-trip.spec.ts tests/auth/magic-link-token-rejection.spec.ts --project=auth` runs both green.
- Re-run once at `--workers=1` and once at default parallelism to confirm no DB-isolation/rate-limit collision this pair introduces.
</verification>

<success_criteria>
AUTH-E2E-01 and AUTH-E2E-02 pass against the built image: the happy round trip yields a server-verified session, and all three bad-token cases yield none.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/auth/magic-link-round-trip.spec.ts` — AUTH-E2E-01
- `apps/e2e/tests/auth/magic-link-token-rejection.spec.ts` — AUTH-E2E-02 (consumed/expired/malformed)
</artifacts_produced>

<output>
Create `.planning/phases/13-authentication-session-e2e/13-03-SUMMARY.md` when done.
</output>
