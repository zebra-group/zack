---
phase: 13-authentication-session-e2e
plan: 07
type: execute
wave: 1
depends_on: [13-01, 13-02]
files_modified:
  - apps/e2e/tests/auth/sso.spec.ts
autonomous: true
requirements: [AUTH-E2E-04]

must_haves:
  truths:
    - "Clicking 'Mit SSO anmelden' drives a full OIDC authorization-code round trip against the mock IdP and lands an active session on the dashboard"
    - "A first-time SSO user is provisioned least-privilege: accountRole 'member' with zero DomainMembership rows, EVEN when the IdP returns admin-shaped claims (role/groups/admin)"
  artifacts:
    - apps/e2e/tests/auth/sso.spec.ts
  key_links:
    - "the mock's discovery authorization_endpoint (host localhost:9000) is browser-reachable while token/userinfo (oidc-mock:9000) stay server-to-server — the dual-reachability the whole mock design exists for"
    - "auth.ts registers genericOAuth with no mapProfileToUser + accountRole input:false (D-10-04), so IdP claims can never elevate the provisioned user"
    - "this test lives in a test.describe.serial block so it never races AUTH-E2E-05 (13-08) over the mock's single global profile state"
---

<objective>
Prove the OIDC/SSO round trip through a real browser against the mock IdP (AUTH-E2E-04): a first-time SSO user logs in and is provisioned least-privilege ("Mitglied") with zero domain memberships, even when the IdP feeds admin-shaped claims. The existing Vitest sso-auth.integration.test.ts already proves the claim-blocking logic server-side via a hand stub; this phase drives the SAME guarantee through a real browser and a real (mock) IdP over the actual authorization-code redirect dance — honoring D-10-01 (issuer-only discovery), D-10-03 (genericOAuth registered from env), D-10-04 (no claim-to-privilege mapping), D-10-06 (fixed `oidc` callback path).

This plan CREATES apps/e2e/tests/auth/sso.spec.ts inside a `test.describe.serial` block; 13-08 appends the AUTH-E2E-05 merge test to the same serial block so the two SSO tests never race the mock's single global profile state.

Purpose: Close AUTH-E2E-04 through a real browser SSO flow.
Output: The SSO spec file with the AUTH-E2E-04 test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentication-session-e2e/13-RESEARCH.md
@apps/web/src/views/LoginView.vue
@apps/api/src/lib/auth.ts
@apps/api/test/sso-auth.integration.test.ts
@apps/e2e/src/oidc-mock.ts
@apps/e2e/src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH-E2E-04 — browser SSO round trip provisions least-privilege even against admin claims</name>
  <read_first>
    - apps/web/src/views/LoginView.vue (signInWithSso: POST /api/auth/sign-in/oauth2 with { providerId: "oidc", callbackURL: "/", errorCallbackURL: "/auth/error" }, then window.location.assign(data.url); the "Mit SSO anmelden" button appears only when getSsoStatus().enabled)
    - apps/api/test/sso-auth.integration.test.ts (the AUTH-07 least-privilege assertion pattern: provisioned user accountRole "member", zero DomainMemberships, admin-shaped extraClaims role/groups/admin never elevate)
    - apps/api/src/lib/auth.ts (genericOAuth config: no mapProfileToUser, accountRole input:false — the enforcement point, D-10-04)
    - apps/e2e/src/oidc-mock.ts (setOidcProfile / resetOidcProfile — the control client)
    - apps/e2e/src/db.ts (createE2ePrisma for the post-login DB assertions)
    - .planning/phases/13-authentication-session-e2e/13-RESEARCH.md (Architecture Patterns Pattern 1 — the dual-reachability flow the browser exercises)
  </read_first>
  <files>apps/e2e/tests/auth/sso.spec.ts</files>
  <action>
    Create the file with a single top-level `test.describe.serial("SSO login (AUTH-E2E-04/05)", () => { ... })` block. Add a `test.afterEach` that calls resetOidcProfile() so no residual profile leaks between this test and the AUTH-E2E-05 test 13-08 appends.

    Inside, write the AUTH-E2E-04 test:
    1. setOidcProfile({ sub: `sso-04-<short-random>`, email: `sso-04-<short-random>@idp.test`, extraClaims: { role: "admin", groups: ["admins", "owners"], admin: true } }) — feed admin-shaped claims to prove they are ignored. Use a unique sub AND email so this test never collides with AUTH-E2E-05's subject/email or its Account(providerId,accountId) uniqueness.
    2. page.goto("/login"); assert the "Mit SSO anmelden" button is visible (OIDC is enabled in the E2E overlay). Click it. The browser is redirected to the mock's authorization_endpoint on localhost:9000, auto-approved, and redirected back through /api/auth/oauth2/callback/oidc to the dashboard.
    3. Wait for the "Dashboard" nav to render (authenticated). Also assert page.request.get("/api/auth/get-session") returns the SSO user's email.
    4. Least-privilege assertions via createE2ePrisma(): the provisioned User row for that email has accountRole === "member" (NOT "admin", despite the admin-shaped claims), and prisma.domainMembership.findMany({ where: { userId } }) is empty. Disconnect the client.

    Do not hand-create the User row — better-auth's own provisioning must run. If the SSO redirect is slow, wait on the dashboard nav / a get-session poll rather than a fixed timeout.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/auth/sso.spec.ts --project=auth</automated>
  </verify>
  <acceptance_criteria>
    - the file opens with test.describe.serial and an afterEach resetOidcProfile()
    - the AUTH-E2E-04 test feeds admin-shaped claims via setOidcProfile and drives the real "Mit SSO anmelden" browser flow
    - it asserts the provisioned user is accountRole "member" with zero DomainMemberships (least-privilege, no claim elevation)
    - it asserts an active session via get-session
    - passes green against the built compose image with oidc-mock up
  </acceptance_criteria>
  <done>AUTH-E2E-04 proves a browser SSO round trip provisions least-privilege even against admin-shaped IdP claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → mock authorization_endpoint :9000 | browser-driven OAuth authorization hop |
| app container → mock token/userinfo :9000 | server-to-server code exchange |
| IdP claims → provisioned user privilege | untrusted claim data crosses into provisioning |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-13 | Elevation of Privilege | admin-shaped IdP claims elevating an SSO user | high | mitigate | AUTH-E2E-04 drives real admin claims through the browser flow and asserts the user still lands accountRole "member" with zero memberships (D-10-04, input:false) |
| T-13-14 | Spoofing | mock authorization hop reachable/rewritten incorrectly | low | accept | The dual-reachability rewrite is validated by the round trip completing; the mock is E2E-network-only, not attacker-controlled |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/auth/sso.spec.ts --project=auth` passes with the oidc-mock service healthy.
- The provisioned user is member with zero memberships despite admin-shaped claims.
</verification>

<success_criteria>
AUTH-E2E-04 passes: a real browser SSO round trip logs in a first-time user least-privilege, ignoring admin claims.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/auth/sso.spec.ts` — created with the AUTH-E2E-04 test inside a test.describe.serial block (13-08 appends AUTH-E2E-05)
</artifacts_produced>

<output>
Create `.planning/phases/13-authentication-session-e2e/13-07-SUMMARY.md` when done.
</output>
