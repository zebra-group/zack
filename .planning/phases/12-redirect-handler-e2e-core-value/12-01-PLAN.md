---
phase: 12-redirect-handler-e2e-core-value
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/smoke/host-header.spike.spec.ts
autonomous: true
requirements:
  - REDIRECT-E2E-01
  - REDIRECT-E2E-02
  - REDIRECT-E2E-03
  - REDIRECT-E2E-04
  - REDIRECT-E2E-05
must_haves:
  truths:
    - "A Playwright APIRequestContext request carrying an explicit `Host: e2e.kurzly.local` header reaches Fastify's `request.hostname` unmodified, driving `resolveActiveDomainByHost` to the real registered Domain (not the CR-07 SPA-fallback branch)."
  artifacts:
    - "apps/e2e/tests/smoke/host-header.spike.spec.ts — throwaway empirical proof of RESEARCH Open Question 1 / Assumption A1, mirroring tests/smoke/prisma-import.spike.spec.ts."
  key_links:
    - "Caller-supplied `host` header on request.get(...) → Fastify request.hostname → resolveActiveDomainByHost → branded 404 (domain resolves, slug does not) vs. localhost → CR-07 SPA fallback (200)."
---

<objective>
Empirically prove (RESEARCH Open Question 1 / Assumption A1) that Playwright's `APIRequestContext` delivers a caller-supplied `Host` header unmodified to the built app's Fastify instance, so every feature spec in this phase can target the real registered redirect Domain `e2e.kurzly.local` over the wire instead of hitting the CR-07 app-own-host SPA-fallback branch.

This is the mandatory Wave 0 gate from 12-CONTEXT.md ("Host header on requests") and 12-VALIDATION.md (Wave 0 Requirements). It writes ZERO application code and must PASS before any feature spec (12-03/12-04/12-05) depends on Host-header targeting. If it fails, the fallback is Node's raw `http.request` (documented in 12-RESEARCH.md Open Question 1) — surface that outcome rather than proceeding.

Purpose: Remove the single load-bearing unknown for this phase (Playwright Host-header behavior against Fastify) with an execution proof, not documentation inference.
Output: `apps/e2e/tests/smoke/host-header.spike.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-redirect-handler-e2e-core-value/12-RESEARCH.md
@apps/e2e/tests/smoke/prisma-import.spike.spec.ts
@apps/e2e/src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write host-header spike proving APIRequestContext delivers a caller-supplied Host header to Fastify</name>
  <files>apps/e2e/tests/smoke/host-header.spike.spec.ts</files>
  <read_first>
    - apps/e2e/tests/smoke/prisma-import.spike.spec.ts — the exact throwaway-spike file shape and header-comment convention to mirror.
    - apps/e2e/src/db.ts — import `BASELINE_DOMAIN_HOSTNAME` (value `e2e.kurzly.local`); do NOT re-seed a Domain (global-setup.ts already seeds it).
    - apps/api/src/routes/redirect.ts — the `GET /:slug` handler order: `resolveActiveDomainByHost` → if `!domain` and `isAppOwnHost` → `reply.callNotFound()` (SPA fallback); else if `!link` → `renderNotFoundPage` (branded 404). This is why a resolved-domain-but-missing-slug request yields a branded 404 and a localhost request yields the SPA.
    - apps/api/src/lib/publicHtml.ts `renderNotFoundPage` — the branded 404 body contains the literal `Dieser Kurzlink existiert nicht` and echoes the requested host in a `url-chip` (`${safeDomain}/${safeSlug}`).
  </read_first>
  <action>
    Create `apps/e2e/tests/smoke/host-header.spike.spec.ts` in the existing `smoke` Playwright project (matches `testMatch: /smoke\/.*\.spec\.ts$/`, no config change). Use the `request` fixture (APIRequestContext), not `page`.

    Define local const `BROWSER_UA` = the exact real Chrome UA string used by apps/api/test/redirect.integration.test.ts (`Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/128.0 Safari/537.36`) so the request never accidentally trips the isbot branch. Import `BASELINE_DOMAIN_HOSTNAME` from `../../src/db.js`.

    Positive assertion (load-bearing): issue `request.get("/spike-nonexistent-<randomUUID()>", { headers: { host: BASELINE_DOMAIN_HOSTNAME, "user-agent": BROWSER_UA }, maxRedirects: 0 })`. Assert `response.status()` === 404 AND `(await response.text())` contains `Dieser Kurzlink existiert nicht` AND contains `e2e.kurzly.local`. This can ONLY occur if the Host header reached `resolveActiveDomainByHost` and resolved the registered Domain, then found no such slug — proving A1.

    Control assertion (secondary evidence): issue the same request WITHOUT a `host` header override (defaulting to the config baseURL host `localhost`). Assert the body does NOT contain `Dieser Kurzlink existiert nicht` (CR-07 `isAppOwnHost(localhost)` → `reply.callNotFound()` → SPA index shell, never the branded redirect 404). Keep this assertion tolerant of the exact status the SPA fallback returns; only assert the branded-404 marker is ABSENT.

    Do NOT instantiate a Prisma client and do NOT create any Link — the spike relies solely on the already-seeded baseline Domain plus a guaranteed-missing slug. Add a header comment matching prisma-import.spike.spec.ts stating this is the RESEARCH OQ-1/A1 empirical proof and is throwaway.

    Do NOT place fenced code blocks with literal UA/marker strings elsewhere; the spec file is the only place these literals live.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/host-header.spike.spec.ts</automated>
  </verify>
  <done>host-header.spike.spec.ts passes: a `Host: e2e.kurzly.local` request yields a branded 404 echoing the host; a default (localhost) request does not — proving Playwright APIRequestContext delivers the caller-supplied Host header to Fastify.</done>
  <acceptance_criteria>
    - `apps/e2e/tests/smoke/host-header.spike.spec.ts` exists and runs green under the booted compose stack via `./scripts/e2e-compose.sh tests/smoke/host-header.spike.spec.ts`.
    - The positive case asserts status 404 with a branded-404 body echoing `e2e.kurzly.local`, proving the caller-supplied Host header reached Fastify.
    - The control case asserts the branded-404 marker is absent for a default (localhost) host, proving the difference is Host-header-driven.
    - No Prisma client instantiated, no Link created, no application source under `apps/api/src` modified.
    - If the positive assertion FAILS (Host header stripped/ignored), the summary records the failure and recommends the raw `http.request` fallback per 12-RESEARCH.md Open Question 1 — feature specs are NOT written against a broken mechanism.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright client → built app (real HTTP) | The `Host` header is attacker-controllable in production; correct host-scoped Domain resolution is what keeps one customer's slugs from resolving on another's domain. This spike proves the E2E harness exercises that real boundary rather than a false-passing self-host branch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-HOST | Spoofing | Host-header → `resolveActiveDomainByHost` targeting in the E2E harness | high | mitigate | The spike asserts a branded-404-on-registered-host vs. SPA-fallback-on-localhost delta, so downstream specs cannot silently pass against the CR-07 SPA branch (false-negative security proof). |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | This phase installs ZERO new packages (12-RESEARCH.md Standard Stack / Package Legitimacy Audit: not applicable) — no supply-chain surface introduced. |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/smoke/host-header.spike.spec.ts` exits 0 (stack boots, spike passes, stack tears down).
- Phase gate context: this spike must be green before 12-03/12-04/12-05 run; the per-wave-merge full suite (`./scripts/e2e-compose.sh`) includes it.
</verification>

<success_criteria>
- Playwright APIRequestContext Host-header delivery to Fastify is empirically PROVEN (or its failure explicitly surfaced with the documented fallback).
- No application code changed.
</success_criteria>

<artifacts_this_phase_produces>
- `apps/e2e/tests/smoke/host-header.spike.spec.ts` — Wave 0 empirical Host-header proof (throwaway spike).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-redirect-handler-e2e-core-value/12-01-SUMMARY.md` when done.
</output>
