---
phase: 01-test-infrastructure-monorepo-deployment-scaffolding
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - apps/api/prisma/schema.prisma
  - apps/api/prisma/migrations
  - apps/api/src/db.ts
  - apps/api/test/prisma-generate.test.ts
autonomous: true
requirements: [INFRA-01]

must_haves:
  truths:
    - "An initial Prisma migration is authored, committed, and applies cleanly to a real Postgres"
    - "The generated Prisma client resolves from apps/api/src/generated/prisma (no 'missing generated client' failure)"
  artifacts:
    - apps/api/prisma/schema.prisma
    - apps/api/prisma/migrations
    - apps/api/src/db.ts
  key_links:
    - "Prisma 7 explicit output path src/generated/prisma is imported by db.ts and, in Phase 2, by better-auth's adapter"
---

## Phase Goal

**As an** operator, **I want to** launch the full Kurzly stack with `docker compose up` and see the dashboard load live data from Postgres — configured only via environment variables and with data that survives restarts — **so that** I can trust the self-hosted deployment works end-to-end before enabling real features.

> This plan advances the skeleton: it lays the real data layer (schema + applied migration) the DB round-trip needs.

<objective>
Author the Prisma schema with the Prisma 7 explicit generator output path, define the `PersistenceCanary` skeleton model (the vehicle for the D-08 persistence proof and a base for Phase 2's better-auth tables), author and commit the initial migration, and generate the client. This is the [BLOCKING] schema task: build/type checks pass on generated types alone, so a real applied migration must exist before any integration test runs.

Purpose: Fix the Prisma 7 output path now so Phase 2's better-auth adapter imports the same client instance (RESEARCH Pattern 4). Prove the pnpm-blocked generate step actually ran (Pitfall 1).
Output: A committed `prisma/migrations/**`, a generated client, and `db.ts` exporting the Prisma client from the explicit path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] Prisma schema, initial migration, and generated client</name>
  <files>apps/api/prisma/schema.prisma, apps/api/prisma/migrations, apps/api/src/db.ts</files>
  <read_first>
    - .planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-RESEARCH.md (Pattern 4 — Prisma 7 explicit generator output + import path; Pattern 3 — migrate deploy at runtime; Pitfall 1)
    - .claude/CLAUDE.md (Prisma ^7 with explicit output path e.g. src/generated/prisma; better-auth must import the same generated client)
    - .planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-CONTEXT.md (D-05 migrate deploy at entrypoint; D-08 persistence canary)
  </read_first>
  <action>Create `apps/api/prisma/schema.prisma` with a `generator client { provider = "prisma-client"; output = "../src/generated/prisma" }` block (Prisma 7 requires the explicit output — RESEARCH Pattern 4), a `datasource db` block for `provider = "postgresql"` reading `env("DATABASE_URL")`, and a `PersistenceCanary` model with fields `id String @id @default(cuid())`, `token String @unique`, and `createdAt DateTime @default(now())`. Run `pnpm --filter @kurzly/api exec prisma generate` EXPLICITLY (do not rely on the postinstall alone — Pitfall 1). Author the initial migration by running `prisma migrate dev --name init` against a throwaway Postgres 18 instance (spin up a temporary `postgres:18-alpine` container, or reuse a testcontainers instance, to author SQL; the non-interactive fallback is `prisma db push` for ephemeral DBs but `migrate dev` is required here to produce committed `prisma/migrations/**`). Commit `apps/api/prisma/migrations/**` to the repo. Create `apps/api/src/db.ts` exporting a `prisma` PrismaClient singleton imported from `./generated/prisma/client` (the explicit path), NOT from bare `@prisma/client`. Runtime application of this migration happens via the container entrypoint (`prisma migrate deploy`) wired in plan 01-08.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec prisma generate && test -d apps/api/prisma/migrations && ls apps/api/prisma/migrations/*/migration.sql && pnpm --filter @kurzly/api exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/prisma/schema.prisma` contains `output   = "../src/generated/prisma"` and a `PersistenceCanary` model.
    - At least one `apps/api/prisma/migrations/*/migration.sql` exists and is committed.
    - `apps/api/src/db.ts` imports `PrismaClient` from `./generated/prisma/client`.
    - `pnpm --filter @kurzly/api exec tsc --noEmit` passes with the generated client present.
  </acceptance_criteria>
  <done>A real, committed initial migration exists and the generated client resolves.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generated-client-resolves regression test (Pitfall 1 guard)</name>
  <files>apps/api/test/prisma-generate.test.ts</files>
  <read_first>
    - .planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-RESEARCH.md (Pitfall 1 — pnpm silently blocks Prisma generate; add a Wave-0 test asserting the generated client module resolves; Security Domain — Tampering via silent client-gen failure)
    - apps/api/src/db.ts (created in Task 1)
    - apps/api/vitest.config.ts (base unit config from plan 01-02)
  </read_first>
  <behavior>
    - Test 1: importing `../src/generated/prisma/client` resolves and exposes a `PrismaClient` constructor.
    - Test 2: `new PrismaClient()` constructs without throwing (no DB connection attempted).
  </behavior>
  <action>Write `apps/api/test/prisma-generate.test.ts` asserting the generated Prisma client module resolves and `PrismaClient` is constructable. This is a fast unit test (no testcontainers) that fails loudly if the pnpm build-script allowlist regresses and the client was never generated (RESEARCH Pitfall 1, corroborated by the user's global CLAUDE.md "missing generated Prisma client" warning). It runs under the base api vitest config.</action>
  <verify>
    <automated>pnpm --filter @kurzly/api exec vitest run test/prisma-generate.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm --filter @kurzly/api exec vitest run test/prisma-generate.test.ts` exits 0.
    - The test fails if `apps/api/src/generated/prisma` is absent (verified by temporarily confirming a red state before generate, then green after).
  </acceptance_criteria>
  <done>A regression test guards against the silent missing-generated-client failure mode.</done>
</task>

</tasks>

<artifacts_produced>
This plan creates (all NEW — exclude from drift verification):
- `apps/api/prisma/schema.prisma` (generator `prisma-client` output `../src/generated/prisma`; `PersistenceCanary` model: `id`, `token`, `createdAt`)
- `apps/api/prisma/migrations/**` (initial `init` migration SQL, committed)
- `apps/api/src/db.ts` (exports `prisma` from `./generated/prisma/client`)
- `apps/api/src/generated/prisma/**` (generated, gitignored — regenerated by `prisma generate`)
- `apps/api/test/prisma-generate.test.ts`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build tooling → shipped image | A silently ungenerated Prisma client could ship a broken/stale data layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering | Prisma client generation under pnpm script-blocking | medium | mitigate | Explicit `prisma generate` step + `prisma-generate.test.ts` asserting the client resolves; `allowBuilds` set in plan 01-02 |
| T-01-04 | Denial of Service | migrate-at-runtime not destructive | high | mitigate | Migrations authored with `migrate dev` and applied at runtime with `migrate deploy` (never `migrate reset`); wired/verified in plan 01-08 entrypoint |
</threat_model>

<verification>
- The initial migration applies cleanly to a real Postgres (exercised by the harness in plan 01-05).
- The generated client resolves under `tsc --noEmit` and the regression test.
</verification>

<success_criteria>
- A committed initial migration + resolvable generated client exist before any integration test runs.
</success_criteria>

<output>
Create `.planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-03-SUMMARY.md` when done.
</output>
