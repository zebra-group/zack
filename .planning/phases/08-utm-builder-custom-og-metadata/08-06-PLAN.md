---
phase: 08-utm-builder-custom-og-metadata
plan: 06
type: execute
wave: 4
depends_on: [08-01, 08-04, 08-05]
files_modified:
  - apps/web/src/views/LinksView.vue
  - apps/web/src/views/LinksView.test.ts
  - apps/web/src/views/LinkDetailView.vue
  - apps/web/src/views/LinkDetailView.test.ts
autonomous: true
requirements: [META-01, META-02]
user_setup: []

must_haves:
  truths:
    - "Creating a link from the links list persists the UTM and OG values typed in the form"
    - "Editing a link from the list or from the detail page pre-fills all six fields and saves changes, including clearing a field"
    - "A server validation error for a UTM or OG field renders as an inline message under that field, in the locked wording"
    - "The links table shows a UTM badge and an OG badge on rows whose link carries those values, in the locked order"
    - "The link detail page shows a UTM chip and an OG chip when the link carries those values"
    - "The detail page's destination line keeps showing the stored target URL without UTM parameters (UI-08-07)"
    - "The new badges and chips appear right after a successful save without a page reload"
  artifacts:
    - apps/web/src/views/LinksView.vue
    - apps/web/src/views/LinkDetailView.vue
  key_links:
    - "both views' existing single error prop carries the new validation codes into the modal's internal mapLinkFormError call — no second error channel"
    - "both parent views forward the modal's six payload keys into createLink/updateLink unchanged"
    - "badge and chip visibility derives from the returned LinkDTO, so a save updates them in place"
---

## Phase Goal

**As a** dashboard user managing campaign links, **I want to** attach UTM parameters and custom social-preview metadata to a short link and see exactly what the final URL and the social card will look like before I save, **so that** my campaign traffic is attributed correctly and my links preview the way I intend — without Kurzly ever fetching the destination.

> Derived from the ROADMAP Phase 8 `**Goal:**` line into user-story shape (MVP framing), matching the Phase 7 precedent.

<objective>
Close the loop: connect the form sections built in 08-04 and 08-05 to the API client built in 08-01, prove the inline validation messages reach the user through the modal's existing error prop, and deliver UI-SPEC Surfaces C and D — the „UTM"/„OG" attribute badges in the links table and the two chips on the link detail page.

Purpose: Until this lands, the form sections have no parent to submit through and the user has no signal on a saved link that metadata is set.
Output: Payload threading in both parent views, an end-to-end failing-save assertion per view, two badges, two chips.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-utm-builder-custom-og-metadata/08-UI-SPEC.md
@apps/web/src/views/LinksView.vue
@apps/web/src/views/LinkDetailView.vue
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Links list — payload threading, edit pre-fill, and the UTM/OG attribute badges (Surface C)</name>
  <files>apps/web/src/views/LinksView.vue, apps/web/src/views/LinksView.test.ts</files>
  <read_first>
    - apps/web/src/views/LinksView.vue (the create and edit submit handlers around lines 124-205 — they map the modal payload onto CreateLinkInput/UpdateLinkInput; the slug cell markup around line 322; the existing badge CSS rule near line 567; the two modal instances around lines 358-372)
    - .planning/phases/08-utm-builder-custom-og-metadata/08-UI-SPEC.md (Layout Contract Surface C in full, including the locked badge order and the note that the password and expiry badges are a pre-existing Phase 5 gap explicitly out of scope here)
    - packages/shared/src/index.ts (CreateLinkInput takes the six as plain optional strings; UpdateLinkInput takes them as optional nullable strings)
  </read_first>
  <behavior>
    - Creating a link with UTM and OG values typed in the modal sends all six on the create request.
    - Editing a link opens the modal with all six pre-filled from the link's DTO.
    - Saving an edit forwards the modal's payload unchanged, so an explicit clear reaches the API as a clear rather than being dropped.
    - A failed save whose 400 carries the invalid OG image URL code renders the locked scheme message beneath the image-URL input inside the still-open modal; a 400 carrying the too-long UTM code renders the locked UTM message beneath the UTM inputs.
    - A row whose link has any UTM value shows the UTM badge; a row whose link has any OG value shows the OG badge; a row with neither shows neither.
    - When both are present the UTM badge precedes the OG badge, and both precede the tracking badge.
    - After a successful create or edit the badges reflect the returned DTO without a reload.
    - The existing tracking badge keeps its exact appearance and wording.
  </behavior>
  <action>In apps/web/src/views/LinksView.vue thread the six new payload keys through both submit handlers: the create handler passes each value through unchanged, collapsing a null to undefined since the create input type takes plain optional strings; the edit handler forwards each value exactly as received, preserving null so a cleared field actually clears. Pass the six matching initial-value props from the link being edited into the edit-mode modal instance. Confirm — do not rebuild — that both modal instances already forward this view's submit error on the existing single `error` prop, which is what carries the new UTM and OG validation codes into the modal's internal `mapLinkFormError` call and thence to the inline slots plans 08-04 and 08-05 rendered; add the failing-save behavior case below to prove that path end to end rather than introducing any additional error prop. Add two small predicates that report whether a link carries any UTM value and whether it carries any OG value, and render a badge for each inside the existing slug cell, positioned so the locked order holds once the currently-missing Phase 5 password and expiry badges are eventually added — that is, immediately before the tracking badge. Generalize the existing tracking badge CSS class to a neutral attribute-badge name shared by all three badges, per the UI-SPEC's Surface C note that the shipped rule is the one being renamed rather than duplicated, and update every reference to it in the markup and in the test file in the same commit. The badges are neutral chips: they must not use the accent colour. Extend LinksView.test.ts with the behavior cases above. Commit the failing tests first (`test(08-06): failing cases for links-list UTM/OG threading and badges`), then the implementation (`feat(08-06): thread UTM/OG through the links list and add attribute badges`).</action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/views/LinksView.test.ts</automated>
  </verify>
  <done>Create and edit carry all six fields with correct clear semantics; both badges render conditionally in the locked order with the shared neutral badge style; the tracking badge is unchanged visually.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Link detail — payload threading, edit pre-fill, and the two metadata chips (Surface D)</name>
  <files>apps/web/src/views/LinkDetailView.vue, apps/web/src/views/LinkDetailView.test.ts</files>
  <read_first>
    - apps/web/src/views/LinkDetailView.vue (the edit submit handler around lines 270-300; the chips row around lines 364-367; the modal instance around lines 504-518)
    - .planning/phases/08-utm-builder-custom-og-metadata/08-UI-SPEC.md (Layout Contract Surface D in full, including UI-08-07 and the locked chip order and wording)
  </read_first>
  <behavior>
    - Opening the edit modal from the detail page pre-fills all six fields from the link.
    - Saving forwards the modal's payload unchanged, so a cleared field reaches the API as a clear.
    - The chips row shows the UTM chip when the link carries any UTM value and the OG chip when it carries any OG value, after the existing hostname and created chips.
    - A failed save whose 400 carries the invalid OG image URL code renders the locked scheme message beneath the image-URL input inside the still-open modal, reaching it purely through the existing `error` prop.
    - Neither chip renders when the link carries no such values.
    - After a successful save the chips update in place from the returned DTO, with no reload.
    - The destination line above the chips continues to show the stored target URL without any UTM parameters appended (UI-08-07).
  </behavior>
  <action>In apps/web/src/views/LinkDetailView.vue pass the six matching initial-value props from the loaded link into the edit modal instance, and thread the six payload keys through the edit submit handler forwarding each value exactly as received so an explicit clear survives. As in Task 1, confirm rather than rebuild the error path: the existing single `error` prop already carries the new validation codes into the modal's internal mapping, so add the failing-save behavior case and no new error prop. Add the same two predicates used by the links list — computed here against the single loaded link ref — and render two chips in the existing chips row using the existing chip class unchanged, in the locked order after the created chip. Do not add a section, card or panel; two chips and nothing else. Leave the destination line untouched: it must keep rendering the stored target URL, because the UTM parameters live in their own columns and the chip is the only signal that they are set (UI-08-07, D-08-01). Extend LinkDetailView.test.ts with the behavior cases above, including an explicit assertion that the destination line does not contain a UTM parameter for a link that has them. Commit the failing tests first (`test(08-06): failing cases for link-detail UTM/OG threading and chips`), then the implementation (`feat(08-06): thread UTM/OG through link detail and add metadata chips`).</action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/views/LinkDetailView.test.ts</automated>
  </verify>
  <done>Detail-page edit round-trips all six fields with correct clear semantics; both chips render conditionally in the locked order; the destination line still shows the clean stored target.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| modal payload → API request | Six user-typed values are forwarded from the form to authenticated write endpoints |
| API error body → rendered message | A server error code selects which locked message is shown inline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-ERR-ECHO | Information Disclosure | error rendering path | medium | mitigate | The mapper (built in plan 08-04) selects from fixed local message constants keyed on the error code; no server-supplied string is rendered into the UI, and this plan adds no second error channel that could bypass it |
| T-08-CLEAR-DROP | Tampering | parent payload forwarding | medium | mitigate | Both parents forward null through untouched rather than collapsing it with a nullish fallback; explicit tests cover the clear path on both views |
| T-08-BADGE-LEAK | Information Disclosure | badges and chips | low | mitigate | Badges and chips report only that metadata exists; they render no value and no destination |
| T-08-DETAIL-LEAK | Information Disclosure | detail destination line | low | mitigate | The line keeps rendering the stored target only; a test asserts no UTM parameter is appended there (UI-08-07) |
</threat_model>

<verification>
- `pnpm --filter @kurzly/web test` full suite green.
- `pnpm --filter @kurzly/api test` full suite green (regression check against the API contract this plan consumes).
- `pnpm tsc --noEmit` clean across the workspace, with the shared package rebuilt.
- Manual read-through: the badge order in the slug cell and the chip order in the chips row match the locked orders in Surfaces C and D, allowing for the Phase 5 password/expiry badges that are still missing by design.
</verification>

<success_criteria>
- A user can create or edit a link with UTM parameters and custom OG values end to end from either the list or the detail page, sees inline validation messages in the locked wording, and sees the UTM and OG badges and chips appear immediately after saving.
</success_criteria>

<output>
Create `.planning/phases/08-utm-builder-custom-og-metadata/08-06-SUMMARY.md` when done.
</output>
