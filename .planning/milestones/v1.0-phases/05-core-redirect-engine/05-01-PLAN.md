---
phase: 05-core-redirect-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/env.ts
  - .env.example
autonomous: false
requirements: [REDIR-04, REDIR-05, UI-04, UI-05]
user_setup: []

must_haves:
  truths:
    - "bcryptjs, isbot, and @fastify/cookie are installed in @kurzly/api at the RESEARCH-pinned versions after operator sign-off"
    - "A fresh instance boots with no new configuration — BRAND_NAME, BRAND_ACCENT, PASSWORD_HASH_COST all have fail-safe defaults"
    - "The env schema and .env.example stay in lockstep (drift test green)"
  prohibitions:
    - "No package is installed before the blocking-human supply-chain checkpoint is approved"
    - "No new pnpm-workspace.yaml allowBuilds entry is added (none of the three declare a postinstall script)"
    - "No required (non-defaulted) env var is added — a bare deployment must not start failing to boot"
  artifacts:
    - path: apps/api/package.json
      provides: "bcryptjs/isbot/@fastify/cookie dependency declarations"
      contains: "bcryptjs"
    - path: apps/api/src/env.ts
      provides: "BRAND_NAME/BRAND_ACCENT/PASSWORD_HASH_COST env schema keys (optional, defaulted)"
      contains: "BRAND_NAME"
    - path: .env.example
      provides: "documented examples for the three new env keys"
      contains: "BRAND_NAME"
  key_links:
    - from: apps/api/src/env.ts
      to: .env.example
      via: "env-example-drift test asserts the schema key set equals the documented set"
      pattern: "BRAND_NAME"
---

<objective>
Install the three new backend dependencies this phase needs (D-02 bcryptjs password hashing, D-04 isbot bot detection, D-07/D-08 @fastify/cookie link-unlock cookie) behind the established supply-chain human-verify gate, and add the branding/hash-cost configuration knobs (D-10) the public HTML render layer and password hashing consume.

Purpose: Everything downstream in Phase 5 depends on these packages and env keys existing. `isbot` and `@fastify/cookie` were flagged `SUS` ("too-new" heuristic only) by the package-legitimacy gate — this plan clears them through the same blocking-human checkpoint pattern as Phases 1/2/4 before the first install, then wires the fail-safe-defaulted config.
Output: Installed deps at pinned versions; `BRAND_NAME`/`BRAND_ACCENT`/`PASSWORD_HASH_COST` in the env schema + `.env.example`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-core-redirect-engine/05-RESEARCH.md
@.planning/phases/05-core-redirect-engine/05-UI-SPEC.md

# Env pattern to mirror (CNAME_TARGET/A_RECORD_IP optional-with-default):
@apps/api/src/env.ts
# Drift guard the new keys must satisfy:
@apps/api/test/env-example-drift.test.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain sign-off for bcryptjs / isbot / @fastify/cookie (T-05-SC)</name>
  <files>apps/api/package.json</files>
  <read_first>
    - .planning/phases/05-core-redirect-engine/05-RESEARCH.md § Package Legitimacy Audit (the SUS flags on isbot / @fastify/cookie are the "too-new" heuristic only; bcryptjs is OK; none declare a postinstall script)
  </read_first>
  <action>
    Pause for a blocking-human supply-chain sign-off BEFORE running any `pnpm add`. Present the package-legitimacy readout below to the operator; do not install until they approve. This is not auto-approvable (legitimacy checkpoints ignore auto_advance) — proceed only on explicit operator approval.
  </action>
  <verify>
    <automated>MISSING — blocking-human checkpoint; approval is the gate, no automated command applies</automated>
  </verify>
  <what-built>
    Nothing installed yet. This checkpoint presents the package-legitimacy readout for operator sign-off BEFORE the first `pnpm add`, matching the T-02-SC-Gate / T-04-SC-Gate pattern from prior phases.
  </what-built>
  <how-to-verify>
    1. Confirm each package's official repo + pinned version:
       - bcryptjs `^3.0.3` — https://www.npmjs.com/package/bcryptjs (repo github.com/dcodeIO/bcrypt.js)
       - isbot `^5.2.0` — https://www.npmjs.com/package/isbot (repo github.com/omrilotan/isbot)
       - @fastify/cookie `^11.1.1` — https://www.npmjs.com/package/@fastify/cookie (repo github.com/fastify/fastify-cookie; exact version already pinned in .claude/CLAUDE.md's approved stack table)
    2. Confirm none declares a postinstall script (`npm view <pkg> scripts.postinstall` is empty for all three).
    3. isbot and @fastify/cookie are flagged SUS on a "recently published" heuristic only — both have tens of millions of weekly downloads and resolve to their well-known official repos.
  </how-to-verify>
  <acceptance_criteria>
    Operator types "approved" (or names concerns). Legitimacy checkpoints are never auto-approvable — do not proceed on auto_advance.
  </acceptance_criteria>
  <resume-signal>Type "approved" to authorize the install, or describe concerns.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install the three dependencies at pinned versions</name>
  <files>apps/api/package.json, pnpm-lock.yaml</files>
  <read_first>
    - apps/api/package.json (current dependency block — add alongside existing deps)
    - .planning/phases/05-core-redirect-engine/05-RESEARCH.md § Standard Stack (pinned versions + installation command)
  </read_first>
  <action>
    Run `pnpm --filter @kurzly/api add bcryptjs isbot @fastify/cookie`. Confirm resolved versions land in the researched ranges: bcryptjs ^3.0.x, isbot ^5.2.x, @fastify/cookie ^11.1.x. Do NOT add any pnpm-workspace.yaml allowBuilds entry — verify none of the three ran (or requested) a build/lifecycle script during install; if pnpm reports an ignored build script for any of them, stop and surface it rather than approving a build. This mirrors the Phase 1/2/4 precedent (T-01-02) of keeping allowBuilds minimal. Rebuild is not required here — no source consumes the packages yet.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api ls bcryptjs isbot @fastify/cookie | grep -E '(└|├)?.*(bcryptjs|isbot|@fastify/cookie)@'</automated>
  </verify>
  <acceptance_criteria>
    All three packages appear in apps/api/package.json dependencies at the pinned ranges; `pnpm install` completes clean; no new allowBuilds entry was needed.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add BRAND_NAME / BRAND_ACCENT / PASSWORD_HASH_COST env keys (D-10, D-02)</name>
  <files>apps/api/src/env.ts, .env.example</files>
  <read_first>
    - apps/api/src/env.ts (copy the CNAME_TARGET/A_RECORD_IP `.optional().default(...)` idiom exactly; note DOMAIN_VERIFICATION_DEFAULTS export pattern)
    - apps/api/test/env-example-drift.test.ts (the schema key set must equal the documented .env.example key set)
    - .planning/phases/05-core-redirect-engine/05-UI-SPEC.md § "NEU in Phase 5: Branding-Konfiguration" (defaults: BRAND_NAME="Kurzly", BRAND_ACCENT="#d7ff01")
  </read_first>
  <action>
    Extend `envSchema` with three OPTIONAL, fail-safe-defaulted keys following the CNAME_TARGET precedent, so a fresh instance boots unchanged with zero new config:
      - `BRAND_NAME`: string, min 1, default "Kurzly" (D-10; consumed by the public HTML brand row + bot-OG tags).
      - `BRAND_ACCENT`: string, min 1, default "#d7ff01" (D-10; overrides --accent only in the public pages' inline style).
      - `PASSWORD_HASH_COST`: coerced positive int, default 11 (D-02; RESEARCH Pitfall 2 — start conservative at 10–11, not 12, and keep it ENV-tunable to avoid blocking the redirect hot path under concurrent verify load).
    Add matching documented example lines to the repo-root `.env.example` (keep the drift test green — the schema key set and the documented key set must be identical). Do not mark any of the three required.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/api test -- env-example-drift env</automated>
  </verify>
  <acceptance_criteria>
    env.test.ts + env-example-drift.test.ts pass; `parseEnv({...minimal})` succeeds without any of the three keys set, returning the documented defaults (BRAND_NAME="Kurzly", BRAND_ACCENT="#d7ff01", PASSWORD_HASH_COST=11).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build | Third-party package code enters the trusted build/runtime |
| operator env → process | Untrusted operator-supplied config crosses into the app |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-SC | Tampering | npm installs (bcryptjs/isbot/@fastify/cookie) | high | mitigate | Blocking-human supply-chain checkpoint (Task 1) verifies repo/version/no-postinstall before install; not auto-approvable |
| T-05-ENV-01 | Tampering | PASSWORD_HASH_COST / BRAND_* env | low | accept | Coerced/validated by Zod; defaults are safe; operator misconfiguration is a self-inflicted, non-crossing-trust-boundary risk |
</threat_model>

## Artifacts This Phase Produces (this plan)

- **New env keys:** `BRAND_NAME` (default `"Kurzly"`), `BRAND_ACCENT` (default `"#d7ff01"`), `PASSWORD_HASH_COST` (default `11`) — all optional with fail-safe defaults in `apps/api/src/env.ts` + documented in `.env.example`.
- **New dependencies:** `bcryptjs@^3.0.3`, `isbot@^5.2.0`, `@fastify/cookie@^11.1.1` in `apps/api/package.json`.

<verification>
- Blocking-human checkpoint approved before any install.
- `pnpm --filter @kurzly/api ls` shows all three packages at pinned ranges.
- `pnpm --filter @kurzly/api test -- env env-example-drift` green.
- `pnpm -r typecheck` clean.
</verification>

<success_criteria>
- Three packages installed at RESEARCH-pinned versions after operator sign-off.
- Branding + hash-cost env keys present, optional, defaulted; drift test green.
- No allowBuilds entry added; a bare deployment still boots without new config.
</success_criteria>

<output>
Create `.planning/phases/05-core-redirect-engine/05-01-SUMMARY.md` when done.
</output>
