---
phase: 06-internal-tracking-analytics
plan: 08
type: execute
wave: 4
depends_on: ["06-05", "06-07"]
files_modified:
  - apps/web/src/api.ts
  - apps/web/src/views/AnalyticsView.vue
  - apps/web/src/views/AnalyticsView.test.ts
  - apps/web/src/router/index.ts
autonomous: true
requirements: [TRACK-05]
user_setup: []

must_haves:
  truths:
    - "/analytics resolves to AnalyticsView (no longer ComingSoonView) and requires auth (TRACK-05)"
    - "The global overview shows 4 stat cards (Klicks 30 Tage, Unique Visitors, Aktive Links, QR-Scans), a 30-bar chart, a Top-Links list, and a Referrer list from GET /api/analytics"
    - "The QR-Scans tile shows the real COUNT(source='qr') value (0 this phase) with no 'coming soon' copy (D-14)"
    - "The Aktive Links tile shows its real count even in the zero-data state (not forced to 0)"
    - "Loading / zero-data / data states are mutually exclusive"
    - "Top-Links rows navigate to /links/:id"
  artifacts:
    - apps/web/src/api.ts (getGlobalAnalytics client)
    - apps/web/src/views/AnalyticsView.vue (global overview, Surface B)
    - apps/web/src/router/index.ts (/analytics → AnalyticsView)
  key_links:
    - "getGlobalAnalytics() → GET /api/analytics (06-05 endpoint, scoped to the caller's domains)"
    - "reuses the same skeleton/zero/data 3-state pattern and locked tokens as Surface A"
  prohibitions:
    - "QR-Scans tile is a normal metric read as 0 — no special 'bald verfügbar' copy (D-14)"
    - "The Aktive Links tile is never zero-forced when links exist, even with zero clicks"
    - "No new spacing/color tokens — Surface B reuses the locked Surface A / prototype tokens"
---

<objective>
Replace `ComingSoonView` at `/analytics` with the real global analytics overview (`AnalyticsView.vue`, Surface B), delivering TRACK-05: an account-wide dashboard of 4 stat cards (30-day clicks, unique visitors, active links, QR scans), a 30-bar "Klicks gesamt" chart, a clickable Top-Links list, and a Referrer list — all scoped to the caller's own domains via the 06-05 `GET /api/analytics` endpoint.

The QR-Scans tile shows the real `COUNT(source='qr')` (0 this phase, D-14 — no special "coming soon" copy, so Phase 7 just starts writing `source='qr'` and the tile lights up). The Aktive Links tile reads its real Link count independent of clicks (never zero-forced). The screen reuses the exact loading/zero-data/data 3-state pattern and locked tokens from Surface A.

Purpose: a link owner opens Analytics and sees their whole account's reach at a glance.
Output: the api client + the new view + the router swap, tested.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-internal-tracking-analytics/06-UI-SPEC.md
@apps/web/src/api.ts
@apps/web/src/views/LinksView.vue
@apps/web/src/router/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: getGlobalAnalytics client + AnalyticsView global overview (Surface B, TRACK-05)</name>
  <files>apps/web/src/api.ts, apps/web/src/views/AnalyticsView.vue, apps/web/src/views/AnalyticsView.test.ts</files>
  <read_first>
    - apps/web/src/api.ts (parseJsonOrThrow shape; GlobalAnalyticsDTO import from @kurzly/shared, added in 06-05)
    - 06-UI-SPEC.md § Surface B (screen container max-width 1060px, padding 28px 36px 48px, gap 16px; header title 21/600 "Analytics" + subtitle "alle Links · letzte 30 Tage"; stat-grid repeat(4,1fr); chart height 150px "Klicks gesamt"; two-col 1.3fr 1fr — Top-Links list width 190px/clicks 48px, rows clickable → /links/:id; Referrer list; loading/zero-data/data rules; "Aktive Links tile not zero-forced" note; QR-Scans normal metric)
    - 06-UI-SPEC.md § Copywriting Contract (global stat labels; zero-data hint global copy; "Keine Daten")
    - apps/web/src/views/LinksView.vue (screen-container/header CSS + per-view toast + navigation via router.push({ name: 'link-detail', params: { id } }) to reuse)
    - apps/web/src/views/LinkDetailView.vue (the Surface A skeleton/zero/data markup + tokens to mirror for consistency, produced in 06-07)
  </read_first>
  <action>
    Add `getGlobalAnalytics(): Promise<GlobalAnalyticsDTO>` to api.ts as `GET /api/analytics` via parseJsonOrThrow. Create `AnalyticsView.vue` (`<script setup>`): load global analytics on mount into `analytics = ref<GlobalAnalyticsDTO | null>(null)` + `loading = ref(true)`, with the existing try/catch + per-view toast pattern (toast "Analytics konnten nicht geladen werden." on failure). Header: "Analytics" (21/600, letter-spacing -.02em) + subtitle "alle Links · letzte 30 Tage". Render exactly one of: loading skeleton (4 stat skeletons + 150px chart skeleton + 2 list skeletons), zero-data (when analytics.clicks30Days === 0), or data. DATA: `.stat-grid` repeat(4,1fr) with "Klicks (30 Tage)"=clicks30Days, "Unique Visitors"=uniqueVisitors, "Aktive Links"=activeLinks, "QR-Scans"=qrScans (real value, 0 now — normal metric, no special copy); `.chart-card` height 150px titled "Klicks gesamt" with 30 bars from dailySeries (per-bar title tooltip, hover accent); `.two-col` 1.3fr 1fr — a "Top Links" card whose rows show the short slug (Mono, 190px), an accent progress bar, and the click count (48px), each row clickable (cursor pointer, hover) navigating to `/links/:id` via router.push; and a "Referrer" card (90px/flex/38px rows, null host → "Direkt"). ZERO-DATA: 4 stat cards render with 0 EXCEPT "Aktive Links" which shows its real value (activeLinks may be >0 with zero clicks — never zero-force it); chart area → centered hint "Noch keine Klicks erfasst — sobald Links aufgerufen werden, erscheinen hier Daten."; both lists show a single "Keine Daten" row. Reuse the exact locked Surface A/prototype tokens (no new tokens). Test: data payload → 4 stat cards incl QR-Scans "0", 30 bars, clickable Top-Links rows; zero-data payload (clicks30Days 0, activeLinks 3) → hint + "Keine Daten" lists + Aktive Links shows 3 (not 0); loading → skeleton; clicking a Top-Links row calls router navigation to /links/:id.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/views/AnalyticsView.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - getGlobalAnalytics hits GET /api/analytics
    - Data state: 4 stat cards (incl QR-Scans reading 0 as a normal metric), 30-bar chart, clickable Top-Links → /links/:id, Referrer list
    - Zero-data: chart hint + "Keine Daten" lists; Aktive Links shows real count, not forced 0
    - Loading skeleton (no spinner); the three states mutually exclusive
    - web typecheck passes
  </acceptance_criteria>
  <done>The Analytics screen renders the account-wide overview from the scoped endpoint, with QR-Scans as a live-0 metric and Aktive Links independent of clicks.</done>
</task>

<task type="auto">
  <name>Task 2: Router swap /analytics → AnalyticsView</name>
  <files>apps/web/src/router/index.ts, apps/web/src/views/AnalyticsView.test.ts</files>
  <read_first>
    - apps/web/src/router/index.ts (the /analytics route entry lines 66-71 using ComingSoonView; the imports block lines 12-19; the generic beforeEach guard lines 87-118 that already covers meta.requiresAuth)
    - 06-PATTERNS.md § router/index.ts (single-line component swap, keep meta unchanged, no guard change)
  </read_first>
  <action>
    Import `AnalyticsView` alongside the existing view imports and change the `/analytics` route's `component: ComingSoonView` to `component: AnalyticsView`, keeping `name: "analytics"`, `meta: { requiresAuth: true, label: "Analytics" }` unchanged. Do not touch the beforeEach guard (it is generic over meta.requiresAuth). Leave the other ComingSoonView routes (qr-codes, team) as-is. Add a router-level test asserting the `/analytics` route resolves to AnalyticsView (and still carries requiresAuth), so a future accidental revert to ComingSoonView is caught.
  </action>
  <verify>
    <automated>pnpm --filter @kurzly/web test -- src/views/AnalyticsView.test.ts && grep -q "AnalyticsView" apps/web/src/router/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - /analytics maps to AnalyticsView with requiresAuth: true
    - qr-codes/team routes still use ComingSoonView (untouched)
    - `grep -c "ComingSoonView" apps/web/src/router/index.ts` reflects only the remaining coming-soon routes (analytics no longer among them)
    - web typecheck passes
  </acceptance_criteria>
  <done>Navigating to /analytics renders the real global analytics overview, gated by auth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GET /api/analytics | the view renders only the server-scoped global DTO; the router guard is UX-only, the server is authoritative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-GLOBALUI | Information Disclosure | AnalyticsView | low | accept | Renders only GET /api/analytics's scopedDomainIds-scoped DTO (06-05); the router guard is a UX convenience, not the access boundary (T-02-14) |
| T-06-ROUTE | Spoofing | router guard | low | accept | Client-side guard is UX-only; every /api/analytics request is independently re-authorized server-side |
</threat_model>

## Artifacts this plan produces
- **api client:** `getGlobalAnalytics()` in apps/web/src/api.ts
- **views:** `apps/web/src/views/AnalyticsView.vue` (global overview, Surface B)
- **router:** `/analytics` → `AnalyticsView` (replaces ComingSoonView)
- **tests:** `apps/web/src/views/AnalyticsView.test.ts`

<verification>
- `pnpm --filter @kurzly/web test -- src/views/AnalyticsView.test.ts` green
- `/analytics` routes to AnalyticsView (grep)
- web typecheck green
</verification>

<success_criteria>
A link owner opens /analytics and sees their account-wide reach — 30-day clicks, unique visitors, active links, QR scans (0), a 30-bar chart, clickable top links, and top referrers — scoped to their own domains, with correct loading/zero/data states.
</success_criteria>

<output>
Create `.planning/phases/06-internal-tracking-analytics/06-08-SUMMARY.md` when done.
</output>
