---
phase: 01-test-infrastructure-monorepo-deployment-scaffolding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [INFRA-01]
user_setup: []

must_haves:
  truths:
    - "No [SLOP] package is ever installed; every [SUS]/[ASSUMED] package is human-confirmed legitimate before the first pnpm install"
  artifacts: []
  key_links:
    - "Legitimacy approval gates every downstream install plan (02+)"
---

## Phase Goal

**As an** operator, **I want to** launch the full Kurzly stack with `docker compose up` and see the dashboard load live data from Postgres — configured only via environment variables and with data that survives restarts — **so that** I can trust the self-hosted deployment works end-to-end before enabling real features.

> Derived from the ROADMAP Phase 1 `**Goal:**` line (prose form) into user-story shape for MVP/walking-skeleton framing. See SKELETON.md for the phase-level architectural contract.

<objective>
Human-verify the legitimacy of every package this phase will install before the first `pnpm install` runs. RESEARCH.md flagged 12 mainstream packages as `[SUS]` ("too-new" — a recent patch release, not an identity concern) plus a small `[ASSUMED]` set (nodemailer version, tsup/tsx choice). Per the Package Legitimacy Gate protocol this must be a single consolidated blocking-human checkpoint, not one per package.

Purpose: Supply-chain safety (threat T-01-SC). A hallucinated/slopsquatted dependency introduced at scaffolding time would poison every later phase.
Output: A recorded human approval that unblocks Wave 2's install. No files are created by this plan.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-RESEARCH.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Supply-chain legitimacy sign-off for all Phase 1 dependencies</name>
  <read_first>
    - .planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-RESEARCH.md (## Package Legitimacy Audit table + the "Note on too-new SUS verdicts" paragraph + the [ASSUMED] note)
    - .claude/CLAUDE.md (## Technology Stack version matrix — the authoritative pins to confirm against)
  </read_first>
  <what-built>
    Nothing is built yet. This is a review gate. RESEARCH.md's Package Legitimacy Audit lists every package Phase 1 installs with a registry verdict. All 12 `[SUS]` verdicts are "too-new" (a mainstream package shipped a patch release recently), not identity/trust concerns; no `[SLOP]` packages exist.
  </what-built>
  <how-to-verify>
    1. Open the `## Package Legitimacy Audit` table in 01-RESEARCH.md.
    2. For each `[SUS]` package (fastify, @fastify/static, @fastify/cors, @fastify/helmet, @fastify/rate-limit, vitest, @vitest/coverage-v8, testcontainers, @testcontainers/postgresql, vue, vite, typescript) and each `[ASSUMED]` item (nodemailer, tsup/tsx), confirm on npmjs.com that: the package name matches exactly, the GitHub source repo is under the expected well-known org, and weekly downloads are in the millions.
    3. Confirm every version to be pinned matches the CLAUDE.md version matrix.
    4. Confirm no `[SLOP]` verdict appears anywhere in the table.
  </how-to-verify>
  <action>Do NOT run `pnpm install` or create any package.json in this plan. Present the RESEARCH.md legitimacy table to the operator and request explicit approval. This gate is never auto-approvable regardless of `workflow.auto_advance`. On approval, record it and release Wave 2 (plan 01-02) to author manifests and install. If the operator flags any package, stop and escalate before any install.</action>
  <verify>
    <human-check>Operator types "approved" after confirming all [SUS]/[ASSUMED] packages against npmjs.com + the CLAUDE.md version matrix, and that no [SLOP] package is present.</human-check>
  </verify>
  <acceptance_criteria>
    - Operator has explicitly approved the consolidated package list before any install runs.
    - No package with a `[SLOP]` verdict is on the install list.
    - Approval is recorded in this plan's SUMMARY so downstream install plans can proceed.
  </acceptance_criteria>
  <resume-signal>Type "approved" to release the install, or name the package(s) to investigate.</resume-signal>
</task>

</tasks>

<artifacts_produced>
This plan produces no files. Its output is a recorded human approval (in the plan SUMMARY) that gates every downstream `pnpm install`.
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine | Third-party package code is fetched and executed at install/build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm installs (all Phase 1 deps) | high | mitigate | This blocking-human checkpoint verifies every [SUS]/[ASSUMED] package against npmjs.com + the CLAUDE.md matrix before the first install; [SLOP] forbidden; never auto-approvable |
</threat_model>

<verification>
- The gate blocks until an explicit human "approved".
- No file-system changes occur in this plan.
</verification>

<success_criteria>
- All Phase 1 dependencies human-confirmed legitimate; Wave 2 install unblocked.
</success_criteria>

<output>
Create `.planning/phases/01-test-infrastructure-monorepo-deployment-scaffolding/01-01-SUMMARY.md` when done, recording the approval.
</output>
