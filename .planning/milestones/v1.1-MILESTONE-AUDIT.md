---
milestone: v1.1
audited: 2026-07-25
status: passed
scores:
  requirements: 32/32
  phases: 7/7
  integration: 6/6
  flows: 1/1
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - id: "D-17-05-01 (Phase 17)"
    item: "Full tests/authed/ directory (15 files) run in one invocation exhausts the global 100req/15min rate-limit bucket, since only some specs' direct request calls carry the x-e2e-bypass header — real browser-driven page navigations don't"
    severity: low
    note: "Confirmed unrelated to any individual spec's correctness (every spec passes standalone, live-reverified on fresh containers). Recommended fix: extraHTTPHeaders bypass on chromium-admin/chromium-member projects in playwright.config.ts. Deferred to a dedicated stabilization pass, out of Phase 17's own scope."
  - id: "17-REVIEW WR-02"
    item: "Cross-file Link/QrCode/ClickEvent truncate race vs. db-isolation.spec.ts mitigated only by retries:2, not eliminated"
    severity: low
    note: "Pre-existing since Phase 12, cross-cutting across the whole suite. Explicitly reviewed and left unfixed per the review's own framing (no production change needed; a deeper fix would need Fastify-level signal to distinguish a truncate-race 404 from a genuine regression)."
  - id: "17-REVIEW (unlabeled)"
    item: "team-role-domain-reassign.spec.ts promotes a throwaway member to real admin role + baseline-domain membership; teardown fix (WR-01) added afterward, but accumulation across many manual re-invocations this session contributed to analytics top-5-crowding flakiness observed mid-session"
    severity: low
    note: "Fixed going forward via finally-block Prisma teardown (commit 210d435); the flakiness observed during this session's own repeated verification passes was from DB state accumulated across many manual reboots, not a defect in a single CI run."
  - id: "IN-01 (repeated across Phases 12-17)"
    item: "Retry/skip boilerplate (admin-only project guard + retry-attribution logging) duplicated verbatim across 5+ spec files instead of a shared apps/e2e/src/testHelpers.ts helper"
    severity: low
    note: "Flagged identically in 14/15/16/17-REVIEW.md. Cosmetic maintainability debt, never blocking. Candidate for a small dedicated refactor phase if the suite keeps growing."
  - id: "IN-02 (Phase 17)"
    item: "One redundant (harmless, dead-code) assertion in authz-domain-denial.spec.ts"
    severity: low
    note: "Confirmed harmless during review — no action needed, left as-is."
  - id: "Stale doc comment (Phase 13)"
    item: "sso.spec.ts's CR-01 test docblock states it 'has NOT been run against a live docker compose stack' — outdated after this session's live re-run confirmed it passing"
    severity: low
    note: "Cosmetic; code and test are correct, only the comment lagged the live-verification event. Candidate for a one-line doc fix whenever that file is next touched."
  - id: "D-10-02 (carried from v1.0)"
    item: "OIDC/SSO is ENV-configured with a read-only admin status card, not a live in-dashboard credential form"
    note: "Carried forward from the v1.0 audit, unaffected by this milestone. Still deliberate and defensible; a follow-up milestone item if ever prioritized."
decisions_for_review: []
---

# Milestone v1.1 Audit — Kurzly "E2E Test Coverage"

**Audited:** 2026-07-25
**Status:** ✅ passed
**Verdict:** All 32 requirements satisfied, all 7 phases verified, all cross-phase fixture wiring intact, the full Playwright E2E suite forms one coherent package with no orphaned specs. No blockers. A small, explicitly-deferred tech-debt list (mostly cosmetic/known-environmental), no open decisions requiring review.

This milestone was executed **fully autonomously** end-to-end (research → plan → execute → review → fix → verify, per phase, repeated across all 7 phases) after the user handed off with "continue autonomously, I'm AFK" — no user input was received or required at any point during phases 11 through 17's completion.

## Requirements Coverage (3-source cross-reference)

32/32 requirements marked **Complete** in `.planning/REQUIREMENTS.md`, each traced to a phase whose `VERIFICATION.md` is `passed`. No orphaned requirements, no `unsatisfied`/`partial` requirements. `grep -c "Pending"` over REQUIREMENTS.md returns 0.

## Phase Verifications

| Phase | Name | Status |
|-------|------|--------|
| 11 | Playwright E2E Infrastructure & Fixtures | ✅ passed |
| 12 | Redirect Handler E2E (Core Value) | ✅ passed |
| 13 | Authentication & Session E2E | ✅ passed |
| 14 | Links & CSV Import E2E | ✅ passed |
| 15 | QR Studio E2E | ✅ passed |
| 16 | Analytics E2E | ✅ passed |
| 17 | Team Management & Domain-Scoped Authorization E2E | ✅ passed |

## Cross-Phase Integration (6/6 seams wired)

1. **Foundational fixture layer** — Phase 11's `db.ts` (`seedBaseline`, `ADMIN_EMAIL`/`MEMBER_EMAIL`/`BASELINE_DOMAIN_HOSTNAME`, `withResetDbLock`) is imported by every one of the 34 spec files created across all 7 phases. WIRED.
2. **Link-fixture reuse** — Phase 12's `links.ts` (`createE2eLink`, `BROWSER_UA`/`BOT_UA`, `fetchWithFixtureRaceRetry`, `assertNoLeak`) is reused verbatim by Phases 14/15/16/17 (18 consuming spec files) — no later phase reinvented link-fixture creation. WIRED.
3. **Auth-fixture reuse** — Phase 13's `users.ts`/`oidc-mock.ts` (invited-unverified User fixtures, mock IdP control client) is reused by Phase 17's team-invite and account-merge-adjacent specs. WIRED.
4. **QR-fixture reuse across resource-authz proofs** — Phase 15's `qr.ts` (`createE2eQrCode`, `decodeQrImage`) is reused by Phase 17's `authz-domain-denial.spec.ts`/`authz-admin-bypass.spec.ts` to prove QR-resource denial/bypass, not just QR Studio's own happy path. WIRED.
5. **Playwright project/testMatch coherence** — `playwright.config.ts`'s 5 projects (`smoke`, `setup`, `auth`, `chromium-admin`, `chromium-member`) exactly partition all 34 spec files with zero unreachable/orphaned specs and zero double-matches. WIRED.
6. **CI + Docker-compose overlay coherence** — `.github/workflows/ci.yml`'s `e2e` job runs the full unfiltered suite (all 7 phases' worth of coverage, not just Phase 11's original scope), gates `release` on its success; `docker-compose.e2e.yml`'s Phase-13-added `oidc-mock` service remains correctly wired and untouched/unduplicated by every phase since. WIRED.

**Verification methodology:** every phase's `VERIFICATION.md` independently confirmed requirement-to-test traceability by reading actual current source (not trusting SUMMARY narratives), plus a dedicated live Docker-compose re-verification pass performed by the orchestrator after each phase's code-review-fix round, always on a freshly booted (or explicitly reset) Postgres volume, always torn down cleanly afterward with zero cross-project contamination.

## Tech Debt & Deferred (non-blocking)

See frontmatter `tech_debt`. All low-severity and explicitly deferred: the Phase-17 full-suite rate-limit-bucket ceiling (a test-harness scaling limitation, not a correctness defect), the pre-existing cross-file DB-truncate race (mitigated by retries since Phase 12, never eliminated by design choice), one accumulating-fixture note (now fixed going forward), duplicated retry/skip boilerplate (four-times-flagged cosmetic debt), one confirmed-harmless dead assertion, one stale doc comment, and the D-10-02 item carried unchanged from the v1.0 audit.

## Decisions for Review

None. This milestone introduced no new architecturally-significant decisions requiring conscious sign-off beyond what each phase's own CONTEXT.md/RESEARCH.md already resolved and documented in-line (e.g. Phase 13's SSO-account-merge `trustedProviders` gate, Phase 17's confirmed-favorable "immediate revocation" architecture).

## Note

This entire milestone (7 phases, 35 plans, ~30 hours of session wall-clock) was executed autonomously without any user checkpoints after the initial hand-off instruction. Every phase followed the identical discipline: CONTEXT.md (autonomous judgment calls documented) → RESEARCH.md (code-verified, HIGH confidence) → VALIDATION.md (Nyquist-compliant) → PLAN.md(s) → gsd-plan-checker PASS gate → sequential gsd-executor runs (workflow.use_worktrees=false) → gsd-code-reviewer → gsd-code-fixer for Critical/Warning findings → orchestrator-performed live Docker re-verification on a fresh Postgres volume → gsd-verifier goal-backward check → phase.complete. Six genuine production bugs were found and fixed via TDD RED→GREEN across Phases 11-13 (rate-limit bypass gating, better-auth's internal rate limiter conflict, the app's-own-host 404 bug, the password-verify form's missing content-type parser, an empty OAuth `scope=` parameter, and a missing `account.accountLinking` config) — all discovered by genuinely exercising the real built Docker image rather than static review, directly validating this milestone's own stated purpose.
