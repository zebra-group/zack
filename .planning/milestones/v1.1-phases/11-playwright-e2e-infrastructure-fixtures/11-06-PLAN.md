---
phase: 11-playwright-e2e-infrastructure-fixtures
plan: 06
type: execute
wave: 5
depends_on: ["11-02", "11-03", "11-04", "11-05"]
files_modified:
  - apps/e2e/tests/smoke/rate-limit-bypass.spec.ts
  - .github/workflows/ci.yml
autonomous: true
requirements: [INFRA-05, INFRA-06]
must_haves:
  truths:
    - "One dedicated E2E spec proves the narrow bypass end-to-end against the built image: a burst WITHOUT the x-e2e-bypass header still trips a real 429, while a burst WITH the correct header all succeeds"
    - "CI runs the Playwright suite as its own e2e job after the existing test and smoke jobs (needs: [test, smoke]) and uploads the HTML report and traces as artifacts only on failure"
    - "The e2e job injects a freshly-generated E2E_RATE_LIMIT_BYPASS_SECRET, never a fixed/committed value"
  artifacts:
    - "apps/e2e/tests/smoke/rate-limit-bypass.spec.ts"
    - ".github/workflows/ci.yml (new e2e job)"
  key_links:
    - "ci.yml e2e job -> ./scripts/e2e-compose.sh (E2E_RATE_LIMIT_BYPASS_SECRET generated per run) -> full Playwright suite; upload-artifact if: failure()"
    - "rate-limit-bypass.spec.ts negative burst (no header) -> real 429; positive burst (correct header) -> all pass"
---

<objective>
Close the phase: prove the rate-limit bypass end-to-end through the running built image (INFRA-06 proof half), and wire the CI `e2e` job that runs the whole green suite after test/build with failure-artifact upload (INFRA-05). This plan runs last because the CI job runs the FULL suite — every fixture and smoke spec from Plans 01–05 must already be green.

Purpose: Plan 02 landed the bypass mechanism with unit tests; this plan proves it survives against the real deployed server (real 429 without the header, all-pass with it) and makes the entire suite a first-class CI gate that surfaces report/trace artifacts when it fails.
Output: `rate-limit-bypass.spec.ts` and the new `e2e` job in `.github/workflows/ci.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-CONTEXT.md
@.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-RESEARCH.md
@.github/workflows/ci.yml
@scripts/e2e-compose.sh
@apps/api/src/plugins/rateLimit.ts
@apps/api/src/routes/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: rate-limit-bypass.spec.ts — real 429 without header, all-pass with header (INFRA-06 E2E proof)</name>
  <files>apps/e2e/tests/smoke/rate-limit-bypass.spec.ts</files>
  <read_first>
    - RESEARCH.md Validation Architecture INFRA-06 row (test.describe.serial; negative burst without header asserts a real 429, positive burst with the correct header asserts all succeed — both halves in one file)
    - RESEARCH.md Pitfall 6 (the proof spec MUST omit the bypass header to observe a real 429 — proves the limiter is not silently defeated)
    - apps/api/src/routes/auth.ts (POST /api/auth/sign-in/magic-link carries MAGIC_LINK_RATE_LIMIT = 5/15min — the tight bucket to exhaust)
    - apps/e2e/src/db.ts (a seeded allowlisted email so the request path is realistic; a non-allowlisted email also returns neutral 200 pre-429, either works)
  </read_first>
  <action>
    Create apps/e2e/tests/smoke/rate-limit-bypass.spec.ts in the `smoke` project using `test.describe.serial` (isolate from parallel-worker interference). Using Playwright's `request` (APIRequestContext): (a) Negative case — send 6 POSTs to `/api/auth/sign-in/magic-link` (body a fixed probe email) WITHOUT any `x-e2e-bypass` header and assert the 6th response status is 429 (the real per-route limit bites against the built image). (b) Positive case — send 6 POSTs to the same endpoint WITH header `x-e2e-bypass: <process.env.E2E_RATE_LIMIT_BYPASS_SECRET>` and assert none return 429 (all excluded). Run the negative case first (or use distinct probe emails / accept that both share the per-IP bucket — structure the serial order so the negative burst's own limit trip is the asserted signal and the positive burst then proves the bypass overrides it). Keep this spec auth-independent (no storageState).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh apps/e2e/tests/smoke/rate-limit-bypass.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `./scripts/e2e-compose.sh apps/e2e/tests/smoke/rate-limit-bypass.spec.ts` exits 0.
    - The spec asserts a real 429 on a burst that omits the `x-e2e-bypass` header (limiter not silently defeated).
    - The spec asserts zero 429s on an equivalent burst that carries the correct `x-e2e-bypass` header/secret.
    - The spec uses `test.describe.serial` and does not depend on any authenticated storageState.
  </acceptance_criteria>
  <done>The narrow bypass is proven end-to-end: unheadered traffic still hits a real 429 while headered traffic runs unthrottled (INFRA-06 success criterion 5, second half).</done>
</task>

<task type="auto">
  <name>Task 2: Add the e2e job to .github/workflows/ci.yml (after test + smoke, artifacts on failure)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (existing `test` and `smoke` jobs — copy their action versions verbatim: actions/checkout@v7, pnpm/action-setup@v6, actions/setup-node@v7 with node-version 24 + cache pnpm; smoke's `docker compose -f docker-compose.yml build app` step is the image-build template)
    - RESEARCH.md CI YAML Sketch (the pasteable e2e job: needs: [test, smoke], install browsers, build image, run ./scripts/e2e-compose.sh, upload report + traces if: failure()) and Pitfall 7 (--disable-dev-shm-usage; do NOT cache ~/.cache/ms-playwright — install fresh each run)
    - RESEARCH.md Security Domain V6 (generate E2E_RATE_LIMIT_BYPASS_SECRET as a real random value per run — e.g. openssl rand -hex 32 — NOT the sketch's non-random github.run_id placeholder)
    - scripts/e2e-compose.sh (the job's single run entrypoint)
  </read_first>
  <action>
    Add a new `e2e` job to .github/workflows/ci.yml with `name: Playwright E2E suite`, `runs-on: ubuntu-latest`, `needs: [test, smoke]`. Steps (reusing the existing jobs' exact action versions): checkout@v7; pnpm/action-setup@v6; setup-node@v7 (node-version 24, cache pnpm); `pnpm install --frozen-lockfile`; install browsers via `pnpm --filter @kurzly/e2e exec playwright install --with-deps chromium` (fresh each run — do NOT add a ms-playwright cache step); build the app image via `docker compose -f docker-compose.yml build app` (mirror smoke; optionally add BuildKit GHA cache but not required); then a "Run Playwright E2E suite" step running `./scripts/e2e-compose.sh` with `env: E2E_RATE_LIMIT_BYPASS_SECRET: ${{ ... }}` set to a freshly-generated random value per run (generate with `openssl rand -hex 32` in a preceding step exposing it via `$GITHUB_OUTPUT`/`$GITHUB_ENV`, or inline — never a fixed/committed literal and never the plain github.run_id). Add two upload steps with `if: failure()` using `actions/upload-artifact@v4`: one for `apps/e2e/playwright-report/` (name playwright-report) and one for `apps/e2e/test-results/` (name playwright-traces), each with a short retention. Do NOT modify the existing `test`, `smoke`, or `release` jobs; the `release` job's `needs: [test, smoke]` stays as-is (do not add `e2e` to it unless that is an explicit later decision).
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/ci.yml')); j=d['jobs']['e2e']; assert j['needs']==['test','smoke'], j['needs']; steps=[s for s in j['steps']]; assert any('e2e-compose.sh' in (s.get('run','')) for s in steps), 'missing e2e-compose.sh run'; assert any(s.get('if')=='failure()' and 'upload-artifact' in str(s.get('uses','')) for s in steps), 'missing failure artifact upload'; print('e2e job OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` defines an `e2e` job with `needs: [test, smoke]`.
    - The job runs `./scripts/e2e-compose.sh` and sets `E2E_RATE_LIMIT_BYPASS_SECRET` to a per-run generated value (not a committed literal, not bare github.run_id).
    - Two `actions/upload-artifact@v4` steps with `if: failure()` upload `apps/e2e/playwright-report/` and `apps/e2e/test-results/`.
    - Playwright browsers are installed fresh (no ms-playwright cache step); the existing test/smoke/release jobs are unchanged.
    - The workflow file is valid YAML (the verify command parses it and asserts the job shape).
  </acceptance_criteria>
  <done>CI runs the Playwright suite as its own job after test/build and uploads report/trace artifacts on failure (INFRA-05 success criterion 5, first half).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI env -> compose overlay | The `e2e` job injects `E2E_RATE_LIMIT_BYPASS_SECRET` into the run |
| public magic-link endpoint -> rate limiter | The proof spec exercises the real 429 path against the built image |
| CI artifacts -> retention storage | Report/trace uploads may contain request/response detail |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Elevation of Privilege | `E2E_RATE_LIMIT_BYPASS_SECRET` in CI | high | mitigate | Generated fresh per run (`openssl rand -hex 32`), scoped to the `e2e` job's env only; never committed, never a fixed value, never added to the prod compose or `.env.example`. The negative-case spec proves an unheadered request still gets a real 429, so a leaked secret cannot silently weaken the production limiter. |
| T-11-09 | Information Disclosure | uploaded playwright-report/traces | medium | mitigate | Artifacts uploaded only `if: failure()`, with short retention; the suite uses synthetic seeded test data (no real user PII), and storageState credential files are gitignored/not part of the report bundle. |
| T-11-10 | Denial of Service | e2e job flakiness/resource limits on CI runner | low | mitigate | Chromium runs with `--disable-dev-shm-usage`; browsers installed fresh (no stale ms-playwright cache); the script's trap always tears the stack down even on failure. |
</threat_model>

## Artifacts this plan produces

- `apps/e2e/tests/smoke/rate-limit-bypass.spec.ts` — end-to-end bypass proof (real 429 without header; all-pass with header).
- `.github/workflows/ci.yml` `e2e` job — `needs: [test, smoke]`; installs browsers, builds the app image, runs `./scripts/e2e-compose.sh` with a per-run generated `E2E_RATE_LIMIT_BYPASS_SECRET`, uploads `playwright-report/` + `test-results/` on failure.

<verification>
- `./scripts/e2e-compose.sh apps/e2e/tests/smoke/rate-limit-bypass.spec.ts` exits 0 (real 429 without header, all-pass with header).
- The full suite via `./scripts/e2e-compose.sh` (all specs from Plans 01–05 + this) is green before the CI job is considered done.
- `.github/workflows/ci.yml` parses as valid YAML with an `e2e` job (`needs: [test, smoke]`) that runs the script and uploads artifacts `if: failure()`.
</verification>

<success_criteria>
- CI runs the Playwright suite as its own job after test/build, uploading report/trace artifacts on failure, and one dedicated spec still trips a real 429 while the rest of the suite runs unthrottled via the narrow bypass (INFRA-05 + INFRA-06 success criterion 5).
- The bypass secret is generated per run and confined to the e2e job.
</success_criteria>

<output>
Create `.planning/phases/11-playwright-e2e-infrastructure-fixtures/11-06-SUMMARY.md` when done. Note the one-time rollout check (deliberately break an assertion to confirm the failure artifact appears in the run's Summary page) if performed.
</output>
