---
phase: 14-links-csv-import-e2e
plan: 04
type: execute
wave: 1
depends_on: [14-01]
files_modified:
  - apps/e2e/tests/authed/csv-import-conflict.spec.ts
autonomous: true
requirements: [LINKS-E2E-03]

must_haves:
  truths:
    - "A CSV row whose slug already exists is surfaced in the preview as INVALID with reason slug_conflict (label `Slug bereits vergeben oder reserviert`), while the other, new-slug row previews valid — `1 gültig · 1 übersprungen`"
    - "Commit imports only the valid row (importedCount 1, skippedCount 1) and SKIPS the conflict — the pre-existing link's target URL is UNCHANGED in the database afterward (skip, never overwrite — no overwrite path exists in the code)"
  artifacts:
    - apps/e2e/tests/authed/csv-import-conflict.spec.ts
  key_links:
    - "the conflicting link is pre-seeded via createE2eLink with a DISTINCT pre-existing target; after commit the DB assertion proves that exact pre-existing target survives — the definitive skip-not-overwrite proof (14-RESEARCH.md Summary point 2)"
    - "conflict-slug and new-slug are per-test random and the pre-existing fixture is created INSIDE the test body, so a full-suite truncate-race retry re-seeds cleanly; default domain selected before upload (Pitfall 3)"
---

<objective>
Prove the CSV slug-conflict path (LINKS-E2E-03) end-to-end: a CSV whose slug already exists surfaces the conflict in the preview, and commit SKIPS that row — the pre-existing link's target is left untouched. 14-RESEARCH.md closed CONTEXT.md's open question with certainty: conflict resolution is SKIP-ONLY; there is no overwrite path anywhere in routes/links.ts, lib/links.ts, or LinksImportView.vue. This spec therefore asserts skip behaviour exclusively — the definitive proof being that the pre-existing link's target is unchanged after commit (an import can never silently re-point an existing short link).

This spec asserts REAL, currently-passing behaviour. If a genuine app-code gap surfaces, STOP and flag it — do not build a test double for an overwrite mode that does not exist (14-RESEARCH.md anti-pattern).

Purpose: Close LINKS-E2E-03 with a preview-surfaces-conflict → commit-skips flow, DB-asserted as skip-not-overwrite.
Output: `apps/e2e/tests/authed/csv-import-conflict.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-links-csv-import-e2e/14-RESEARCH.md
@apps/web/src/views/LinksImportView.vue
@packages/shared/src/index.ts
@apps/e2e/src/csv.ts
@apps/e2e/src/links.ts
@apps/e2e/src/db.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: LINKS-E2E-03 — CSV slug conflict previews + commit skips, DB-asserted skip-not-overwrite (chromium-admin)</name>
  <read_first>
    - apps/web/src/views/LinksImportView.vue (VERIFIED: SKIP_REASON_LABELS maps slug_conflict → `Slug bereits vergeben oder reserviert`; an invalid preview row gets the `.preview-row.invalid` class and shows the reason label in `.preview-reason` plus a `✕` icon; `.preview-summary` shows `.valid-count` `N gültig` and, when skippedCount>0, `.skipped-count` `N übersprungen`; commit button `Importieren (N)` reflects validCount)
    - packages/shared/src/index.ts (LinkSkipReason includes `slug_conflict`; ImportRowResult has valid/reason/zielUrl/slug — if inspecting the preview response, type it as ImportPreviewResult; ImportCommitResult importedCount/skippedCount)
    - apps/e2e/src/links.ts (createE2eLink — pre-seed the conflicting Link via direct Prisma; 14-RESEARCH.md Pattern 2 explicitly permits the conflict spec to seed its pre-existing link via createE2eLink rather than the create-UI, since this spec's subject is the import flow, not link creation)
    - apps/e2e/src/csv.ts (buildImportCsv from 14-01)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME)
    - lib/links.ts behaviour (via 14-RESEARCH.md Summary point 2): mapErrorToSkipReason maps SLUG_TAKEN → the `slug_conflict` LinkSkipReason; the row is marked valid:false and never written, on BOTH preview and commit — skip-only, no overwrite
    - 14-RESEARCH.md (Pattern 2 forcing a conflict; Pitfall 3 select-domain-before-upload; anti-patterns: no overwrite mode, no preview id)
  </read_first>
  <files>apps/e2e/tests/authed/csv-import-conflict.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` (documented db-isolation truncate-race resilience) and a `test.beforeEach` skip so the spec runs only under chromium-admin (member/domain-scoped import authz is Phase 17), since testMatch registers every tests/authed spec under both authenticated projects.

    Single test "CSV slug conflict is surfaced in preview and skipped on commit (never overwritten)":
    1. Mint per-test random valid-shape slugs: a conflict slug `e2e-conflict-<hex>` and a new slug `e2e-new-<hex>`. Pick a DISTINCT pre-existing target `https://example.com/pre-existing-<hex>` and a DIFFERENT CSV target for the conflict row `https://example.com/csv-attempt-<hex>` (so overwrite-vs-skip is detectable), plus a target for the new row `https://example.com/new-<hex>`.
    2. INSIDE the test body (so retries re-seed): createE2ePrisma(); createE2eLink with the conflict slug and the pre-existing target on the baseline domain. (Creating the fixture in the body — not a beforeAll — means a full-suite truncate between seed and preview is recovered by the whole-test retry with a fresh random slug.)
    3. Build the CSV via buildImportCsv of TWO rows, blank domain both: row 1 = conflict slug with the CSV-attempt target; row 2 = the new slug with the new target.
    4. page.goto("/links/import"); expect URL "/links/import". Select the baseline domain in the default-domain dropdown FIRST (before upload — Pitfall 3).
    5. setInputFiles the built CSV on `input[type="file"]`; waitForResponse the POST /api/links/import/preview.
    6. PREVIEW assertions: `.valid-count` shows `1 gültig` and `.skipped-count` shows `1 übersprungen`; the conflict row renders with `.preview-row.invalid` and its `.preview-reason` text is `Slug bereits vergeben oder reserviert`; the new-slug row renders valid (no invalid class / `✓`). (Optionally assert the preview response rows: the conflict row valid:false reason:"slug_conflict", the new row valid:true.)
    7. COMMIT: click `Importieren (1)`; waitForResponse the POST /api/links/import/commit. Assert the response importedCount is 1 and skippedCount is 1.
    8. DB assertion (skip-not-overwrite — the crux):
       - prisma.link.findMany where slug === conflict slug → length exactly 1, and that row's targetUrl equals the PRE-EXISTING target (NOT the CSV-attempt target) — proving the conflict row was skipped, never overwritten.
       - prisma.link.findFirst where slug === new slug → exists, targetUrl equals the new target (the one valid row was imported).
       - $disconnect in a finally.

    Do NOT assert or build for any overwrite behaviour — it does not exist (14-RESEARCH.md). Do NOT reuse a preview id on commit — none exists; commit re-sends the CSV text.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/csv-import-conflict.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the pre-existing conflicting link is seeded (createE2eLink) inside the test body with a distinct pre-existing target
    - preview asserts `1 gültig · 1 übersprungen` with the conflict row invalid + reason `Slug bereits vergeben oder reserviert`
    - commit asserts importedCount 1 / skippedCount 1
    - the DB assertion proves the conflict slug still has exactly one row whose target is the UNCHANGED pre-existing target (skip, not overwrite), and the new slug was imported
    - scoped to chromium-admin, per-test random slugs, default domain selected before upload; passes green; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>LINKS-E2E-03 proves a CSV slug conflict is surfaced in the preview and skipped on commit — the pre-existing link is never overwritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) → /api/links/import/* | untrusted CSV text crosses into the bulk-import parser/validator |
| test runner → E2E Postgres :5433 | pre-existing conflict-link seed + post-commit target-unchanged assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-14-06 | Tampering | a CSV import overwriting an existing slug's target (redirect hijack) | high | mitigate | LINKS-E2E-03 proves the conflict row is SKIPPED and the pre-existing link's target is unchanged after commit — an import can never silently re-point an existing short link (no overwrite path exists in the code) |
| T-14-07 | Elevation of Privilege | a crafted CSV `domain` column bypassing domain authorization | low | accept | Every row passes requireDomainAccess inside runImport (server-side); the representative E2E denial case is Phase 17 (AUTHZ-E2E-01) and the exhaustive matrix is the existing v1.0 integration Denial-Suite — this spec uses the baseline domain only |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/csv-import-conflict.spec.ts --project=chromium-admin` passes.
- Re-run at `--workers=1` and default parallelism; the pre-existing-target-unchanged assertion is the decisive skip-not-overwrite proof and is scoped to the per-test conflict slug.
</verification>

<success_criteria>
LINKS-E2E-03 passes: a slug conflict is surfaced in the preview and skipped on commit, with the pre-existing link's target proven unchanged at the database.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/csv-import-conflict.spec.ts` — LINKS-E2E-03 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/14-links-csv-import-e2e/14-04-SUMMARY.md` when done.
</output>
