---
phase: 17-team-management-domain-scoped-authz-e2e
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/authz-admin-bypass.spec.ts
autonomous: true
requirements: [AUTHZ-E2E-02]

must_haves:
  truths:
    - "A direct-Prisma precondition read confirms the seeded account-admin (ADMIN_EMAIL, accountRole 'admin') holds ZERO DomainMembership rows — prisma.domainMembership.count({ where: { userId: adminId } }) === 0 — making 'never explicitly assigned' an explicit, self-documenting assertion, not an implicit fact"
    - "A Link (and a bound QR) created on a FRESH, per-test domain the admin was NEVER assigned (a new prisma.domain.create hostname, referenced by no DomainMembership for anyone) is reachable by the admin through the real UI: page.goto('/links/:id') renders the link detail (`.link-slug` = /slug, `.not-found-card` ABSENT)"
    - "The admin's API access to the never-assigned domain's resources succeeds: page.request.get('/api/links/:id') and page.request.get('/api/qr-codes/:id') both return HTTP 200 — proving isAccountAdmin short-circuits requireDomainAccess/scopedDomainIds before any DomainMembership lookup (the intentional bypass, not a bug)"
  artifacts:
    - apps/e2e/tests/authed/authz-admin-bypass.spec.ts
  key_links:
    - "the bypass uses the EXISTING chromium-admin storageState (the seeded admin with zero DomainMembership) — no new admin fixture; the 'never assigned' precondition is made assertable via the explicit domainMembership.count === 0 read"
    - "the resource lives on a SECOND, freshly-seeded per-test domain (never referenced by any DomainMembership for the admin OR anyone) so there is zero doubt the baseline domain isn't implicitly admin-owned by convention (17-RESEARCH.md Pattern 5 / Assumption A2 — the stronger story)"
    - "the proof is admin success + zero-membership precondition; a domain-scoped member would 404 on the same id (AUTHZ-E2E-01's job), so the ONLY differentiator here is accountRole === 'admin'"
---

<objective>
Prove AUTHZ-E2E-02 through the real UI/API: an account-admin reaches a domain never explicitly assigned to them, demonstrating the INTENTIONAL admin-bypass mechanism (not a bug). 17-RESEARCH.md confirms — from `lib/authorization.ts` — that `requireDomainAccess`/`scopedDomainIds` both start with an `isAccountAdmin` short-circuit before any `DomainMembership` lookup, and the EXISTING seeded admin fixture already has zero `DomainMembership` rows, so the `chromium-admin` storageState is itself a live instance of the bypass. This spec makes the "never explicitly assigned" precondition assertable (an explicit `domainMembership.count === 0` read) and, for the strongest story (Pattern 5 / Assumption A2), targets a resource on a freshly-seeded second domain referenced by no membership for anyone — removing any doubt that the baseline domain is implicitly admin-owned. The admin reaching that resource's detail page + a 200 API response is the bypass proof; a domain-scoped member would 404 on the same id (AUTHZ-E2E-01), so the only differentiator is `accountRole === "admin"`.

This spec asserts REAL, currently-passing behaviour — `lib/authorization.ts`'s bypass, `routes/links.ts`/`routes/qrCodes.ts`, `LinkDetailView.vue`, and `db.ts`'s seedBaseline (admin has no membership) were all read in full during research and need no changes. If a genuine app-code gap surfaces (e.g. the admin is unexpectedly denied, or the precondition read shows a membership that shouldn't exist), STOP and flag it as a task; do NOT weaken an assertion (Phase 11-16 TDD RED->GREEN precedent). No production code change is anticipated.

Purpose: Close AUTHZ-E2E-02 with an explicit zero-membership precondition + a real admin reach into a never-assigned domain's Link/QR.
Output: `apps/e2e/tests/authed/authz-admin-bypass.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-team-management-domain-scoped-authz-e2e/17-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/src/qr.ts
@apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTHZ-E2E-02 — account-admin reaches a never-assigned domain's resource (chromium-admin)</name>
  <read_first>
    - 17-RESEARCH.md (Pattern 5 "the existing admin fixture already IS the bypass proof" — the explicit domainMembership.count === 0 precondition + the optional second-domain story; Assumption A2 "a second dedicated domain removes any doubt"; Code Examples "admin bypass short-circuit" and "seeded admin gets NO DomainMembership row")
    - apps/api/src/lib/authorization.ts (VERIFIED: requireDomainAccess and scopedDomainIds both begin with `if (await isAccountAdmin(prisma, userId)) return / return all domain ids` — the bypass is checked FIRST, before any DomainMembership lookup)
    - apps/e2e/src/db.ts (VERIFIED: seedBaseline creates ADMIN_EMAIL with accountRole "admin" and NO domainMembership.upsert for the admin; BASELINE_DOMAIN_HOSTNAME; createE2ePrisma; withResetDbLock never truncates Domain, so a per-test unique domain hostname is collision-free across runs — but do NOT truncate/seed via withResetDbLock here, just additive prisma.domain.create)
    - apps/web/src/views/LinkDetailView.vue (VERIFIED: a reachable link renders `.link-slug` = /{{slug}} with NO `.not-found-card`; a 404 renders `.not-found-card`)
    - apps/e2e/src/links.ts (createE2eLink — accepts a `domainHostname` option to place the Link on the fresh second domain; createdBy resolves to ADMIN_EMAIL, which is irrelevant to the access decision — access is purely domain-scope-based, so this still proves the bypass, not creator-ownership)
    - apps/e2e/src/qr.ts (createE2eQrCode — a QR bound to the second-domain Link; capture its id for the 200 API assertion)
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts (retries:2 + chromium-admin-only test.skip + testInfo.retry attribution console.warn shape)
  </read_first>
  <files>apps/e2e/tests/authed/authz-admin-bypass.spec.ts</files>
  <action>
    Wrap in a single `test.describe` with `test.describe.configure({ retries: 2 })` (doc-comment mirroring qr-dynamic-remap: the fresh domain + Link/QR fixtures straddle the precondition read and the navigation; a whole-test retry with fresh per-test unique hostname/slug is the collision-free equivalent). `test.beforeEach` `test.skip`s when `testInfo.project.name !== "chromium-admin"` (reason: the account-admin bypass is the subject; this spec uses the chromium-admin storageState `page` and would be meaningless under chromium-member) and emits the testInfo.retry attribution console.warn.

    Single test "an account-admin with zero DomainMembership rows reaches a never-assigned domain's Link and QR":

    SETUP (createE2ePrisma in a try/finally that always $disconnects; capture ids):
    - Resolve the seeded admin id: `const admin = await prisma.user.findUniqueOrThrow({ where: { email: ADMIN_EMAIL } })`.
    - PRECONDITION assertion (makes "never explicitly assigned" explicit): `expect(await prisma.domainMembership.count({ where: { userId: admin.id } })).toBe(0)`. If this is ever non-zero, it is a real regression in seedBaseline (or a leaked membership) — the spec should fail loudly here, exactly as intended.
    - Seed a FRESH second domain never referenced by any DomainMembership: mint a per-test unique hostname `bypass-<hex>.kurzly.local`; `const bypassDomain = await prisma.domain.create({ data: { hostname, type: "subdomain", status: "active", verificationTarget: hostname } })` (mirror seedBaseline's Domain shape). Do NOT create any DomainMembership for it, for the admin or anyone.
    - `createE2eLink(prisma, { slug: 'e2e-bypass-' + hex, targetUrl, domainHostname: hostname })` on the fresh domain. Capture linkId.
    - `createE2eQrCode(prisma, { variant: "static", linkId, name: 'bypass-qr-' + hex })`. Capture qrId.

    UI BYPASS PROOF (the existing chromium-admin `page`, already authenticated as the zero-membership admin via storageState):
    - `await page.goto('/links/' + linkId)`; assert the link detail rendered — `.link-slug` has text "/" + slug — and `.not-found-card` is NOT visible. The admin reaches a domain-scoped resource on a domain they were never assigned.

    API BYPASS PROOF (real cookie jar via page.request):
    - `expect((await page.request.get('/api/links/' + linkId)).status()).toBe(200)` and `expect((await page.request.get('/api/qr-codes/' + qrId)).status()).toBe(200)` — confirming isAccountAdmin short-circuits the domain-scope check at the API layer for multiple resource types on the never-assigned domain.

    Do NOT assign the admin any DomainMembership (that would destroy the precondition). Do NOT mutate the seeded admin's accountRole. The fresh domain uses a per-test unique hostname so repeated runs never collide (Domain is not truncated). No fixed waitForTimeout — locator auto-wait + awaited API reads cover sequencing.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/authz-admin-bypass.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - an explicit precondition asserts prisma.domainMembership.count for the seeded admin id === 0 (the "never assigned" fact is proven, not assumed)
    - the resource lives on a FRESH per-test second domain referenced by no DomainMembership; the admin reaches /links/:id (link detail rendered, `.not-found-card` absent) via the existing chromium-admin storageState
    - page.request.get('/api/links/:id') and page.request.get('/api/qr-codes/:id') both return 200, from the admin's real cookie jar
    - no DomainMembership is created for the admin; the seeded admin's role is untouched; per-test unique hostname/slug + retries:2; scoped to chromium-admin (skipped under chromium-member)
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>AUTHZ-E2E-02 proves an account-admin holding zero DomainMembership rows reaches a never-assigned domain's Link (UI detail) and Link/QR (200 API) — the intentional isAccountAdmin bypass, with the zero-membership precondition asserted explicitly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser (zero DomainMembership) -> GET /links/:id, /api/links/:id, /api/qr-codes/:id | the bypass boundary — an account-admin must reach a domain-scoped resource on a domain they were never assigned |
| test runner -> E2E Postgres :5433 | direct-Prisma zero-membership precondition read + fresh second-domain seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-05-BYPASS-REGRESS | Elevation of Privilege / Denial of Service | isAccountAdmin short-circuit in requireDomainAccess/scopedDomainIds | high | mitigate | The explicit zero-membership precondition + successful access makes a regression in EITHER direction test-visible: if the bypass silently became membership-required, the admin would 404 (legit-access DoS); if a member row silently gained bypass, the precondition count would be non-zero — both fail the spec |
| T-17-05-IMPLICIT | Information disclosure | assuming the baseline domain is "implicitly admin-owned" | low | accept | Mitigated by targeting a FRESH second domain referenced by no membership (Pattern 5), removing any doubt the bypass is convention-based rather than a real accountRole check |
| T-17-05-CREATOR | Elevation of Privilege | createE2eLink sets createdBy = admin | low | accept | The access decision (resolveOwnedLink) filters by domain scope, never createdBy — so admin-as-creator is not the reason access succeeds; the bypass is proven by the accountRole path, and a member would 404 on the same id (AUTHZ-E2E-01) |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/authz-admin-bypass.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism — confirm the fresh-domain seeding + precondition read are stable and the seeded admin's zero-membership invariant holds across concurrent specs, beyond what retries:2 absorbs.
</verification>

<success_criteria>
AUTHZ-E2E-02 passes: an account-admin holding zero DomainMembership rows reaches a never-assigned domain's Link (UI detail) and Link/QR (200 API), with the zero-membership precondition asserted explicitly — proving the intentional bypass.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/authz-admin-bypass.spec.ts` — AUTHZ-E2E-02 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/17-team-management-domain-scoped-authz-e2e/17-05-SUMMARY.md` when done.
</output>
</content>
