---
phase: 17-team-management-domain-scoped-authz-e2e
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/team-role-domain-reassign.spec.ts
autonomous: true
requirements: [TEAM-E2E-02]

must_haves:
  truths:
    - "A brand-new active member (createAllowlistedUser, emailVerified:true, zero DomainMembership rows, per-test crypto-unique email) with its OWN real session established inline in a SECOND browser context (magic-link round trip, NOT the seeded chromium-member storageState) initially sees a baseline-domain Link at /links/:id as `.not-found-card` (404, no domain scope) — the BEFORE state"
    - "The admin (chromium-admin `page`, context A) assigning that member the baseline domain through the REAL AssignDomainsModal (`.assign-pill` -> `.domain-pill` -> `.btn-primary` 'Speichern', PUT /api/team/:id/domains) writes DomainMembership without touching the member's Session"
    - "The member's ALREADY-OPEN context B, RE-navigating /links/:id with NO re-login (page.reload/goto), now renders the link detail (`.link-slug` = /slug, `.not-found-card` absent) — proving scopedDomainIds is re-derived from Postgres on the very next request (no cookieCache), the AFTER state"
    - "A subsequent admin role promotion member->admin via the roster `.role-select` (PATCH /api/team/:id/role) makes the member's already-open context B reach /team on its next navigation (GET /api/team returns 200, not the /login or /dashboard route-guard bounce) — proving accountRole is also re-derived per-request"
  artifacts:
    - apps/e2e/tests/authed/team-role-domain-reassign.spec.ts
  key_links:
    - "the AFTER proof is a specific, resource-scoped /links/:id 404 -> detail transition on the member's OWN already-open context, NEVER a LinksView `.empty-state` check (ambiguous, 17-RESEARCH.md Anti-Patterns) and NEVER a member logout/re-login (would trivially prove a fresh login, not the interesting per-request re-derivation)"
    - "context B holds the member's session cookie jar throughout — the reassignment is observed by re-navigating THAT context, never by extracting/re-injecting cookies or by reading the admin context"
    - "the target is the NEWLY created member, never the seeded MEMBER_EMAIL/ADMIN_EMAIL (Pitfall 3 — mutating a baseline fixture would break other specs' storageState dependencies); the fixture Link is created via createE2eLink on BASELINE_DOMAIN_HOSTNAME with a per-test random slug"
---

<objective>
Prove TEAM-E2E-02 end-to-end through two real browser contexts: an admin's role/domain reassignment takes real effect in the affected member's OWN already-open, never-re-authenticated session on its very next navigation. 17-RESEARCH.md confirms — from direct source reads of `createAuth()` (no `cookieCache`) and `lib/authorization.ts` (`scopedDomainIds`/`accountRole` re-derived from Postgres on every request) — that a page reload/re-navigation is SUFFICIENT and is exactly what the spec must exercise; no re-login, no token refresh. The core proof is a concrete, resource-scoped transition: a baseline-domain `/links/:id` that renders `.not-found-card` (404) BEFORE the member has the domain, and the full link detail AFTER the admin assigns it — observed on the member's own context. A second part promotes the member to admin and proves the accountRole re-derivation by the member's context reaching /team on its next navigation.

This spec asserts REAL, currently-passing behaviour — `routes/team.ts`, `lib/team.ts`, `lib/authorization.ts`, `TeamView.vue`, `AssignDomainsModal.vue`, `LinkDetailView.vue`, and the router guard were all read in full during research and need no changes. If a genuine app-code gap surfaces (an unexpected RED), STOP and flag it as a task; do NOT weaken an assertion (Phase 11-16 TDD RED->GREEN precedent). No production code change is anticipated.

Purpose: Close TEAM-E2E-02 with a two-context proof that domain AND role reassignment reach an already-open member session with no re-login.
Output: `apps/e2e/tests/authed/team-role-domain-reassign.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-team-management-domain-scoped-authz-e2e/17-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/users.ts
@apps/e2e/src/links.ts
@apps/e2e/src/mailpit.ts
@apps/e2e/tests/auth/logout-route-guard.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEAM-E2E-02 — role/domain reassignment reaches the member's own re-navigated session (two contexts, chromium-admin)</name>
  <read_first>
    - 17-RESEARCH.md (Pattern 2 "no cookieCache means DB is the single source of truth every request" — the exact reload-is-sufficient design and why; Anti-Patterns "asserting via empty list" and "testing by logout/re-login"; Pitfall 3 last-admin/baseline-fixture guard)
    - apps/web/src/views/TeamView.vue (VERIFIED selectors: a member row's domain cell renders `.domain-chip`(s) + an `.assign-pill` text "+ zuweisen" that opens AssignDomainsModal for a `"member"` row only; the role `<select>` is `.role-select` with options "Admin"/"Mitglied"; rows are `.table-row` with `.user-email`)
    - apps/web/src/components/AssignDomainsModal.vue (VERIFIED selectors: domain toggles are `.domain-pill` (add `.selected` when chosen); save is `.btn-primary` text "Speichern")
    - apps/web/src/views/LinkDetailView.vue (VERIFIED: a 404 renders `.not-found-card` with heading "Link nicht gefunden"; a scoped-in link renders `.link-slug` = /{{slug}} and NO `.not-found-card`; load() runs once on mount, so a re-navigation/reload re-fetches)
    - apps/api/src/routes/team.ts (VERIFIED endpoints: PUT /api/team/:id/domains replaces the member's domain set; PATCH /api/team/:id/role promotes/demotes — both admin-gated, 200 on success)
    - apps/e2e/src/users.ts (createAllowlistedUser — emailVerified:true, accountRole "member", zero DomainMembership — exactly the fresh active member this needs; per-test unique email required, User rows are not truncated)
    - apps/e2e/src/links.ts (createE2eLink — baseline-domain Link fixture with a per-test random slug; used as the specific resource whose visibility flips)
    - apps/e2e/src/mailpit.ts (findMagicLinkUrl — to establish the member's own real session in context B)
    - apps/e2e/tests/auth/logout-route-guard.spec.ts (the reference for browser.newContext({ baseURL }) + inline magic-link session establishment with the INFRA-06 x-e2e-bypass header + resolvedBaseUrl fallback + finally-close)
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts (retries:2 + chromium-admin-only test.skip + testInfo.retry attribution console.warn shape)
  </read_first>
  <files>apps/e2e/tests/authed/team-role-domain-reassign.spec.ts</files>
  <action>
    Wrap in a single `test.describe` with `test.describe.configure({ retries: 2 })` (doc-comment mirroring qr-dynamic-remap: fixtures + a fixture Link straddle multiple steps outside withResetDbLock, so a whole-test retry with fresh per-test unique email/slug is the collision-free equivalent). `test.beforeEach` `test.skip`s when `testInfo.project.name !== "chromium-admin"` (the admin drives the reassignment UI; the member's session is a separately-established context, not the baseline chromium-member storageState) and emits the testInfo.retry attribution console.warn.

    Single test "admin's domain then role reassignment takes effect in the member's own re-navigated session":

    SETUP (createE2ePrisma in a try/finally that always $disconnects; capture the returned member id):
    - Mint per-test unique member email `reassign-<hex>@e2e.kurzly.local` and a per-test random link slug `e2e-reassign-<hex>`.
    - `createAllowlistedUser(prisma, { email })` (active, zero domains). Capture memberId.
    - `createE2eLink(prisma, { slug, targetUrl })` on BASELINE_DOMAIN_HOSTNAME. Capture linkId.
    - Establish the member's OWN real session in context B: POST /api/auth/sign-in/magic-link for the member email WITH the INFRA-06 x-e2e-bypass header (via the top-level `request` fixture — the verify GET, not this POST, sets the cookie), assert ok(); `findMagicLinkUrl(memberEmail)`; `const memberCtx = await browser.newContext({ baseURL: resolvedBaseUrl })`, `const memberPage = await memberCtx.newPage()`, `await memberPage.goto(magicLinkUrl)`, wait for the "Dashboard" nav link. Wrap all context-B usage so memberCtx is closed in a finally.

    PART A — domain reassignment (the core proof):
    1. BEFORE: `await memberPage.goto('/links/' + linkId)` and assert `.not-found-card` is visible (heading "Link nicht gefunden") — the zero-domain member cannot see the baseline link (404 IDOR shape).
    2. As the admin `page` (context A): `await page.goto('/team')`, locate the member's `.table-row` by `.user-email` hasText memberEmail, click its `.assign-pill` ("+ zuweisen"). In AssignDomainsModal, click the baseline `.domain-pill` (the one whose text is BASELINE_DOMAIN_HOSTNAME) to select it, then click `.btn-primary` ("Speichern"). Await the PUT /api/team/:id/domains response ok()===true (page.waitForResponse keyed on PUT + pathname /api/team/<memberId>/domains). Assert the member row now shows a `.domain-chip` with the baseline hostname.
    3. AFTER: on the SAME memberPage (no re-login), `await memberPage.goto('/links/' + linkId)` (a fresh navigation re-runs load()); assert `.not-found-card` is now ABSENT and the link detail rendered (`.link-slug` has text "/" + slug). This is the reassignment reaching the already-open member session on its next request.

    PART B — role promotion (accountRole re-derivation):
    4. As the admin `page`: on /team, on the member row select `.role-select` value "admin" (selectOption "admin"); await the PATCH /api/team/:id/role response ok()===true. (Promote-clears domains per D-09-05, but admin bypass grants all-domain access anyway.)
    5. On the SAME memberPage (still no re-login), navigate `await memberPage.goto('/team')`; assert it does NOT bounce to /login or /dashboard (the requiresAdmin guard now admits it) — e.g. expect the Team header ("Team" / the roster `.table-header`) to be visible, and independently `expect((await memberPage.request.get('/api/team')).ok()).toBeTruthy()` (200, admin-gated endpoint) — proving accountRole is re-derived per-request for the already-open session.

    Do NOT test the reassignment via a member logout/re-login (Anti-Pattern — trivially true) and do NOT use a LinksView empty-list as the domain proof (ambiguous). Never mutate the seeded ADMIN_EMAIL/MEMBER_EMAIL. No fixed waitForTimeout — awaited PUT/PATCH responses + locator auto-wait cover sequencing.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/team-role-domain-reassign.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the member has its OWN real session established inline in a second browser.newContext via a magic-link round trip (never the chromium-member storageState, never a cookie-injection trick)
    - PART A proves a specific baseline-domain /links/:id transitions from `.not-found-card` (before) to the rendered detail (after) on the member's own already-open context, driven by a real admin AssignDomainsModal save (PUT /api/team/:id/domains asserted ok), with NO member re-login
    - PART B proves the member's already-open context reaches /team + GET /api/team 200 after a real admin `.role-select` promotion (PATCH /api/team/:id/role asserted ok)
    - the target is the newly created member (never a seeded baseline fixture); per-test unique email + random slug + retries:2; scoped to chromium-admin (skipped under chromium-member)
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>TEAM-E2E-02 proves an admin's domain assignment and role promotion both take effect in the affected member's own re-navigated session with no re-login — via a concrete /links/:id 404->detail transition and a /team reachability flip.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser -> PUT /api/team/:id/domains, PATCH /api/team/:id/role | account-admin-gated reassignment writes (DomainMembership / accountRole) |
| member browser (context B) -> GET /api/links/:id, GET /api/team | per-request session + scopedDomainIds re-derivation is the access boundary being proven |
| test runner -> E2E Postgres :5433 | direct-Prisma fixture creation (member, link) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-02-STALESCOPE | Elevation of Privilege | scopedDomainIds re-read on every request (no cookieCache) | high | mitigate | This spec PROVES the property directly: a member's own next request picks up the new scope with no re-login (Pattern 2); a regression that cached scope in the cookie would fail the AFTER assertion |
| T-17-02-IDOR | Information disclosure | resolveOwnedLink 404-for-both on /links/:id | low | accept | Exercised as the BEFORE state (zero-domain member gets 404 on a baseline link); the full IDOR matrix is AUTHZ-E2E-01 + the v1.0 integration Denial-Suite, not re-proven here |
| T-17-02-LOCKOUT | Denial of Service | changeMemberRole LAST_ADMIN guard | low | accept | The promotion targets a NEWLY created member and only promotes (never demotes the sole admin), so the guard is never tripped (Pitfall 3) — the seeded fixtures other specs depend on are untouched |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/team-role-domain-reassign.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism — confirm the two-context handoff (admin write -> member re-navigation) is deterministic (awaited PUT/PATCH responses) and no baseline-fixture interference occurs, beyond what retries:2 absorbs.
</verification>

<success_criteria>
TEAM-E2E-02 passes: an admin's domain assignment and subsequent role promotion each take real effect in the affected member's own already-open, never-re-authenticated session on its next navigation — proven by a concrete /links/:id 404->detail transition and a /team reachability + GET /api/team 200 flip.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/team-role-domain-reassign.spec.ts` — TEAM-E2E-02 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/17-team-management-domain-scoped-authz-e2e/17-02-SUMMARY.md` when done.
</output>
</content>
