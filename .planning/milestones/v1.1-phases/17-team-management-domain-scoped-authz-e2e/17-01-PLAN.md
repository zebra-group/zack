---
phase: 17-team-management-domain-scoped-authz-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/e2e/tests/authed/team-invite-accept.spec.ts
autonomous: true
requirements: [TEAM-E2E-01]

must_haves:
  truths:
    - "An admin (chromium-admin storageState) opening /team, clicking `.invite-button`, filling InviteMemberModal's `.field-input` with a per-test crypto-unique email, and clicking `.btn-primary` ('Magic Link senden') creates a new roster row immediately whose `.status-badge` reads 'Ausstehend' (NOT the `.active` class) — proving the invite send + optimistic list append (TEAM-01/D-09-04)"
    - "A SEPARATE, fresh unauthenticated browser context (browser.newContext, no storageState) retrieving the invite email via findMagicLinkUrl(newEmail) and fully navigating (await page.goto) the returned magic-link URL completes better-auth's verify handler — which flips the invitee's User.emailVerified to true and establishes a Session (Pattern 1)"
    - "After the acceptance navigation fully resolves, the admin re-navigating /team shows the SAME row's `.status-badge` now reading 'Aktiv' (`.active` class present) — the emailVerified-driven status flip cross-checked before/after is the concrete end-to-end proof (toTeamMemberDto's status = emailVerified ? active : pending, the ONE derivation point)"
  artifacts:
    - apps/e2e/tests/authed/team-invite-accept.spec.ts
  key_links:
    - "the invite email IS an ordinary better-auth magic-link email (lib/team.ts's triggerMagicLinkSend calls auth.api.signInMagicLink verbatim) — findMagicLinkUrl (apps/e2e/src/mailpit.ts) retrieves it UNCHANGED, recipient-scoped to the unique invitee email"
    - "the roster row is located by `.table-row` containing a `.user-email` with hasText the invitee email; the status is asserted on that row's `.status-badge` (text 'Ausstehend' -> 'Aktiv'), never a bare positional `.status-badge`"
    - "the acceptance navigation MUST fully resolve (await page.goto of the magic link, including better-auth's post-verify redirect) BEFORE the admin re-fetches /team — the two browser contexts must not be raced (Pitfall 2)"
---

<objective>
Prove TEAM-E2E-01 end-to-end through the real Team UI + Mailpit + a real magic-link acceptance: an admin invites a brand-new member through the actual InviteMemberModal, the new row appears immediately as "Ausstehend", a fresh unauthenticated browser context accepts the invite by opening the emailed magic link, and the admin's re-navigated Team list flips that row to "Aktiv". The invite mechanism is — code-verified in 17-RESEARCH.md — the SAME better-auth `signInMagicLink` path the login flow uses (no separate invite-token route), so Phase 11's `findMagicLinkUrl` retrieves the invite email unchanged, and acceptance's `emailVerified: true` write is exactly what drives the `.status-badge` "Ausstehend" -> "Aktiv" flip via `toTeamMemberDto`.

This spec asserts REAL, currently-passing Phase 9 behaviour — `lib/team.ts`, `routes/team.ts`, `TeamView.vue`, `InviteMemberModal.vue`, and better-auth's magic-link verify were all read in full during research and need no changes. If a genuine app-code gap surfaces (an unexpected RED), STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-16 TDD RED->GREEN precedent: a real bug is fixed, never papered over). No production code change is anticipated for this phase.

Purpose: Close TEAM-E2E-01 with a real UI invite -> Mailpit -> real acceptance -> status-flip round trip.
Output: `apps/e2e/tests/authed/team-invite-accept.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-team-management-domain-scoped-authz-e2e/17-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/mailpit.ts
@apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: TEAM-E2E-01 — real UI invite -> Mailpit -> accept -> Team-list status flip (chromium-admin)</name>
  <read_first>
    - 17-RESEARCH.md (Pattern 1 "invite is a magic-link send, not a separate flow" — the exact 4-step flow and selectors; Pitfall 2 "racing the invite-acceptance's emailVerified write against the admin's list re-fetch" — await the acceptance navigation fully before re-fetch; Code Examples "invite reuses the login magic-link mechanism verbatim")
    - apps/web/src/views/TeamView.vue (VERIFIED selectors: `.invite-button` text "+ Mitglied einladen"; roster rows are `.table-row` with `.user-email` per row and `.status-badge`; the `.active` class + text "Aktiv" mark an active member, plain `.status-badge` text "Ausstehend" marks pending — statusLabel/`:class="{ active: member.status === 'active' }"`; handleInviteSubmit appends/replaces the returned row immediately)
    - apps/web/src/components/InviteMemberModal.vue (VERIFIED selectors: email input is `.field-input` (also `.mono`), placeholder "kollege@firma.de"; role defaults to "member" so no role-card click is needed for a member invite; submit button is `.btn-primary` text "Magic Link senden")
    - apps/e2e/src/mailpit.ts (findMagicLinkUrl — recipient-scoped, retrieves the invite email UNCHANGED because it is an ordinary magic-link email; hard-asserts the To address before returning)
    - apps/e2e/src/db.ts (createE2ePrisma — for the DB cross-check + $disconnect discipline; do NOT touch seedBaseline/withResetDbLock; User rows are NOT truncated between spec files so a per-test crypto-unique email is mandatory to avoid P2002 across repeated runs — 17-RESEARCH.md Wave 0 Gaps)
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts (the precedent for `test.describe.configure({ retries: 2 })` + a `test.beforeEach` that `test.skip`s when `testInfo.project.name !== "chromium-admin"` + the testInfo.retry attribution console.warn — reuse this exact shape)
    - apps/e2e/tests/auth/magic-link-round-trip.spec.ts (the reference for a fresh-context magic-link acceptance: browser.newContext({ baseURL }) -> page.goto(magicLinkUrl) -> waitFor Dashboard nav; and the get-session cross-check idiom)
  </read_first>
  <files>apps/e2e/tests/authed/team-invite-accept.spec.ts</files>
  <action>
    Wrap the spec in a single `test.describe` with `test.describe.configure({ retries: 2 })` (doc-comment mirroring qr-dynamic-remap.spec.ts: the full-suite phase gate runs db-isolation.spec.ts's concurrent truncates, and this spec's fixtures/roster reads straddle multiple steps, so a whole-test retry with a fresh per-test unique email is the collision-free equivalent). Add a `test.beforeEach(async ({}, testInfo) => {...})` that `test.skip`s when `testInfo.project.name !== "chromium-admin"` (reason: TEAM-E2E-01's invite actions require the admin-only /team surface; the invitee's own session is established inline in a fresh context, not the chromium-member storageState) and emits the same `testInfo.retry > 0` attribution console.warn qr-dynamic-remap uses.

    Single test "admin invites a new member; acceptance via magic link flips the roster status Ausstehend -> Aktiv":
    1. Mint a per-test crypto-unique invitee email `team-invite-<hex>@e2e.kurzly.local` (hex from randomUUID().slice(0,8)).
    2. As the admin (chromium-admin `page`, already authenticated via storageState — no login): `await page.goto('/team')`, wait for the roster to render, click `.invite-button`. In the opened InviteMemberModal, fill the `.field-input` (email) with the invitee email (role stays the default "member"; do not select a domain — a pending member with zero domains is a valid invitee and keeps the assertion focused on the status flip). Click `.btn-primary` ("Magic Link senden"). Await the POST /api/team/invite response resolving 201 (use page.waitForResponse keyed on method POST + pathname /api/team/invite, ok()===true), so the roster append is deterministic.
    3. BEFORE-acceptance assertion: locate the new row via `page.locator('.table-row', { has: page.locator('.user-email', { hasText: inviteeEmail }) })` and assert its `.status-badge` has text "Ausstehend" (and does NOT carry the `.active` class). This proves the invite send + optimistic append.
    4. Acceptance in a SEPARATE fresh context: `findMagicLinkUrl(inviteeEmail)` retrieves the invite email; `const acceptCtx = await browser.newContext({ baseURL: resolvedBaseUrl })` (resolvedBaseUrl = baseURL ?? process.env.PLAYWRIGHT_BASE_URL ?? "http://localhost:3000", mirroring logout-route-guard.spec.ts); in a fresh page from that context, `await page.goto(magicLinkUrl)` and wait for the authenticated App Shell ("Dashboard" nav link) to render — this FULLY resolves the verify + emailVerified:true write + session creation BEFORE step 5 (Pitfall 2). Close acceptCtx in a finally.
    5. AFTER-acceptance assertion: back on the admin `page`, re-navigate `await page.goto('/team')` (or page.reload) so listTeamMembers re-reads fresh from the DB, re-locate the SAME row by invitee email, and assert its `.status-badge` now reads "Aktiv" (`.active` class present). This is the emailVerified-driven status flip — the strongest available proof the full round trip completed.
    6. DB cross-check (createE2ePrisma in a try/finally that always $disconnects): assert `prisma.user.findUniqueOrThrow({ where: { email: inviteeEmail } })` has `emailVerified === true` after acceptance.

    Use the INFRA-06 bypass header ONLY where a `request.post` establishes something directly — here the invite is sent through the real UI (which cannot send the header), so do NOT add a bypass anywhere for the invite itself; a single invite call never trips MAGIC_LINK_RATE_LIMIT's per-route/per-IP bucket. Do NOT mutate the seeded ADMIN_EMAIL/MEMBER_EMAIL fixtures. Use no fixed waitForTimeout — Playwright locator auto-wait + the awaited POST/verify responses cover all sequencing.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/team-invite-accept.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the invite is sent through the REAL InviteMemberModal UI (`.invite-button` -> `.field-input` -> `.btn-primary`), asserting the POST /api/team/invite response is 201 — never a direct prisma.user.create or a raw invite API call
    - the new roster row is asserted "Ausstehend" (no `.active`) immediately after invite, then "Aktiv" (`.active`) after acceptance, both scoped to the `.table-row` whose `.user-email` matches the unique invitee email
    - acceptance is performed in a fresh browser.newContext (no storageState) by navigating the findMagicLinkUrl-retrieved magic link, fully awaited (Dashboard nav rendered) before the admin re-fetch (Pitfall 2)
    - a direct-Prisma cross-check confirms the invitee's emailVerified === true after acceptance
    - the spec uses a per-test crypto-unique invitee email + retries:2, is scoped to chromium-admin (skipped under chromium-member), and never mutates the seeded baseline admin/member fixtures
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>TEAM-E2E-01 proves a real-UI admin invite delivered via Mailpit, accepted by opening the magic link in a fresh context, flips the Team roster status from "Ausstehend" to "Aktiv" — UI-and-DB cross-checked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin browser (chromium-admin) -> POST /api/team/invite | account-admin-gated invite that triggers a server-side magic-link mail send (MAGIC_LINK_RATE_LIMIT, T-09-INVITE-BOMB) |
| fresh unauthenticated context -> GET /api/auth/magic-link/verify | untrusted invite-token acceptance crosses here; better-auth flips emailVerified + mints a Session |
| test runner -> E2E Postgres :5433 | direct-Prisma emailVerified cross-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-17-01-INVITE | Spoofing | inviteMember creating an allowlisted User (emailVerified:false) | low | accept | The invite only makes the address able to RECEIVE a magic link (the allowlist IS the User table); a real session still requires accepting the emailed link — this spec proves exactly that gated flip, introduces nothing |
| T-17-01-BOMB | Denial of Service | POST /api/team/invite mail-send | low | accept | The endpoint carries MAGIC_LINK_RATE_LIMIT (existing, Phase 9); this spec issues a single invite per test that never trips the per-route/per-IP bucket — no bypass header is (or can be) sent through the real UI |
| T-17-01-XWORKER | Information disclosure | Mailpit invite-email retrieval | low | accept | findMagicLinkUrl is recipient-scoped and hard-asserts the To address (T-11-07); a per-test crypto-unique invitee email makes cross-worker email theft structurally impossible |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/team-invite-accept.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (the spec is registered under chromium-member too, where it is skipped) — confirm the invite is never 429'd and the status-flip re-fetch never races the acceptance (Pitfall 2), beyond what retries:2 absorbs.
</verification>

<success_criteria>
TEAM-E2E-01 passes: a real-UI invite is delivered via Mailpit, accepted by opening the magic link in a fresh browser context, and the admin's re-navigated Team list flips the invitee's status badge from "Ausstehend" to "Aktiv", cross-checked at the database (emailVerified === true).
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/team-invite-accept.spec.ts` — TEAM-E2E-01 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/17-team-management-domain-scoped-authz-e2e/17-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
