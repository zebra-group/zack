---
phase: 12-redirect-handler-e2e-core-value
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - apps/e2e/tests/smoke/redirect-slug-redirect.spec.ts
  - apps/e2e/tests/smoke/redirect-expiry.spec.ts
autonomous: true
requirements:
  - REDIRECT-E2E-01
  - REDIRECT-E2E-03
must_haves:
  truths:
    - "A slug on the registered domain `e2e.kurzly.local` resolves to its exact stored target with a 302 status and an exact `Location` header (status contract, not just final landing URL), proven over real HTTP with `maxRedirects: 0`. [REDIRECT-E2E-01]"
    - "An expired link returns HTTP 410 (distinct from a 404 slug-not-found), carries no `Location` header, and never leaks its target in any body or header. [REDIRECT-E2E-03]"
    - "A script-injection slug renders an entity-escaped branded 404 body over real HTTP (reflected-XSS guard holds on the wire). [V5 input validation]"
  artifacts:
    - "apps/e2e/tests/smoke/redirect-slug-redirect.spec.ts — REDIRECT-E2E-01 happy path + reflected-XSS-guard case."
    - "apps/e2e/tests/smoke/redirect-expiry.spec.ts — REDIRECT-E2E-03 410-vs-404 + no-leak."
  key_links:
    - "APIRequestContext { host: e2e.kurzly.local, user-agent: BROWSER_UA, maxRedirects: 0 } → GET /:slug → 302 exact Location + Cache-Control: no-store."
    - "Expired fixture (expiresAt in the past) → resolveLinkState expiry-first precedence (D-14) → 410 renderExpiredPage (no target field, no Location)."
---

<objective>
Re-prove the redirect handler's happy-path redirect (REDIRECT-E2E-01) and expiry gate (REDIRECT-E2E-03) through the real network stack against the built image — the one thing `fastify.inject` structurally cannot prove (real TCP, real Host resolution over the wire). Both specs use Playwright's `APIRequestContext` with `maxRedirects: 0`, exactly as the ROADMAP success criteria specify.

This phase writes ZERO application code (12-RESEARCH.md Architectural Responsibility Map: every capability already lives correctly in the API/Backend tier; touching `apps/api/src` is out of scope). The specs mirror the assertion vocabulary of `apps/api/test/redirect.integration.test.ts` verbatim (D-14 expiry precedence, D-11 identical generic 404), adapted to real HTTP.

Purpose: Prove Kurzly's stated single most important guarantee — slug → target redirect and the expiry/no-leak contract — over the deployed transport, not just at the inject layer.
Output: `redirect-slug-redirect.spec.ts` + `redirect-expiry.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-redirect-handler-e2e-core-value/12-RESEARCH.md
@apps/e2e/src/db.ts
@apps/api/test/redirect.integration.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: REDIRECT-E2E-01 — happy-path redirect + reflected-XSS-guard spec</name>
  <files>apps/e2e/tests/smoke/redirect-slug-redirect.spec.ts</files>
  <read_first>
    - apps/api/src/routes/redirect.ts `GET /:slug` — the 302 branch: `reply.header("Cache-Control","no-store")` set first, then `reply.code(302).redirect(target)`; a plain link (no UTM, forwardQuery off) redirects to the exact stored `targetUrl`.
    - apps/api/test/redirect.integration.test.ts REDIR-01 case (`returns 302 with the exact target and Cache-Control: no-store`) and the Reflected-XSS-guard case (`a script-injection slug ... renders an entity-escaped 404 body`) — the exact assertion shapes to mirror over real HTTP.
    - apps/e2e/src/links.ts (from 12-02) — `createE2eLink`, `BROWSER_UA`; apps/e2e/src/db.ts — `createE2ePrisma`, `BASELINE_DOMAIN_HOSTNAME`.
    - apps/api/src/lib/publicHtml.ts `renderNotFoundPage` — branded 404 marker `Dieser Kurzlink existiert nicht`, and `escapeHtml` producing `&lt;script&gt;`.
  </read_first>
  <action>
    Create `apps/e2e/tests/smoke/redirect-slug-redirect.spec.ts` in the `smoke` project. Import `createE2eLink`, `BROWSER_UA` from `../../src/links.js` and `createE2ePrisma`, `BASELINE_DOMAIN_HOSTNAME` from `../../src/db.js`. Use the `request` fixture; give each test a `randomUUID()`-derived unique slug for `fullyParallel` safety; open a client via `createE2ePrisma()` inside `try/finally { await prisma.$disconnect() }`.

    Happy-path test: seed a plain link via `createE2eLink(prisma, { slug, targetUrl: "https://destination.example.com/landing" })`. Issue `request.get(\`/${slug}\`, { headers: { host: BASELINE_DOMAIN_HOSTNAME, "user-agent": BROWSER_UA }, maxRedirects: 0 })`. Assert `response.status()` === 302, `response.headers()["location"]` === `https://destination.example.com/landing` (exact string), and `response.headers()["cache-control"]` === `no-store`. Always pin `BROWSER_UA` — never rely on Playwright's default UA, which isbot@5.2.0 classifies as a bot (12-RESEARCH.md Pitfall 1).

    Reflected-XSS-guard test: request `\`/${encodeURIComponent("<script>alert(1)</script>")}\`` with the same Host + BROWSER_UA + `maxRedirects: 0`. Assert status 404, body does NOT contain the raw `<script>alert(1)</script>` sequence, and body DOES contain `&lt;script&gt;` — proving `escapeHtml` holds over real HTTP (Security Domain V5).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/redirect-slug-redirect.spec.ts</automated>
  </verify>
  <done>redirect-slug-redirect.spec.ts passes: 302 + exact Location + Cache-Control no-store for a plain slug (REDIRECT-E2E-01), and an entity-escaped branded 404 for a script-injection slug.</done>
  <acceptance_criteria>
    - Happy-path test asserts 302 + exact `Location` + `Cache-Control: no-store` via APIRequestContext with `maxRedirects: 0` and a pinned `BROWSER_UA`. [REDIRECT-E2E-01]
    - Reflected-XSS-guard test asserts an entity-escaped branded 404 (`&lt;script&gt;` present, raw `<script>` absent).
    - Every request sends `Host: e2e.kurzly.local` (never localhost) so it exercises the redirect engine, not the CR-07 SPA fallback (12-RESEARCH.md Pitfall 2).
    - Spec passes green under `./scripts/e2e-compose.sh tests/smoke/redirect-slug-redirect.spec.ts`.
    - No application source under `apps/api/src` modified.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: REDIRECT-E2E-03 — expired link 410 (distinct from 404) + no-leak spec</name>
  <files>apps/e2e/tests/smoke/redirect-expiry.spec.ts</files>
  <read_first>
    - apps/api/src/lib/redirectEngine.ts `resolveLinkState` — D-14: `expiresAt` checked FIRST/unconditionally (an expired+protected link is ALWAYS "expired").
    - apps/api/src/routes/redirect.ts — the `state === "expired"` branch: `reply.code(410).type("text/html").send(renderExpiredPage(...))`, no `Location` header.
    - apps/api/src/lib/publicHtml.ts `renderExpiredPage` (markers `Dieser Link ist abgelaufen`, `HTTP 410 · Gone`) and `renderNotFoundPage` (marker `Dieser Kurzlink existiert nicht`).
    - apps/api/test/redirect.integration.test.ts REDIR-03 case + the `Precedence (D-14): expiry beats the password gate` describe block — the exact assertion shapes.
    - apps/e2e/src/links.ts — `createE2eLink`, `CANARY_TARGET`, `assertNoLeak`, `BROWSER_UA`.
  </read_first>
  <action>
    Create `apps/e2e/tests/smoke/redirect-expiry.spec.ts` in the `smoke` project. Import `createE2eLink`, `CANARY_TARGET`, `assertNoLeak`, `BROWSER_UA` from `../../src/links.js`; `createE2ePrisma`, `BASELINE_DOMAIN_HOSTNAME` from `../../src/db.js`. Use the `request` fixture, `randomUUID()`-derived slugs, and `createE2ePrisma()` in `try/finally`.

    Expiry test: seed `createE2eLink(prisma, { slug, targetUrl: CANARY_TARGET, expiresAt: "2020-01-01" })`. GET the slug with `{ host: BASELINE_DOMAIN_HOSTNAME, "user-agent": BROWSER_UA, maxRedirects: 0 }`. Assert status 410; `response.headers()["location"]` is undefined; body contains `Dieser Link ist abgelaufen` and `HTTP 410 · Gone`; `cache-control` === `no-store`; and `assertNoLeak(await response.text(), response.headers(), CANARY_TARGET)`.

    Distinct-from-404 test: GET a guaranteed-missing slug (a fresh `randomUUID()`, never seeded) with the same Host/UA. Assert status 404 and body contains `Dieser Kurzlink existiert nicht`. Together with the expiry test this proves 410 ≠ 404 over real HTTP (REDIRECT-E2E-03's "distinct from" requirement).

    Expiry-beats-password precedence test (12-RESEARCH.md Pitfall 3, D-14): seed `createE2eLink(prisma, { slug, targetUrl: CANARY_TARGET, expiresAt: "2020-01-01", password: "correct-horse-battery" })`. GET the slug (Host + BROWSER_UA + maxRedirects:0). Assert status 410, body does NOT contain the password-page marker `Dieser Link ist geschützt`, and `assertNoLeak(...)` — the single highest-value cross-gate security check.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/redirect-expiry.spec.ts</automated>
  </verify>
  <done>redirect-expiry.spec.ts passes: expired link → 410 (no Location, no leak), missing slug → distinct 404, and expired+protected → 410 not the password page (REDIRECT-E2E-03, D-14).</done>
  <acceptance_criteria>
    - Expiry test asserts 410, no `Location`, branded expiry copy, `no-store`, and no canary leak in body or headers. [REDIRECT-E2E-03]
    - A separate missing-slug test asserts a distinct 404, proving 410 ≠ 404 over real HTTP.
    - The expiry-beats-password test asserts 410 (never the password page) for an expired+protected link, no leak (D-14 precedence).
    - Every request sends `Host: e2e.kurzly.local` and a pinned `BROWSER_UA`.
    - Spec passes green under `./scripts/e2e-compose.sh tests/smoke/redirect-expiry.spec.ts`.
    - No application source under `apps/api/src` modified.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public visitor → redirect handler (real HTTP) | Any unauthenticated caller can request any slug on any host; the handler must never leak an expired/gated target and must render user-controlled slug segments safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-LEAK-EXP | Information Disclosure | `renderExpiredPage` / 410 branch | high | mitigate | `assertNoLeak` over the real 410 body + every header proves the target never appears pre-check; expiry-first precedence (D-14) proves an expired+protected link cannot fall through to a target-bearing path. |
| T-12-XSS-SLUG | Tampering (rendered content) | `renderNotFoundPage` slug interpolation | medium | mitigate | Reflected-XSS-guard test asserts `&lt;script&gt;` (escaped) and the raw `<script>` absent over real HTTP, proving `escapeHtml` holds on the wire. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase — no supply-chain surface. |
</threat_model>

<verification>
- Both specs green under their scoped `./scripts/e2e-compose.sh <file>` runs.
- Per-wave-merge gate: `./scripts/e2e-compose.sh` (full suite) stays green alongside 12-04, 12-05, and Phase 11's smoke specs.
</verification>

<success_criteria>
- REDIRECT-E2E-01 and REDIRECT-E2E-03 proven over real HTTP against the built image with exact status/Location/no-leak contracts.
- No application code changed.
</success_criteria>

<artifacts_this_phase_produces>
- `apps/e2e/tests/smoke/redirect-slug-redirect.spec.ts` — REDIRECT-E2E-01 + reflected-XSS guard.
- `apps/e2e/tests/smoke/redirect-expiry.spec.ts` — REDIRECT-E2E-03 (410 vs 404, no-leak, expiry-beats-password).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-redirect-handler-e2e-core-value/12-03-SUMMARY.md` when done.
</output>
