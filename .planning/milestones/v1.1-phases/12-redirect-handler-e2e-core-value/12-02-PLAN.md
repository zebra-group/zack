---
phase: 12-redirect-handler-e2e-core-value
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/e2e/src/links.ts
  - apps/e2e/tests/smoke/links-fixture.spec.ts
autonomous: true
requirements:
  - REDIRECT-E2E-01
  - REDIRECT-E2E-02
  - REDIRECT-E2E-03
  - REDIRECT-E2E-04
  - REDIRECT-E2E-05
must_haves:
  truths:
    - "A fixture Link created via the helper stores a REAL bcrypt hash in `passwordHash` (so `POST /:slug/verify`'s `bcrypt.compare` can succeed), never the plaintext password."
    - "A fixture Link created with an `expiresAt` date string stores the UTC end-of-day instant (`${date}T23:59:59.999Z`), matching `lib/links.ts`'s `deriveExpiresAt` behavior exactly."
    - "All five feature specs seed their own Link(s) through this single helper against the baseline seeded Domain `e2e.kurzly.local`, with cryptographically-random slugs for parallel isolation."
  artifacts:
    - "apps/e2e/src/links.ts — raw-Prisma-insert fixture helper (createE2eLink) plus the shared redirect-test vocabulary (BROWSER_UA, BOT_UA, CANARY_TARGET, assertNoLeak) reused verbatim from apps/api/test/redirect.integration.test.ts."
    - "apps/e2e/tests/smoke/links-fixture.spec.ts — RED→GREEN proof of the helper's password-hash and expiry-derivation correctness plus a create+read round-trip."
  key_links:
    - "createE2eLink → derivePasswordHash (bcryptjs) → passwordHash column → bcrypt.compare at POST /:slug/verify."
    - "createE2eLink → deriveExpiresAt (UTC end-of-day) → expiresAt column → resolveLinkState expiry precedence (D-14)."
    - "createE2eLink → domainId resolved from BASELINE_DOMAIN_HOSTNAME, createdBy resolved from the seeded ADMIN_EMAIL User.id."
---

<objective>
Build the ONE shared fixture helper every feature spec in this phase consumes: `apps/e2e/src/links.ts`. Because `@kurzly/api`'s `exports` map exposes only `.` and `./prisma-client` (12-RESEARCH.md Q2, RESOLVED — `lib/links.ts`'s `createLink`/`updateLink` are unreachable from `apps/e2e`), fixtures must be created via a raw `prisma.link.create` that reproduces the two invariants those functions normally enforce (12-RESEARCH.md Pitfall 4): a REAL bcrypt `passwordHash` (never plaintext) and a UTC-end-of-day `expiresAt`.

TDD is genuine here: the helper's derivation correctness (a hash that `bcrypt.compare` accepts; a correctly-computed expiry instant) is a testable `expect(fn(input)).toBe(output)` contract writable BEFORE the implementation. This plan runs RED→GREEN. The five feature specs (12-03/12-04/12-05) are the acceptance proof for the already-shipped v1.0 redirect handler — there is no new production code to TDD there; this helper is the one net-new piece of test infrastructure with its own unit-level correctness.

This plan writes ZERO application code under `apps/api/src`.

Purpose: A single, correct, DRY fixture source so no feature spec hand-rolls a raw insert that silently stores plaintext or an off-by-hours expiry.
Output: `apps/e2e/src/links.ts` + `apps/e2e/tests/smoke/links-fixture.spec.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-redirect-handler-e2e-core-value/12-RESEARCH.md
@apps/e2e/src/db.ts
@apps/api/test/redirect.integration.test.ts
</context>

<tasks>

<task type="tdd">
  <name>Task 1 (RED): Write the failing helper-contract spec for password hashing, expiry derivation, and create+read round-trip</name>
  <files>apps/e2e/tests/smoke/links-fixture.spec.ts</files>
  <read_first>
    - apps/api/test/redirect.integration.test.ts — copy the exact constant VALUES to reuse verbatim: `CANARY_TARGET` (`https://canary-leak-marker.example.net/super-secret-target-xyz123`), `BOT_UA` (`Mozilla/5.0 (compatible; Googlebot/2.1; +http://www.google.com/bot.html)`), `BROWSER_UA` (the Chrome 128 string), and the `assertNoLeak` shape (body + every header value must not contain the canary).
    - apps/api/src/lib/links.ts — `derivePasswordHash` (bcrypt.hash at `PASSWORD_HASH_COST`, default 11) and `deriveExpiresAt` (`new Date(\`${expiresAt}T23:59:59.999Z\`)`) — the exact behaviors the helper must mirror.
    - apps/e2e/src/db.ts — `createE2ePrisma()`, `BASELINE_DOMAIN_HOSTNAME`, `ADMIN_EMAIL`, `withResetDbLock`; per-file isolation pattern.
    - apps/e2e/tests/smoke/db-isolation.spec.ts — the `createE2ePrisma()` + `try/finally { prisma.$disconnect() }` + random-slug consumption pattern to mirror.
  </read_first>
  <behavior>
    - derivePasswordHash("correct-horse-battery") returns a string for which `bcrypt.compare("correct-horse-battery", hash)` resolves true, and which is NOT equal to the plaintext.
    - deriveExpiresAt("2020-01-01") returns a Date whose `.toISOString()` is exactly `2020-01-01T23:59:59.999Z`.
    - createE2eLink(prisma, { slug, targetUrl, password: "pw" }) inserts a Link on the baseline Domain (`e2e.kurzly.local`) whose read-back `passwordHash` satisfies `bcrypt.compare("pw", passwordHash)` and whose `createdBy` equals the seeded ADMIN_EMAIL User's id.
    - createE2eLink(prisma, { slug, targetUrl, expiresAt: "2020-01-01" }) inserts a Link whose read-back `expiresAt.toISOString()` === `2020-01-01T23:59:59.999Z`.
    - A plain createE2eLink (no password, no expiry) stores `passwordHash: null` and `expiresAt: null`.
  </behavior>
  <action>
    Create `apps/e2e/tests/smoke/links-fixture.spec.ts` in the `smoke` project. Import (RED — these do not exist yet) `createE2eLink`, `derivePasswordHash`, `deriveExpiresAt` from `../../src/links.js`; import `createE2ePrisma`, `BASELINE_DOMAIN_HOSTNAME` from `../../src/db.js`; import `bcrypt` from `bcryptjs` (available transitively via `@kurzly/api`).

    Write tests encoding the five behaviors above. For the pure-derivation tests (`derivePasswordHash`, `deriveExpiresAt`) assert without touching the DB. For the create+read round-trips, use `createE2ePrisma()` inside `try/finally { await prisma.$disconnect() }`, wrap create+read in `withResetDbLock` (or a plain create with a `randomUUID()`-derived unique slug if read-only against its own row — mirror db-isolation.spec.ts), and read the row back with `prisma.link.findUniqueOrThrow`.

    Use cryptographically-random slugs (`randomUUID()`-derived) so the file is safe under `fullyParallel`. Run the spec now and confirm it FAILS (module `../../src/links.js` not found / exports missing) — this is the RED state.

    Do NOT weaken any assertion to make RED "less red"; the failure must be the missing implementation, not a malformed test.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/links-fixture.spec.ts</automated>
  </verify>
  <done>links-fixture.spec.ts exists and FAILS (RED) before the helper is implemented, encoding the five derivation/round-trip behaviors with exact expected values.</done>
  <acceptance_criteria>
    - `apps/e2e/tests/smoke/links-fixture.spec.ts` exists and, run before Task 2, FAILS because `apps/e2e/src/links.ts` (or its exports) does not yet exist (verified RED).
    - Tests encode all five behaviors listed above with exact expected values (`2020-01-01T23:59:59.999Z`, `bcrypt.compare` true, `createdBy` === seeded admin id, null when omitted).
    - No application source under `apps/api/src` modified.
    - RED commit message: `test(12-02): add failing fixture-helper contract for apps/e2e/src/links.ts`.
  </acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2 (GREEN): Implement apps/e2e/src/links.ts fixture helper + shared redirect-test vocabulary</name>
  <files>apps/e2e/src/links.ts</files>
  <read_first>
    - apps/api/src/lib/links.ts — `derivePasswordHash`, `deriveExpiresAt`, `resolvePasswordHashCost` (reads `process.env.PASSWORD_HASH_COST`, default 11) — reproduce these behaviors, not the authorization/validation core.
    - apps/e2e/src/db.ts — reuse `Prisma`/`PrismaClient` types from `@kurzly/api/prisma-client`; `BASELINE_DOMAIN_HOSTNAME`, `ADMIN_EMAIL`.
    - apps/api/prisma/schema.prisma `model Link` — the exact createable columns: `domainId`, `slug`, `targetUrl`, `createdBy`, `passwordHash`, `expiresAt`, `forwardQuery`, `trackingEnabled`, `utmSource`, `utmMedium`, `utmCampaign`, `ogTitle`, `ogDescription`, `ogImageUrl`.
  </read_first>
  <behavior>
    - Exports `derivePasswordHash(plaintext: string): Promise<string>` — bcryptjs hash at cost from `process.env.PASSWORD_HASH_COST` (default 11); mirrors lib/links.ts.
    - Exports `deriveExpiresAt(date: string): Date` — `new Date(\`${date}T23:59:59.999Z\`)`.
    - Exports `createE2eLink(prisma, opts)` — resolves domainId from `opts.domainHostname ?? BASELINE_DOMAIN_HOSTNAME` and createdBy from the seeded `ADMIN_EMAIL` User; hashes `opts.password` when provided (else null), derives `opts.expiresAt` when provided (else null); passes through forwardQuery/trackingEnabled/utm*/og* verbatim; returns the created `Link` row.
    - Exports the shared vocabulary consumed by every feature spec: `BROWSER_UA`, `BOT_UA`, `CANARY_TARGET`, and `assertNoLeak(body: string, headers: Record<string, string>, canary: string)`.
  </behavior>
  <action>
    Create `apps/e2e/src/links.ts`. Implement `derivePasswordHash` and `deriveExpiresAt` to mirror `apps/api/src/lib/links.ts` byte-for-byte in behavior (same UTC-end-of-day format string, same bcrypt cost resolution reading `process.env.PASSWORD_HASH_COST` with a default of 11). Implement `createE2eLink(prisma: PrismaClient, opts)` accepting a typed options object with fields: `slug`, `targetUrl`, optional `domainHostname`, `password`, `expiresAt` (YYYY-MM-DD string), `forwardQuery`, `trackingEnabled`, `utmSource`, `utmMedium`, `utmCampaign`, `ogTitle`, `ogDescription`, `ogImageUrl`.

    Inside createE2eLink: `prisma.domain.findUniqueOrThrow({ where: { hostname } })` and `prisma.user.findUniqueOrThrow({ where: { email: ADMIN_EMAIL } })` to resolve `domainId`/`createdBy`; build the `data` object hashing the password (await derivePasswordHash) and deriving expiresAt only when the caller supplied them, leaving them `null`/omitted otherwise; return `prisma.link.create({ data })`.

    Export the shared vocabulary constants (`BROWSER_UA`, `BOT_UA`, `CANARY_TARGET`) with the EXACT values from apps/api/test/redirect.integration.test.ts, and `assertNoLeak(body, headers, canary)` adapted to Playwright's `APIResponse` shape (a plain string body plus a plain `Record<string,string>` headers object): assert the canary appears in neither the body nor any header value.

    Add a header comment stating this helper exists because `@kurzly/api`'s exports map blocks importing `lib/links.ts` (12-RESEARCH.md Q2), and that it deliberately mirrors only `derivePasswordHash`/`deriveExpiresAt`'s SHAPE, not the authorization/validation core. Run the Task-1 spec and confirm it now PASSES (GREEN).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/smoke/links-fixture.spec.ts</automated>
  </verify>
  <done>apps/e2e/src/links.ts implemented; links-fixture.spec.ts passes (GREEN); `pnpm --filter @kurzly/e2e typecheck` clean; passwordHash is a real bcrypt hash and expiresAt the exact UTC end-of-day instant.</done>
  <acceptance_criteria>
    - `apps/e2e/src/links.ts` exists and exports `createE2eLink`, `derivePasswordHash`, `deriveExpiresAt`, `BROWSER_UA`, `BOT_UA`, `CANARY_TARGET`, `assertNoLeak`.
    - The Task-1 spec `tests/smoke/links-fixture.spec.ts` now passes green under the booted stack (GREEN).
    - `pnpm --filter @kurzly/e2e typecheck` passes (`tsc --noEmit`).
    - passwordHash is a real bcrypt hash (verified by bcrypt.compare in the spec), never plaintext; expiresAt is the exact UTC end-of-day instant.
    - No application source under `apps/api/src` modified.
    - GREEN commit message: `feat(12-02): implement apps/e2e/src/links.ts raw-insert fixture helper + shared vocabulary`.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture helper → Postgres (raw insert) | With `createLink` unreachable, there is no validation core between the test and the DB — the helper is the only thing guaranteeing a real bcrypt hash and correct expiry instant land in the columns the redirect engine reads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-FIXTURE-PW | Information Disclosure / Cryptography | `createE2eLink` password handling | high | mitigate | Helper stores `bcrypt.hash(...)`, never plaintext; RED→GREEN spec asserts `bcrypt.compare` accepts the stored hash AND that the stored value differs from the plaintext, so a regression to plaintext fails the suite. |
| T-12-FIXTURE-EXP | Tampering | `createE2eLink` expiry handling | medium | mitigate | Helper computes the UTC end-of-day instant; spec pins `2020-01-01T23:59:59.999Z`, preventing an off-by-hours fixture that would make the expiry-precedence spec pass for the wrong reason. |
| T-12-SC | Tampering | npm/pip/cargo installs | low | accept | Zero new packages this phase (bcryptjs already present transitively via `@kurzly/api`) — no supply-chain surface introduced. |
</threat_model>

<verification>
- RED: `tests/smoke/links-fixture.spec.ts` fails before `apps/e2e/src/links.ts` exists.
- GREEN: same spec passes after implementation; `pnpm --filter @kurzly/e2e typecheck` clean.
- Per-wave-merge gate: `./scripts/e2e-compose.sh` (full suite) stays green.
</verification>

<success_criteria>
- One shared, correct fixture helper (real bcrypt hash + UTC end-of-day expiry) proven by a RED→GREEN cycle.
- Shared redirect-test vocabulary available for all five feature specs to import.
- No application code changed.
</success_criteria>

<artifacts_this_phase_produces>
- `apps/e2e/src/links.ts` — raw-insert fixture helper + shared redirect-test vocabulary.
- `apps/e2e/tests/smoke/links-fixture.spec.ts` — RED→GREEN correctness proof for the helper.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/12-redirect-handler-e2e-core-value/12-02-SUMMARY.md` when done.
</output>
