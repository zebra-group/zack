---
phase: 15-qr-studio-e2e
plan: 04
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - apps/e2e/tests/authed/qr-export-formats.spec.ts
autonomous: true
requirements: [QR-E2E-03]

must_haves:
  truths:
    - "Clicking the REAL `.export-png` and `.export-svg` Studio buttons each triggers a real browser download (QrStudioPanel.exportFile's Blob -> `<a download>` click), captured via page.waitForEvent('download'), with a suggested filename ending `.png` / `.svg` respectively"
    - "The downloaded PNG bytes decode via decodeQrImage, and the downloaded SVG bytes — rasterized via sharp(svgBytes).png().toBuffer() first, exactly as apps/api/test/qrDecode.test.ts's own SVG case does — also decode, BOTH back to the SAME expected short URL `https://{BASELINE_DOMAIN_HOSTNAME}/{slug}?qr={qrId}` — a genuine 'valid file' proof, not just a non-empty download"
    - "The server render responses carry the correct content-types: /api/qr-codes/:id/render.png is image/png and /api/qr-codes/:id/render.svg is image/svg+xml"
  artifacts:
    - apps/e2e/tests/authed/qr-export-formats.spec.ts
  key_links:
    - "the QR under test is seeded once via createE2eQrCode({variant:'static'}) bound to a fixture Link (export validity is independent of customization state, 15-RESEARCH.md Open Question 2) — the export UI is this spec's genuine subject, so the buttons are driven for real"
    - "content-type assertions use authenticated page.request.get (shares the chromium-admin storageState cookie jar the 401-gated render endpoints require); the download bytes themselves prove file validity via jsQR decode"
---

<objective>
Prove QR-E2E-03 end-to-end: the real PNG and SVG export buttons each produce a valid, downloadable file. Drive the actual `.export-png`/`.export-svg` Studio buttons (whose `exportFile` fetches the server-rendered bytes as a Blob and triggers an `<a download>` click), capture the real browser downloads, and prove each downloaded file is genuinely valid by decoding it via jsQR back to the same expected short URL — the strongest available "valid file" proof, well beyond "a non-empty download happened". Additionally assert the render endpoints' content-types (image/png, image/svg+xml) via authenticated page.request.

This spec asserts REAL, currently-passing behaviour — QrStudioPanel.vue's exportFile and routes/qrCodes.ts's render.png/render.svg handlers were read in full and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-14 TDD RED->GREEN precedent).

Purpose: Close QR-E2E-03 with a real-export-button download + decode + content-type proof on both formats.
Output: `apps/e2e/tests/authed/qr-export-formats.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qr-studio-e2e/15-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/src/qr.ts
@apps/e2e/tests/authed/links-crud.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QR-E2E-03 — PNG + SVG export both download + decode + correct content-type (chromium-admin)</name>
  <read_first>
    - apps/web/src/components/QrStudioPanel.vue (VERIFIED: `<button class="export-button export-png" @click="exportFile('png')">` and `.export-button.export-svg` @click exportFile('svg'); exportFile fetches fetchQrRenderBlob(id, format) then creates an `<a>` with download=`${qr.name || "qr-code"}.${format}`, appends it, clicks it, removes it — a real browser download; the panel mounts for the `?selected={id}` QR)
    - apps/web/src/views/QrCodesView.vue (VERIFIED: `?selected={qrId}` deep-link selects the card; QrStudioPanel is rendered for selectedQr, so a directly-seeded static QR shows its export buttons once selected)
    - apps/api/src/routes/qrCodes.ts (VERIFIED: GET /api/qr-codes/:id/render.png replies type "image/png"; render.svg replies type "image/svg+xml"; both 401-gated via resolveUserId/resolveOwnedQrCode; resolveQrPayload for a static QR builds `https://${hostname}/${slug}?qr=${id}`)
    - apps/api/src/lib/redirectEngine.ts (VERIFIED: `export const QR_SCAN_PARAM = "qr"` — the literal scan param; apps/e2e hardcodes `qr` with a source comment)
    - apps/api/test/qrDecode.test.ts (the SVG-decode idiom: rasterize the SVG string via `sharp(Buffer.from(svg)).png().toBuffer()` THEN decode — decodeQrImage decodes PNG bytes; SVG must be rasterized first)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME — import the constant)
    - apps/e2e/src/links.ts (createE2eLink — seed the parent Link with a per-test random slug)
    - apps/e2e/src/qr.ts (createE2eQrCode for the static QR fixture; decodeQrImage for both formats)
    - apps/e2e/tests/authed/links-crud.spec.ts (retries:2 for the db-isolation truncate race; chromium-admin-only test.skip; per-test random identifiers)
    - 15-RESEARCH.md (Code Example "PNG and SVG export"; Open Question 2 separate spec file with its own fixture; Pitfall 1 wrong-string decode; Pitfall 2 authenticated page.request)
  </read_first>
  <files>apps/e2e/tests/authed/qr-export-formats.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` with a doc-comment (mirror links-crud.spec.ts) — the db-isolation.spec.ts cross-file truncate race applies (fixtures created outside withResetDbLock). Add a `test.beforeEach` that `test.skip`s when testInfo.project.name !== "chromium-admin" (admin-scoped; role-differentiated QR authz is Phase 17). Import `sharp` (from 15-01's devDep) for the SVG rasterization step, `readFileSync` from node:fs for reading the downloaded files, decodeQrImage + createE2eQrCode from ../../src/qr.js, createE2eLink from ../../src/links.js, createE2ePrisma + BASELINE_DOMAIN_HOSTNAME from ../../src/db.js.

    Single test "PNG and SVG exports are both valid, independently decodable downloads":
    1. Mint a per-test crypto-random slug `e2e-qr-exp-<hex>`. createE2ePrisma(); createE2eLink with that slug and targetUrl `https://example.com/qr-export-<hex>`; createE2eQrCode({variant:"static", linkId: link.id, name:"Export Test <hex>"}); read qrId from the created row. $disconnect in a finally. Build expectedShortUrl = `https://${BASELINE_DOMAIN_HOSTNAME}/${slug}?qr=${qrId}` (literal `qr` = QR_SCAN_PARAM, with a source comment).
    2. page.goto(`/qr-codes?selected=${qrId}`); assert the Studio panel is present (e.g. the `.export-png` button is visible) — confirms the seeded QR selected and the panel mounted.
    3. PNG export download: use Promise.all of page.waitForEvent('download') and page.locator('.export-png').click(); assert the download's suggestedFilename() ends with ".png"; read its bytes via readFileSync(await download.path()); assert decodeQrImage(pngBytes) === expectedShortUrl.
    4. SVG export download: same pattern with page.locator('.export-svg').click(); assert suggestedFilename() ends with ".svg"; read the downloaded SVG bytes, rasterize via `await sharp(svgBytes).png().toBuffer()`, then assert decodeQrImage(rasterized) === expectedShortUrl (same expected string as the PNG — both formats encode identical payloads).
    5. CONTENT-TYPE: const pngResp = await page.request.get(`/api/qr-codes/${qrId}/render.png`); assert pngResp.headers()["content-type"] === "image/png". const svgResp = await page.request.get(`/api/qr-codes/${qrId}/render.svg`); assert svgResp.headers()["content-type"] === "image/svg+xml". (Also assert svgResp text contains "<svg" as a cheap structural sanity check.)

    Never assert a decoded payload equals link.targetUrl (Pitfall 1). Decode is the file-validity proof — do not settle for a non-empty byte-length check alone.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/qr-export-formats.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - both the REAL `.export-png` and `.export-svg` buttons are clicked and their downloads captured via page.waitForEvent('download') with `.png`/`.svg` suggested filenames (no API-only shortcut for the export mechanism)
    - the downloaded PNG decodes, and the downloaded SVG (rasterized via sharp) decodes, BOTH to the same expectedShortUrl built from fixture values — never targetUrl
    - render.png content-type is image/png and render.svg content-type is image/svg+xml (authenticated page.request)
    - the spec is scoped to chromium-admin (skipped under chromium-member) with a per-test random slug and retries: 2
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>QR-E2E-03 proves both the real PNG and SVG export buttons produce genuinely valid, independently jsQR-decodable downloadable files with correct content-types.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser (admin session) -> `<a download>` export mechanism | authenticated Blob fetch + client-side download of server-rendered bytes |
| host browser (admin session) -> GET /api/qr-codes/:id/render.png|svg | 401-gated, IDOR-guarded server renders fetched via the shared cookie jar |
| test runner -> E2E Postgres :5433 | direct-Prisma QR + parent-Link fixture seeding |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-04-VALID | Tampering | export file integrity (both formats) | medium | mitigate | A "download happened" check alone would pass on a corrupt/empty file; the spec decodes BOTH downloaded formats via jsQR back to the exact expected short URL, so a malformed PNG or SVG that no scanner could read would fail loudly |
| T-15-04-CT | Spoofing | render response content-type | low | mitigate | Asserts image/png and image/svg+xml explicitly, so a regression serving the wrong MIME (which would break real-world download/scan handling) is caught |
| T-15-04-IDOR | Elevation of Privilege | render resolveOwnedQrCode | low | accept | Exercised incidentally under the admin session; role/domain-scoped render denial is Phase 17's job, not re-proved here |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/qr-export-formats.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (spec is registered but skipped under chromium-member) — confirm no new db-isolation truncate race.
</verification>

<success_criteria>
QR-E2E-03 passes: both the PNG and SVG export buttons produce valid, downloadable files that independently decode to the same expected short URL, with correct content-types.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/qr-export-formats.spec.ts` — QR-E2E-03 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/15-qr-studio-e2e/15-04-SUMMARY.md` when done.
</output>
