---
phase: 15-qr-studio-e2e
plan: 03
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
autonomous: true
requirements: [QR-E2E-02]

must_haves:
  truths:
    - "A dynamic QR seeded (via createE2eQrCode) bound to target A resolves through the REAL host-agnostic `GET /q/:code` handler to a 302 whose Location is exactly target A's targetUrl BEFORE any remap (fixture Links carry no UTM, so applyUtmParams passes targetUrl through unchanged)"
    - "After a REAL-UI remap (the `.target-select` dropdown on the selected QR card -> PATCH /api/qr-codes/:id {targetLinkId} -> remapQrCode's $transaction), the SAME printed `/q/:code` URL now 302s to target B's targetUrl — the printed code never changed, only its current destination did (QR-03's headline guarantee)"
    - "Exactly one QrRemapHistory row exists for this QR (direct-Prisma findMany ordered by createdAt asc), matching {fromLinkId: targetA.id, toLinkId: targetB.id} — the audit trail can never drift from the current target because remapQrCode batches the linkId update + history insert in one transaction"
  artifacts:
    - apps/e2e/tests/authed/qr-dynamic-remap.spec.ts
  key_links:
    - "the initial 'bound to target A' precondition is set via a direct-Prisma createE2eQrCode fixture (not the real '+ Dynamischer QR' UI, which always binds to links[0] non-deterministically) — the REMAP is this spec's genuine subject, so only the remap is driven through the real UI (15-RESEARCH.md Pattern 3, Alternatives Considered)"
    - "`GET /q/:code` is host-agnostic (no resolveActiveDomainByHost) — plain request.get against PLAYWRIGHT_BASE_URL resolves, no Host-header machinery needed unlike Phase 12's /:slug (15-RESEARCH.md Anti-Patterns)"
---

<objective>
Prove QR-E2E-02 end-to-end: a dynamic QR's printed `/q/:code` URL resolves to target A, then — after a real Studio remap — to target B, while the printed URL itself never changes, and an ordered remap-history row is recorded. The initial "bound to target A" state is seeded directly via createE2eQrCode (creation is not this test's subject; the deterministic remap is), and only the remap itself is driven through the real `.target-select` dropdown. The redirect assertions hit `PLAYWRIGHT_BASE_URL` directly — `GET /q/:code` is host-agnostic by design, so no Host-header handling is needed (unlike Phase 12's `/:slug`).

This spec asserts REAL, currently-passing behaviour — routes/qrRedirect.ts, lib/qrCodes.ts (remapQrCode's $transaction), and QrCodesView.vue's handleRemapChange were read in full and need no changes. If a genuine app-code gap surfaces, STOP and flag it as a task; do NOT weaken an assertion to make it pass (Phase 11-14 TDD RED->GREEN precedent).

Purpose: Close QR-E2E-02 with a before/remap/after redirect proof plus a DB-asserted ordered history row.
Output: `apps/e2e/tests/authed/qr-dynamic-remap.spec.ts` under the chromium-admin project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-qr-studio-e2e/15-RESEARCH.md
@apps/e2e/src/db.ts
@apps/e2e/src/links.ts
@apps/e2e/src/qr.ts
@apps/e2e/tests/authed/links-crud.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: QR-E2E-02 — dynamic /q/:code remap A->B + ordered history (chromium-admin)</name>
  <read_first>
    - apps/api/src/routes/qrRedirect.ts (VERIFIED: `GET /q/:code` -> prisma.qrCode.findUnique({where:{code}}) -> prisma.link.findUnique({where:{id:qrCode.linkId}}) -> resolveLinkState "ok" -> `reply.code(302).redirect(applyUtmParams(link.targetUrl, link))`; deliberately NO resolveActiveDomainByHost call — host-agnostic, plain request.get resolves; target is read FRESH every request so a remap takes effect on the very next scan)
    - apps/api/src/lib/redirectEngine.ts applyUtmParams (VERIFIED: returns targetUrl UNCHANGED when no utmSource/Medium/Campaign are set — so fixture Links with no UTM give Location === targetUrl exactly)
    - apps/web/src/views/QrCodesView.vue (VERIFIED: the remap control is `<select class="target-select" :disabled="qr.variant!=='dynamic'" :value="qr.linkId" @change="handleRemapChange(qr,$event)">` with one `<option :value="link.id">/{{link.slug}}</option>` per link from GET /api/links; the selected card carries `.qr-card.selected`; handleRemapChange -> remapQrCode(qr.id, newLinkId) -> PATCH /api/qr-codes/:id {targetLinkId}; the `?selected={qrId}` deep-link picks the initial selection)
    - apps/api/src/lib/qrCodes.ts remapQrCode (the $transaction batching qrCode.update({linkId}) + qrRemapHistory.create({qrCodeId, fromLinkId, toLinkId}) — the single write path; history can never drift from the current target)
    - apps/api/prisma/schema.prisma QrRemapHistory model (fields id/qrCodeId/fromLinkId/toLinkId/createdAt)
    - apps/e2e/src/db.ts (createE2ePrisma, BASELINE_DOMAIN_HOSTNAME)
    - apps/e2e/src/links.ts (createE2eLink — seed targetA and targetB with distinct per-test random slugs, no UTM so applyUtmParams passes targetUrl through)
    - apps/e2e/src/qr.ts (createE2eQrCode — seed the dynamic QR bound to targetA; its random `code` is the printed /q/:code)
    - apps/e2e/tests/authed/links-crud.spec.ts (retries:2 for the db-isolation truncate race; chromium-admin-only test.skip; per-test random identifiers; waitForResponse the specific PATCH)
    - 15-RESEARCH.md (Code Example "Dynamic QR remap"; Anti-Pattern: no host-header spike for /q/:code; Assumption A2 random code collision-free-enough)
  </read_first>
  <files>apps/e2e/tests/authed/qr-dynamic-remap.spec.ts</files>
  <action>
    At file top: `test.describe.configure({ retries: 2 })` with a doc-comment (mirror links-crud.spec.ts) — db-isolation.spec.ts truncates QrCode/QrRemapHistory/Link concurrently during the full-suite gate; a whole-test retry with fresh per-test random slugs/code is the collision-free equivalent (the fixtures here are created outside withResetDbLock, so a sibling truncate mid-test is the documented race). Add a `test.beforeEach` that `test.skip`s when testInfo.project.name !== "chromium-admin" (reason: admin-scoped; member/domain-scoped QR authz is Phase 17).

    Use the `{ page, request }` fixtures. Single test "dynamic QR remap changes /q/:code resolution and records ordered history":
    1. createE2ePrisma(); createE2eLink targetA (slug `e2e-qr-a-<hex>`, targetUrl `https://example.com/target-a-<hex>`, no UTM) and targetB (slug `e2e-qr-b-<hex>`, targetUrl `https://example.com/target-b-<hex>`, no UTM); createE2eQrCode({variant:"dynamic", linkId: targetA.id, name:"Remap Test QR <hex>"}). Keep the prisma client for the final history assertion; $disconnect in a finally.
    2. BEFORE remap: const beforeResp = await request.get(`/q/${qr.code}`, { maxRedirects: 0 }); assert beforeResp.status() === 302 and beforeResp.headers()["location"] === targetA.targetUrl.
    3. REMAP via real UI: page.goto(`/qr-codes?selected=${qr.id}`); on the selected card, selectOption the target dropdown to targetB.id via `page.locator(".qr-card.selected .target-select").selectOption(targetB.id)`; waitForResponse a PATCH whose url includes `/api/qr-codes/${qr.id}` (method PATCH). (Both targetA and targetB appear as options because GET /api/links returns them; the select's initial value is targetA.id, so selecting targetB is a real change that fires handleRemapChange.)
    4. AFTER remap: const afterResp = await request.get(`/q/${qr.code}`, { maxRedirects: 0 }); assert afterResp.status() === 302 and afterResp.headers()["location"] === targetB.targetUrl — the SAME /q/:code URL, a NEW destination.
    5. HISTORY (direct Prisma): const history = await prisma.qrRemapHistory.findMany({ where: { qrCodeId: qr.id }, orderBy: { createdAt: "asc" } }); assert history.length === 1 and history[0] matches { fromLinkId: targetA.id, toLinkId: targetB.id }.

    Always await the specific PATCH response before the AFTER redirect fetch — never a fixed sleep. Never add Host-header handling for /q/:code (host-agnostic by design).
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/authed/qr-dynamic-remap.spec.ts --project=chromium-admin</automated>
  </verify>
  <acceptance_criteria>
    - the BEFORE and AFTER redirect assertions hit the REAL /q/:code handler over real HTTP (maxRedirects:0, 302, exact Location) and prove the same printed code resolves to targetA then targetB
    - the remap is driven through the REAL `.target-select` dropdown on the selected card (PATCH awaited), not an API-only shortcut
    - exactly one ordered QrRemapHistory row is asserted via direct Prisma with {fromLinkId: targetA.id, toLinkId: targetB.id}
    - the spec is scoped to chromium-admin (skipped under chromium-member) with per-test random slugs/code and retries: 2
    - passes green against the built compose image; zero apps/api / apps/web diffs
  </acceptance_criteria>
  <done>QR-E2E-02 proves a dynamic QR's printed /q/:code resolves to target A, then to target B after a real-UI remap, with exactly one ordered history row — the printed URL never changing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public internet -> GET /q/:code | unauthenticated, host-agnostic dynamic-QR redirect resolution |
| host browser (admin session) -> PATCH /api/qr-codes/:id {targetLinkId} | authenticated, dual-sided-domain-checked remap |
| test runner -> E2E Postgres :5433 | direct-Prisma fixture seeding + ordered history assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-03-DRIFT | Tampering | remap history vs. current target consistency | medium | mitigate | remapQrCode batches the linkId update + QrRemapHistory insert in one $transaction; the spec asserts BOTH the post-remap redirect (current target) AND exactly one ordered history row, so a drift between audit trail and live destination would fail loudly |
| T-15-03-IDOR | Elevation of Privilege | remapQrCode dual-sided resolveLinkDomainAccess (old + new Link) | low | accept | Exercised incidentally under the admin session; the cross-domain remap-denial case is Phase 17's representative E2E (AUTHZ-E2E-01), not re-proved here |
| T-15-03-GATE | Tampering | /q/:code reads target FRESH per request (no stale cache) | low | mitigate | The AFTER-remap redirect asserts the very next scan resolves to targetB against the SAME code, proving no cached/stale target survives a remap |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/authed/qr-dynamic-remap.spec.ts --project=chromium-admin` passes.
- Re-run once at `--workers=1` and once at default parallelism (spec is registered but skipped under chromium-member) — confirm no new db-isolation truncate race.
</verification>

<success_criteria>
QR-E2E-02 passes: a dynamic /q/:code resolves to target A, then to target B after a real-UI remap, with an ordered remap-history row recorded.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/authed/qr-dynamic-remap.spec.ts` — QR-E2E-02 (chromium-admin)
</artifacts_produced>

<output>
Create `.planning/phases/15-qr-studio-e2e/15-03-SUMMARY.md` when done.
</output>
