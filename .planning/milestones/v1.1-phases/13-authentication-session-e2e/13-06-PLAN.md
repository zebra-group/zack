---
phase: 13-authentication-session-e2e
plan: 06
type: execute
wave: 1
depends_on: [13-02]
files_modified:
  - apps/e2e/tests/auth/resend-rate-limit.spec.ts
autonomous: true
requirements: [AUTH-E2E-07]

must_haves:
  truths:
    - "When the magic-link request rate limit (5 per 15 minutes) is tripped, the login UI shows the exact German copy 'Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst.' — not a silent failure"
    - "The real @fastify/rate-limit limiter drives the 429; the E2E bypass header is NEVER sent by this spec"
  artifacts:
    - apps/e2e/tests/auth/resend-rate-limit.spec.ts
  key_links:
    - "LoginView.vue's sendMagicLink sets error.value to the German string on response.status === 429, rendered via <p v-if=\"error\" class=\"error-inline\">"
    - "MAGIC_LINK_RATE_LIMIT = { max: 5, timeWindow: '15 minutes' } (rateLimit.ts), IP-keyed — so a 6th same-IP request 429s"
    - "this is the ONLY magic-link-sending spec that omits x-e2e-bypass, so its IP bucket is driven solely by its own burst (all sibling specs use the bypass)"
---

<objective>
Prove the resend rate-limit UX (AUTH-E2E-07): a magic-link request burst that trips the real limiter surfaces a clear German UI message rather than failing silently. This spec deliberately does NOT use the E2E rate-limit bypass (unlike every other magic-link spec) — the whole point is to observe the login form's real behavior under a genuine 429. The assertion target is LoginView.vue's exact German copy, quoted verbatim in 13-RESEARCH.md, not a paraphrase.

Purpose: Close AUTH-E2E-07 against the real @fastify/rate-limit limiter and the real login UI.
Output: One isolated spec file under the `auth` project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentication-session-e2e/13-RESEARCH.md
@apps/web/src/views/LoginView.vue
@apps/api/src/plugins/rateLimit.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH-E2E-07 — tripped rate limit shows the exact German UI copy</name>
  <read_first>
    - apps/web/src/views/LoginView.vue (sendMagicLink: `if (response.status === 429) { error.value = "Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst."; return; }`; the input, the "Magic Link senden" button, the `<p v-if="error" class="error-inline">{{ error }}</p>` render target)
    - apps/api/src/plugins/rateLimit.ts (MAGIC_LINK_RATE_LIMIT max 5 / 15 minutes, IP-keyed; the allowList bypass mechanism it must NOT receive here)
    - apps/e2e/tests/auth.setup.ts (the shape of a magic-link POST — but here send it WITHOUT the bypass header)
    - .planning/phases/13-authentication-session-e2e/13-RESEARCH.md (Rate-Limited Resend UI Copy, Verbatim — isolate this spec's own never-reused email; MUST NOT send x-e2e-bypass)
  </read_first>
  <files>apps/e2e/tests/auth/resend-rate-limit.spec.ts</files>
  <action>
    Use a dedicated, never-reused email `ratelimit-<short-random>@e2e.kurzly.local`. This spec sends NO x-e2e-bypass header anywhere — it is the only magic-link-sending spec that drives the real limiter, so its IP bucket is driven solely by its own requests (all sibling specs bypass the limiter and never consume it).

    Deterministically trip the bucket, then assert the UI: fire enough same-IP requests to exceed MAGIC_LINK_RATE_LIMIT.max (5). Pre-exhaust via the browser page's own request context (`page.request.post("/api/auth/sign-in/magic-link", { data: { email, callbackURL: "/", errorCallbackURL: "/auth/error" } })`) — send it 6 times with NO bypass header so the bucket is over the limit (this avoids fighting LoginView's idle→"sent" state transition on the first success). Then drive the REAL UI once: page.goto("/login"), fill the email input with the same email, click the "Magic Link senden" button. The button's fetch hits a now-tripped bucket → 429 → LoginView sets error.value to the German copy.

    Assert the exact rendered string is visible: locate the `.error-inline` element (or getByText of the exact copy) and expect it to contain "Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst." — the verbatim copy, not a paraphrase. Confirm the form did NOT transition to the "Link gesendet" (sent) state (the 429 branch returns before state becomes "sent"), so the error is shown beneath the still-visible send button.

    Keep this spec's email unique so its burst never collides with another spec's rate-limit accounting, and never send the bypass secret.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/auth/resend-rate-limit.spec.ts --project=auth && grep -q 'Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst.' apps/e2e/tests/auth/resend-rate-limit.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - the spec asserts the verbatim German copy "Zu viele Anfragen. Bitte warte kurz, bevor du es erneut versuchst." is visible
    - the spec never sends the rate-limit bypass header/secret anywhere (verify by manual review — the whole point is a genuine 429)
    - it trips the real limiter with a same-IP burst exceeding 5 and drives the real LoginView UI for the assertion
    - it uses a dedicated, never-reused email
    - passes green against the built compose image
  </acceptance_criteria>
  <done>AUTH-E2E-07 proves the login UI surfaces the exact German rate-limit message under a real 429.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → app /api/auth/sign-in/magic-link | rate-limited endpoint (email-bomb protection) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-11 | Denial of Service | magic-link email-bombing | medium | mitigate | AUTH-E2E-07 proves the real @fastify/rate-limit limiter enforces 5/15min and the UI communicates the throttle clearly; spec never sends the bypass, so it exercises the true protection |
| T-13-12 | Denial of Service (masking) | bypass header hiding the real limiter | high | mitigate | Automated verify greps that x-e2e-bypass is absent from this spec, structurally guaranteeing the 429 is genuine and not bypass-masked |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/auth/resend-rate-limit.spec.ts --project=auth` passes.
- grep confirms the verbatim German copy is asserted AND the bypass header is absent.
- Because the email is unique and all sibling magic-link specs use the bypass, this spec's bucket trip never affects (or is affected by) them.
</verification>

<success_criteria>
AUTH-E2E-07 passes: a genuine 429 renders the exact German rate-limit copy in the login UI.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/auth/resend-rate-limit.spec.ts` — AUTH-E2E-07 (real limiter, no bypass)
</artifacts_produced>

<output>
Create `.planning/phases/13-authentication-session-e2e/13-06-SUMMARY.md` when done.
</output>
