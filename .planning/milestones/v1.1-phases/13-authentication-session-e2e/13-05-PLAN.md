---
phase: 13-authentication-session-e2e
plan: 05
type: execute
wave: 1
depends_on: [13-02]
files_modified:
  - apps/e2e/tests/auth/logout-route-guard.spec.ts
autonomous: true
requirements: [AUTH-E2E-06]

must_haves:
  truths:
    - "Clicking the sidebar 'Abmelden' button signs out and lands the user on /login"
    - "After logout the session is gone SERVER-SIDE — GET /api/auth/get-session returns no user, not just a client-side redirect"
    - "A fresh unauthenticated context visiting a requiresAuth route (e.g. /) is redirected to /login by the router guard"
  artifacts:
    - apps/e2e/tests/auth/logout-route-guard.spec.ts
  key_links:
    - "AppShell.vue handleLogout = authSession.signOut() (POST /api/auth/sign-out) then router.push({name:'login'})"
    - "router/index.ts beforeEach redirects any requiresAuth route to /login when !isAuthenticated (UX convenience T-02-14, backed by independent server re-verification)"
    - "distinct from Phase 11 storage-state.spec.ts which only proves an authenticated session REACHES an authenticated route"
---

<objective>
Prove the logout half of the session lifecycle (AUTH-E2E-06): logging out ends the session server-side AND unauthenticated access to a dashboard route redirects to /login. Phase 11's storage-state.spec.ts already proves the forward direction (an authenticated session reaches authenticated routes); this spec proves the reverse — signOut genuinely revokes the server session (not just a client redirect), and the router guard bounces a cold, unauthenticated visitor to /login.

Purpose: Close AUTH-E2E-06 against the real AppShell logout button and router guard.
Output: One spec file under the `auth` project.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-authentication-session-e2e/13-RESEARCH.md
@apps/web/src/layouts/AppShell.vue
@apps/web/src/router/index.ts
@apps/web/src/stores/authSession.ts
@apps/e2e/tests/authed/storage-state.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: AUTH-E2E-06 — logout ends the session; unauthenticated access redirects to /login</name>
  <read_first>
    - apps/web/src/layouts/AppShell.vue (handleLogout: `await authSession.signOut(); await router.push({ name: "login" })`; the button `title="Abmelden"` in the sidebar footer — locate it with getByTitle("Abmelden") or an equivalent role-based locator)
    - apps/web/src/stores/authSession.ts (signOut calls logout() → POST /api/auth/sign-out, then clears user)
    - apps/web/src/router/index.ts (beforeEach: a requiresAuth route with !isAuthenticated returns { name: "login" })
    - apps/e2e/tests/auth.setup.ts (how to establish a session via magic link with the x-e2e-bypass header — reuse this mechanic, do NOT depend on the setup project's storageState)
    - apps/e2e/src/users.ts (createAllowlistedUser)
  </read_first>
  <files>apps/e2e/tests/auth/logout-route-guard.spec.ts</files>
  <action>
    Establish a real session inline (this project has no storageState): createAllowlistedUser(prisma, { email }) with a dedicated `logout-<short-random>@e2e.kurzly.local`, POST /api/auth/sign-in/magic-link with the x-e2e-bypass header, findMagicLinkUrl, page.goto(link), wait for the "Dashboard" nav to render (authenticated).

    Part 1 — logout: click the sidebar footer button located by `page.getByTitle("Abmelden")`. Assert the final URL is `/login` (expect page to be at the login route). THEN independently prove the server session is gone: `page.request.get("/api/auth/get-session")` returns no user (the httpOnly cookie was revoked by POST /api/auth/sign-out, not merely hidden client-side).

    Part 2 — route guard for the unauthenticated: open a SEPARATE browser context with NO storageState (browser.newContext()), navigate to `/` (a requiresAuth route), and assert the router guard redirects to `/login` (final URL is the login route, the login card renders). This is distinct from storage-state.spec.ts's forward proof. Optionally also assert a second protected route (e.g. /links) redirects, to show the guard is route-meta-driven, not path-specific.

    Disconnect any Prisma client in afterAll.
  </action>
  <verify>
    <automated>./scripts/e2e-compose.sh tests/auth/logout-route-guard.spec.ts --project=auth</automated>
  </verify>
  <acceptance_criteria>
    - establishes its own session (magic link + bypass), not via the setup project's storageState
    - clicks getByTitle("Abmelden") and asserts the URL becomes /login
    - asserts GET /api/auth/get-session returns no user AFTER logout (server-side revocation)
    - a fresh no-storageState context visiting / is redirected to /login
    - passes green against the built compose image
  </acceptance_criteria>
  <done>AUTH-E2E-06 proves logout revokes the server session and the guard redirects unauthenticated dashboard access to /login.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host browser → app /api/auth/sign-out | session-revocation request |
| host browser → protected routes (no session) | unauthenticated access attempt |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-13-09 | Elevation of Privilege | stale session after logout | high | mitigate | AUTH-E2E-06 asserts get-session returns no user server-side after logout, proving the httpOnly cookie is genuinely revoked, not just client-cleared |
| T-13-10 | Broken Access Control | unauthenticated reach of dashboard routes | medium | mitigate | Spec proves the router guard redirects a cold context to /login; noted that the guard is UX-only (T-02-14) and every API route independently re-verifies the session |
</threat_model>

<verification>
- `./scripts/e2e-compose.sh tests/auth/logout-route-guard.spec.ts --project=auth` passes.
- get-session assertion confirms server-side revocation, distinguishing a real logout from a client-only redirect.
</verification>

<success_criteria>
AUTH-E2E-06 passes: logout ends the server session and unauthenticated dashboard access redirects to /login.
</success_criteria>

<artifacts_produced>
- `apps/e2e/tests/auth/logout-route-guard.spec.ts` — AUTH-E2E-06
</artifacts_produced>

<output>
Create `.planning/phases/13-authentication-session-e2e/13-05-SUMMARY.md` when done.
</output>
