---
milestone: v1.0
audited: 2026-07-23
status: passed
scores:
  requirements: 53/53
  phases: 10/10
  integration: 5/5
  flows: 5/5
gaps:
  requirements: []
  integration: []
  flows: []
tech_debt:
  - id: "IN-01 (Phase 6, carried)"
    item: "LinkDetailView edit-submit drops trackingEnabled"
    severity: low
    note: "Pre-existing Phase-6 issue surfaced during Phase 8 review; not a Phase-8 regression. Deferred, tracked for a follow-up."
  - id: "WR-07 (Phase 7)"
    item: "No delete path for QR codes"
    severity: low
    note: "A feature, not a defect — needs UX copy the locked Copywriting Contract doesn't yet have. Deferred."
  - id: "WR-09 (Phase 7)"
    item: "One-static-QR-per-link invariant enforced only in code comments, not a DB-level unique index"
    severity: low
    note: "Needs a dedup migration + a new error code/status. Deferred; current create path does not permit duplicates in practice."
  - id: "UTM denial coverage (Phase 9/8 seam)"
    item: "Denial suite does not explicitly exercise UTM fields"
    severity: low
    note: "UTM fields are plain Link columns written through the same PATCH /api/links/:id gate the denial suite already covers — no separate endpoint, no additional authorization surface. Advisory only."
decisions_for_review:
  - id: "D-10-02"
    item: "OIDC/SSO is ENV-configured with a read-only admin status card, not a live in-dashboard credential form"
    note: "Deliberate, documented. Deviates from a literal reading of the prototype's OIDC card + AUTH-05 success-criterion wording. Consistent with the whole self-hosted/ENV-everywhere product and the installed better-auth (1.6.23 has no sso plugin; genericOAuth configures statically at boot; secret stays out of the app DB). Literal in-dashboard credential entry with DB-stored, live-reconfigurable config is a well-scoped follow-up if desired."
  - id: "WR-01 (Phase 10)"
    item: "SSO auto-provisions any IdP-authenticated user (no invite gate), unlike magic-link's invite-only allowlist"
    note: "Ratified as spec-intended: AUTH-07 / success criterion 3 says a user 'newly created via SSO' gets the Member role, so auto-provisioning is in-scope. Risk bounded by the least-privilege default (accountRole=member, zero domains) — a self-provisioned SSO user can see/do nothing until an admin assigns domains. A disableSignUp gate would break criterion 3."
---

# Milestone v1.0 Audit — Kurzly

**Audited:** 2026-07-23
**Status:** ✅ passed
**Verdict:** All 53 requirements satisfied, all 10 phases verified, all cross-phase seams wired, Core Value E2E flow intact. No blockers. A small, explicitly-deferred tech-debt list and two decisions worth a conscious review (both already documented and defensible).

## Requirements Coverage (3-source cross-reference)

53/53 requirements marked **Complete** in `.planning/REQUIREMENTS.md`, each traced to a phase whose `VERIFICATION.md` is `passed`. No orphaned requirements (every REQ-ID appears in a phase verification). No `unsatisfied` or `partial` requirements.

## Phase Verifications

| Phase | Name | Status |
|-------|------|--------|
| 01 | Test Infrastructure, Monorepo & Deployment Scaffolding | ✅ passed |
| 02 | Magic-Link Auth, App Shell & Domain Authorization Core | ✅ passed |
| 03 | Domains & Multi-Domain TLS Routing | ✅ passed |
| 04 | Links Management & Bulk Import | ✅ passed |
| 05 | Core Redirect Engine | ✅ passed |
| 06 | Internal Tracking & Analytics | ✅ passed |
| 07 | QR Codes (Static + Dynamic, QR Studio) | ✅ passed |
| 08 | UTM Builder + Custom OG Metadata | ✅ passed |
| 09 | Team Management & Domain-Scoped Authorization Enforcement | ✅ passed |
| 10 | OIDC/SSO Integration | ✅ passed |

## Cross-Phase Integration (5/5 seams wired)

1. **Authorization end-to-end** — `requireDomainAccess`/`scopedDomainIds` (Phase 2) gate every Links/QR/Analytics path; the Phase-9 account-admin bypass lives inside those two helpers so every call site inherits it. Phase 9's denial suite (`team-domain-denial.integration.test.ts`) proves a Member cannot reach an unassigned domain's resources across the real route surface (links CRUD + analytics, qr CRUD + remap-history + render, csv import, analytics). WIRED.
2. **Redirect engine composition** — Phase 5 precedence + Phase 6 tracking + Phase 7 static-QR scan attribution + Phase 8 UTM + forwardQuery all compose in one `state==='ok'` branch with correct ordering: the `?qr=` marker is stripped before `forwardQuery`, UTM applies before the visitor-query merge. WIRED.
3. **`/q/:code` reuse** — Phase 7 imports `recordClickHook`/`incrementLifetimeScans`/`brandCtx` and the `redirectEngine` primitives rather than duplicating them; single `clickEvent.create` site. WIRED.
4. **Additive auth** — Phase 10 `genericOAuth` registered only when OIDC env is set; magic-link path unchanged; SSO users default to `member` with zero domains (`accountRole` `input:false`). WIRED.
5. **Core Value E2E** — domain (3) → verify → link (4) → redirect (5) with tracking (6) + QR (7) + UTM (8) hanging off the same link; route registration order avoids SPA-fallback shadowing. WIRED.

**Verification suite state at audit:** apps/api 540 tests, apps/web 256 tests, workspace `tsc --noEmit` clean.

## Tech Debt & Deferred (non-blocking)

See frontmatter `tech_debt`. All low-severity and explicitly deferred: the Phase-6 `trackingEnabled` edit-submit drop, the QR delete path (a feature), the static-QR uniqueness DB index, and the advisory UTM denial-coverage note.

## Decisions for Review

See frontmatter `decisions_for_review`. Two conscious, documented decisions: D-10-02 (ENV-configured OIDC with a read-only status card) and WR-01 (SSO auto-provisioning ratified per AUTH-07, bounded by least-privilege). Both are defensible and reversible; neither blocks the milestone.

## Note

The verification data reflects the state after an in-run WSL filesystem fault was cleanly recovered (Phase 10 lost no work; see `10-03-SUMMARY.md`'s recovery note).
