node_modules/
dist/
.env
.env.*
!.env.example
apps/api/src/generated/

# Editor/OS
.DS_Store
*.log

# Coverage
coverage/

# GSD planning artifacts (plans, summaries, research, debug journals).
# Intentionally local-only: these are internal working documents, not part of
# the published project. They were also purged from the git history when the
# repository was prepared for open-source release, so re-adding them here would
# undo that. Keep them out of commits.
.planning/

# Playwright E2E (apps/e2e) — live session cookies and run artifacts, never committed
apps/e2e/playwright/.auth/
apps/e2e/playwright-report/
apps/e2e/test-results/
